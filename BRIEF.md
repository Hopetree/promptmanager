# promptmanager — 需求合同（BRIEF）

> 唯一需求来源。dsh 只按本文件开发；本文件由 host_manger 维护，**dsh 不得修改**。
> 未覆盖且影响交付的决策 → 写 `QUESTIONS.md` 并停手，不要自行拍板。
>
> **开工前必读**：`/root/greenhouse/STANDARDS.md`（开发规范，优先级高于本文件）。
> **红线摘要**（详见 STANDARDS，冲突以 STANDARDS 为准）：
> ① 服务类产物监听 `0.0.0.0`（内网可达），不许只绑 127.0.0.1；端口只用台账分配的（**8765–8770**）；
> **定端口前先 `ss -ltn` 实测当前占用并避开已占用端口**；定好后写入项目 README（默认端口+监听地址）、
> PROGRESS（选择理由 + ss 实测输出）与配置默认值；**合集台账由 host_manger 登记，不要写它**；
> 暴露在局域网 ⇒ **必须有认证**；
> ② **依赖**：能不自己造就别造，**大胆用成熟开源框架/库**（这是默认路径，不必请示）；
> 只要过两道闸门——**安全**（无未修高危 CVE、维护活跃、pin 版本 + 提交 lockfile）与
> **开源协议**（MIT/Apache/BSD/ISC 直接用；MPL/LGPL 可用；GPL/AGPL/无协议 → 先提 QUESTIONS）。
> 禁止手搓基础设施（HTTP/WS/markdown/加密/cron/布局/DB 驱动等）；
> 依赖与协议登记到 `docs/dependencies.md`；
> ③ 不改系统配置（systemd/nginx/防火墙）、不装全局包、不对外发布、不写自己项目目录以外的文件；
> ④ 逐可验收单元 commit；凭据只从本项目 `_env/` 读，不得进代码/日志/commit。

| 项 | 值 |
| --- | --- |
| 版本 | v45 |
| 状态 | 待开发 |
| 项目路径 | `/root/greenhouse/projects/promptmanager` |
| 目标用户 | 第一用户 = 用户本人（现在用 203 上的 PromptHub 管 prompt）；同类用户 = 想要**轻量、自托管、数据自持**的 prompt 管理工具的开发者 |
| 立项理由 | 用户决策「把 PromptHub 的 **prompt 模块**功能 clean-room 重做为一个轻量自研工具」；上游实测 prompt 模块 ≈4,640 行（28 端点）+ 前端 ≈27,979 行，且为 **AGPL-3.0**、被五端与四个非 prompt 模块包裹 → 自研 P0 才能"轻 + 许可自由"；证据与竞品核对见 `/root/greenhouse/README.md` 与工作区 `projects/greenhouse/topics/promptmanager.md` |
| 需求方 | 用户 |
| 规格作者 / 验收 | host_manger |
| 实现 | dsh |
| 更新时间 | 2026-09-18 |

## 1. 一句话目标

做一个**单进程、单端口、单文件数据库**的轻量 Prompt 管理器（Node + SQLite），
能结构化存取 prompt（标题/用户提示词/系统提示词/备注/文件夹/标签/收藏）、**中文可用的全文检索**、
**版本历史（diff + 回滚）**、**模板变量填值**、**Markdown 预览**、**JSON 导入导出**，
带认证的管理后台界面，数据全在本地。

> **成功标准不只是「管得好」，而是「被用出去」**（用户 2026-09-18 明确）：同一份数据要能被 **dsh 插件 / MCP 客户端 / CLI / 浏览器** 等外部客户端
> **直接取用**（而不是靠人工复制粘贴）。因此本项目除「管理面」外，还包含「**客户端就绪面**」（Token 认证 + CORS + 使用侧 CLI）与 **MCP server**
> —— 见 §4 的 FR-15~FR-18、§5 的「外部客户端就绪」、§11 的阶段 6–7。

## 2. 背景与用途

- **给谁用**：用户本人（第一用户）与其 AI 助手；未来同类自托管用户。
- **解决什么问题**：现有工具（PromptHub，AGPL-3.0）功能很全但**太重**——五端（desktop/web/mobile/cli/cloudflare）
  共享同一套 layout/路由/store/DB，skills/rules/mcp/plugin 四个非 prompt 模块与 prompt 工作区缠绕；
  实测 prompt 模块服务端 ≈4,640 行、前端 ≈27,979 行。用户只想要 **prompt 管理**那一部分，并且要**轻、可控、许可自由**。
- **为什么自己写**：① 上游 AGPL-3.0，照搬代码即传染（要闭源/自由分发只能 clean-room）；② 裁剪 fork 要背长期分叉；
  ③ 更轻的替代（prompt-shelf / PromptPal / promptmanager 扩展）**都已停更**，不宜做基底。
- **成功是什么样**：228 上 `node bin/pm.mjs` + `npm start` 起一个服务（8767），
  用浏览器能完成「登录 → 新建/编辑 prompt → 文件夹与标签归类 → 中文搜索 → 看版本 diff 并回滚 → 填变量复制出成品 → 导出 JSON 备份」全流程，
  且数据只在自己机器上（`data/pm.db` + `data/media/`），备份=拷文件。

## 3. 非目标（本次明确不做）

- **不做 RAG / 向量检索 / 知识库问答 / AI 调用**（无 LLM provider、无 embedding、无对话）——这是已否决方向
- **不做 Skill / MCP / Agent / rules 管理**（上游的非 prompt 模块，本项目完全不碰）
- **不做多端**：不做桌面壳（Electron/Tauri）、不做移动 App、不做 CLI 之外的客户端
- **不做多用户/团队/权限/分享链接/协作**（单用户；只有"登录"这一层认证）
- **不做外部同步**：无 WebDAV / S3 / 云同步 / 多端合并（P2，明确不做）
- **不做媒体上传与预览、多视图（画廊/看板/表格/图谱）、关系树、输出格式序列、私有加密文件夹、7 语言 i18n**（P1/P2）
- **不做 PromptHub 数据迁移工具**（若用户后续要求，单独立项；本项目的导入导出用自有 JSON 格式）
- **不部署**：只交付部署文件；实际安装/开机自启/反代由 host_manger 在用户要求时执行
- **不接外部网络**：不引 CDN、不发遥测、运行时不对外发起 HTTP 请求（**例外**：本机/内网的 MCP 与 CLI 客户端经本服务 API 取数，属设计内路径）
- **本期不做浏览器扩展与桌面客户端**（用户 2026-09-18 明确：「暂时不做，我们暂时只保证服务端提供可对接的服务」）：
  本项目只交付**服务端 + 可对接面**（REST API + API Token + CORS + 使用侧 CLI + MCP）；
  客户端形态各自单独立项，且**服务端不得为任何特定客户端做假设或特化**（不许出现"给某个扩展专用的接口/字段"）
- **公网暴露的"支持"与"执行"分开**：用户 2026-09-18 拍板「内网、外网都会部署，都要支持」→ 本项目**必须同时适配两种部署形态**
  （内网直连 HTTP / 公网经反代 HTTPS，见 §5 与 AC-28）；但**实际暴露动作**（反代、证书、防火墙、DNS）属**安全红线**，
  **只由 host_manger 在用户授权后执行**；dsh 只交付"两种形态都能用"的配置开关、样例与文档，**不动系统配置**。
  **环境口径见 §9 D-27**：8767 上的 systemd 实例 = **测试环境**（数据由 host_manger 管理、可随意改动；用户验证时也会改）；dsh 的临时实例 = **开发环境**（数据可随意重建）；**生产环境**待未来公网部署时另立。

## 4. 功能需求（分级编号）

**P0（必须交付，本次范围）**

- FR-1 认证：单用户登录（口令哈希 + 服务端会话 + HttpOnly cookie）、登出、`/api/me`、登录失败限流；
  管理员口令由 CLI 工具设置（`user set-password`，从 stdin 读，不落日志）
- FR-2 prompt CRUD：字段 = 标题 / 用户提示词 / 系统提示词 / 备注 / 文件夹 / 标签 / 收藏；
  创建、读取（单个 + 列表）、更新、删除
- FR-3 列表与筛选：按 `q`（搜索）、`folder_id`、`tag`、`favorite` 筛选，分页（`limit`/`offset`），按更新时间倒序
- FR-4 文件夹：树形（`parent_id`）、CRUD、排序字段
- FR-5 标签：CRUD + 重命名（重命名时合并同名）+ 每个标签的 prompt 计数
- FR-6 全文检索：中文可用（见 §6.6 语义契约），匹配 标题/用户提示词/系统提示词/备注
- FR-7 版本历史：每次内容变更**自动留档**（`version_no` 递增），列表、两版本 **unified diff**、**回滚**（回滚 = 新版本，不删历史）
- FR-8 变量：从 prompt 文本提取 `{{变量}}`（见 §6.5），填值后渲染出成品文本（不落库）
- FR-9 Markdown 渲染：服务端渲染 + **XSS 净化** + 代码块高亮
- FR-10 导入导出：自有 JSON 格式（见 §6.4）全量导出 / 导入（`replace` 与 `merge` 两种模式）
- FR-11 管理后台界面：登录页 + prompt 列表（搜索/筛选）+ 编辑器（标题/双提示词/备注/文件夹/标签/收藏）+
  版本历史面板（diff/回滚）+ 变量填值面板（渲染后一键复制）+ Markdown 预览；**响应式**（手机浏览器可用）；亮/暗主题跟随系统
- FR-12 前端与 API 由**同一个进程同一个端口**提供（单端口交付）
- FR-13 部署文件（交付物 + 验收项，见 §5 与 §8）
- FR-10b **导入校验收紧（v9 新增）**：`POST /api/import` 必须**拒绝**缺少 `title` 或 `user_prompt` 的 prompt 条目
  （400 `invalid_import`，且**不改动任何数据**）。背景：阶段 5 验收实测"缺 `title` 的条目被接受并落库为空串"，
  会在列表里产生无名条目；后端是唯一入口（CLI/MCP 也可能导入），故校验放后端，UI 不必兜。
- FR-11b **导入界面的二次确认（v8 新增，来自阶段 9 前的验收发现）**：前端提供导入入口时，**`replace` 模式必须二次确认**
  并明确写出"将清空现有全部 prompt / 文件夹 / 标签 / 版本历史"；`merge` 模式同样要显示将要新增的条目数。理由：
  后端 `replace` 语义是忠实的**清空重建**——一个合法但内容为空的导出文件就能把整库清空（实测 200 + `imported` 全 0），
  **后端不加保护是对的**（契约如此），把安全提示放在 UI 层是最合适的位置。
- FR-14 数据自持：单文件 SQLite（WAL）+ 本地 `data/` 目录，重启不丢数据

**P0 追加（用户 2026-09-18 拍板「统一做 token/CORS 与 MCP」）**

- FR-15 **API Token**：创建（明文只返回一次）/ 列出（不含明文）/ 撤销；支持 `Authorization: Bearer <token>`，与 cookie 会话并存；记录 `last_used_at`
- FR-16 **使用侧 CLI**：`pm get`（检索 / 按 id 取）、`pm render`（变量渲染出成品）—— **只有 HTTP 一条路**
  （`PM_API_URL` + `PM_API_TOKEN`；连不上或没有 token 必须非 0 退出且 stdout 为空）；
  `pm token create|list|revoke` 采用**双通道**（见 §5，解决"第一个 token 从哪来"的引导问题）
- FR-17 **CORS**：默认关闭；按 env 白名单显式开放（**禁止 `*`**）；不使用 `Access-Control-Allow-credentials`
- FR-18 **MCP server（stdio）**：工具面只读 —— `prompt_search` / `prompt_get` / `prompt_render`，供 dsh 等 agent 直接取用 prompt
- FR-19 **使用记录（usage）**（用户 2026-09-18 拍板「可以做」，由 P1 提升为 **P0**）：记录 prompt 的**取用**（谁/何时/经哪条通道），
  用于回答「这个库到底有没有被用出去」；契约见 §5，验收见 AC-27

- FR-40 **视觉重设计（用户 2026-09-18 追加：*"目前的 UI 效果不太行、太单调，要有设计感和美感"*）**：
  在**保持现有全部功能与约束**的前提下，对**全部界面**做一次有明确美学方向的重设计。
  - **必须先定方向、再做打样、经用户挑选后铺开**（见 §11 阶段 10A/10B）——不许一上来就全站重做。
  - **允许的手段**：`antd` 的 `ConfigProvider theme`（`token` + 组件级 `components` 定制）、CSS 变量、**少量**自定义 CSS（渐变/纹理/描边/阴影/动效/背景），
    以及 dsh 已有的设计类技能（`frontend-design` 定方向、`design-system` 落令牌、`extract-design-system` 提炼设计原语、
    `dsh-web-artifact-designer` 出设计稿、`webapp-testing` 截图、`dsh-commercial-ui-ux` 做商业级界面评审）。
  - **禁止**：引入第二套 CSS 框架/样式体系；自建基础组件或改写组件库源码；引 CDN；**复制他人站点的内容、文案、图片或品牌元素**
    （`extract-design-system` 只允许提炼**设计原语**：色阶、间距、字号、圆角等数值语言）。
  - **必须保住**（不得回归）：单进程单端口与前端同托管、响应式（含 390×844 手机）、亮/暗跟随系统、`zh_CN`、
    组件库硬约束（AC-20/AC-21）、Markdown 净化硬化（BRIEF v4 §5）、全量测试全绿、无 CDN。
  - **美学要求**：有明确的设计意图与视觉层级（不是把 antd 默认观感换个颜色）；排版与间距成体系；**不落"模板/半成品"俗套**；
    编辑器与列表是主战场（高频界面优先打磨）；动效克制、不影响可用性。

- FR-40b **结构/排版重设计必须落地（用户 2026-09-18 选定方向时追加）**：
  用户的明确反馈是「**不仅仅好了样式还对排版进行了重新设计，这才是我需要的设计，让他保持这种设计感**」——
  因此本阶段的验收对象**不只是配色/圆角**，而是**设计稿里的结构与排版**。**设计稿即契约**：下列结构改动必须真正实现，
  **不许"只换 theme token、布局退回 antd 默认"**：
  1. **编辑器**：从「四个标签页（编辑/版本历史/变量填值/Markdown 预览）」改为**三栏常驻**——左（列表）· 中（编辑主体）·
     右（**版本历史 / 变量填值 / Markdown 预览**三块同时可见）；标签页不再承载这三块。
  2. **列表页**：顶部**统计行（KPI）**4 项（prompt 总数 / 近 30 天取用 / 最近改动版本 / 检索耗时）+ 左侧栏（文件夹树 / 标签 / 工作区）
     + 筛选行（搜索·文件夹·标签·排序·只看收藏）+ 表格密度（1280 一屏 ≥10 行）+ 分页 + **底部状态条**（数据位置 / 备份提示 / 筛选命中数）。
  3. **顶栏**：品牌 + 环境标识（`pm.host:8767` 之类）+ 导航（使用统计 / API 令牌 / 导入导出）+ 主题跟随指示 + 用户区。
  4. **移动端（390×844）**：卡片式列表 + 抽屉编辑（按设计稿形态），不得退化为横向溢出的表格。
  5. 空态 / 加载态 / 错误态也要用新设计语言表达（不是 antd 默认的干瘪 Empty/Spin）。
  - **固定 `data-testid` 约定（供 AC 机械复验，名字不许改）**：`pm-topnav`、`pm-sidebar`、`pm-kpi-row`、
    `pm-filter-row`、`pm-statusbar`、`pm-editor`、`pm-panel-versions`、`pm-panel-variables`、`pm-panel-markdown`。
  - **与设计稿的一致性**：实现完成后必须产出**并排对照**（同一界面：设计稿截图 vs 实现截图，≥4 组），
    任何**未落地 / 降级 / 偏离**的点必须在 `PROGRESS.md` 里列成「降级清单」并写明原因——**不许默默简化**。

- FR-41 **使用优先（用户 2026-09-18 纠偏，最高优先级）**：用户原话——
  「**这个服务不是只用来管理 prompt 的，更是需要提供给用户（管理员）日常使用的**，但是目前很明显**连最基本的复制都没有**，
  更别说其他方便用户使用的功能，**界面排版也没有考虑使用和管理分离**，大部分都是使用而不是管理。建议好好参考 PromptHub 页面。」
  - **定位（写死）**：本服务的**第一身份是「日常使用工具」**——主流程是「**找到 → 一键复制 → 贴走**」；
    「管理」（增删改、文件夹、标签、导入导出、统计）是**次要视角**，不应占据默认落地页。
  - **FR-41a 一键复制（最基本，必须有）**：
    ① **使用视图的每个条目**都要有**显式的复制按钮**（复制该条的 `user_prompt`；hover 显现，移动端常显）；
    ② 复制成功弹 **`已复制` 提示**，并**计入使用记录**（BRIEF FR-19，`channel=session`）；
    ③ **编辑器内**也要能直接复制「用户提示词 / 系统提示词」，**不必先打开变量面板**；
    ④ 变量面板的「渲染后复制成品」（含 `missing` 提示）**保留并强化**；
    ⑤ 若某条含变量：复制按钮旁提供「**填值后复制**」入口（直接跳变量填值并聚焦复制）。
  - **FR-41b 使用 / 管理 分离（IA 重排）**：
    ① 顶栏提供**两个模式的显式切换**：**使用**（**默认落地**）与 **管理**；用 `localStorage` 记忆上次模式；
    ② **使用视图**：搜索优先（大搜索框 + 自动聚焦）· **卡片网格**（标题 + 正文摘要 2–3 行 + 标签 + **一键复制**）·
       收藏置顶 · 排序（最近更新 / **最近使用** / 标题）· 点击卡片打开**详情弹窗**（正文大字号全宽、**常驻复制按钮**、变量填值 + 复制、Markdown 预览）·
       **不出现管理操作**（删除等不得占位）；
    ③ **管理视图**：保留现有表格（KPI 行 / 筛选行 / 编辑 / 删除 / 文件夹 / 标签 / 导入导出 / 底部状态条）；
    ④ 两个视图共用同一套令牌与组件库，**不新增第二套样式体系**。
  - **FR-41c 快捷与效率**：`/` 聚焦搜索 · `Ctrl/Cmd+K` 打开搜索 · `Esc` 关闭详情弹窗 · 列表键盘上下选择 + `Enter` 复制当前项 ·
    **双击条目打开详情** · 复制后可选自动关闭详情（默认关，行为写进设置/说明）。
  - **FR-41d 移动优先**：390×844 下**默认使用视图**；复制按钮**常显且触控尺寸 ≥44×44 CSS px**；详情以全屏抽屉呈现。
  - **参考依据（host_manger 实测 PromptHub 前端产物，2026-09-18）**：使用侧文案频次 —— **复制 58 · 分发 73 · 测试 46 · 收藏 46 ·
    变量 33 · 快捷 22 · 快捷键 19 · 拖拽 11 · 双击 3**；视图模块有 `PromptGalleryView` / `PromptKanbanView` / `PromptGraphView` /
    `PromptTableView` / `PromptDetailModal` / `PromptQuickRewriteDialog` / `ImagePromptReverseModal` / `usePromptNativeFullscreen`。
    **本期明确不做**：多视图（画廊/看板/图谱）、媒体、AI 改写/反推、对外分发；**本期必须补**：复制、快捷、收藏优先、详情弹窗、使用优先的 IA。
  - **FR-41e 从 PromptHub 实页考察吸收的交互（2026-09-18 host_manger 登录其 203 实例逐屏看过；**只借交互与信息架构，不抄代码/文案/配色**）**：
    1. **详情面板底部固定操作条**（最高价值）：选中一条后，窗格底部常驻一排——**`复制提示词`（主按钮，蓝色）** · `版本历史` · `删除`（危险色）。
       （其 `AI Test` 我们不吸收——本期无 AI 能力。）
    2. **复制时的变量对话框**：条目含 `{{变量}}` 时，点复制先弹 **「填变量」** 对话框：
       `请填写变量值（自动记忆）` + 每个变量一个输入框 + **实时预览渲染结果** + 底部 `取消` / **`复制结果`（主按钮）**。
       **变量值要按 prompt 自动记忆**（下次打开预填上次的值）。
       **⚠️ 未填写的变量必须原样保留（v15，用户 2026-09-19 决定）**：预览与复制结果里，**没填的变量保持 `{{name}}` 原文**，
       **不得替换成空串**（用户原话："**没填写的变量按照原样输出**"）。这与服务端 §6.5 的既有语义完全一致
       （`/api/prompts/:id/render` 对**未提供值**的变量就是"原样保留 + 列进 `missing`"，阶段 4 已实测通过）
       → **前端只传已填写的变量，不要给未填变量传空串**；对话框上要显示「**未填 N 个**」并在预览里可见 `{{name}}`。
    3. **正文阅读切换**：`显示纯文本`（Plain Text）切换 · **编辑 / 预览** 双模式 · **全屏展开** 图标（长文阅读/复制体验）。
    4. **列表/表格增强**：视图切换档位（**分栏 / 表格 / 卡片** 三种，按此顺序显示，**默认分栏** —— 见 FR-46；原「列表」档自 v19 删除）· 表格列含 **`变量数` 与 `取用次数`**（与 BRIEF FR-19 的 usage 对齐）·
       **行密度 S/M/L** 选择器 · 「列设置」。
    5. **顶栏常驻大搜索框**（居中、显眼，一眼就能"先搜再抄"）；左栏常驻 **文件夹树 + 标签（带计数）+ 收藏**。
    6. **版本历史弹窗三视图**：`详情` / **`对比版本`** / `表格`，且每个版本带 **`变更备注`（CHANGE NOTE）** —— 我们的版本记录可加**可选备注**字段（UI 层输入，不改 API 契约时可存进现有 notes 或用本地扩展，落地方案由实现方在 PROGRESS 说明）。
    7. **变量语法增强（可选，P1）**：PromptHub 支持 `{{name:示例值}}`（冒号后为默认值）。我们的契约是 `{{name}}`；
       本阶段**不强制**，但**不得把 `{{name:示例}}` 判成变量**（现契约下 `:` 不在变量名字符集内，会原样保留 → 行为可接受，需在 README 已知限制里写明）。
  - **FR-41f 明确摒弃（PromptHub 有但我们不做）**：`Skills` / `Rules` 两个模块（非 prompt 管理）· `AI Test` / AI 改写 / 图片反推 / 表格里的 `AI Response` 列 ·
    `Image` 类型 prompt 与媒体上传 · `Relationship Graph`（关系图谱）· `Relations` / `Custom Output Format` 折叠区 · 云同步与多端 · 7 语言 i18n。
    理由：本期定位是"**轻量的日常使用工具**"，这些是上游"一站式 AI 工具箱"的冗余面。
  - **新增固定 `data-testid`（供 AC 复验，名字不许改）**：`pm-use-viewmode`、`pm-view-split`、`pm-split-list`、`pm-split-item`、`pm-tag-cloud`、`pm-tag-chip`、`pm-folder-rename`、`pm-folder-delete`、`pm-view-table`、`pm-view-card`、`pm-use-card`（**v19 清理**：`pm-mode-use` / `pm-mode-manage` 已随 FR-43 移除，此处不再作为约定；分栏相关为 v19 新增）（v18 对齐实现：卡片网格容器由 `pm-use-grid` 演进为 `pm-use-viewmode` + `pm-view-card`）、
    `pm-copy-<id>`（每条一个）、`pm-detail`、`pm-search-input`、`pm-use-sort`、
    **`pm-detail-actions`（详情底部操作条）**、**`pm-vars-dialog`（填变量对话框）**、**`pm-vars-preview`（实时预览区）**、**`pm-var-input-<name>`（每个变量输入框）**。

- FR-42 **导航归位 + 信息克制（用户 2026-09-19 反馈，原话：①「导航太不合理，很多属于管理的东西应该放到管理页面，类似于设置」
  ②「很多页面和卡片都有一些提示信息，比如数据库信息，监听信息，操作提示的过量意思等」）**：
  - **FR-42a 使用视图只留"用"**（把管理的东西全部收到管理页，像"设置"一样）：
    - **使用视图顶栏只允许**：品牌 · **使用/管理 模式切换** · 主题切换 · 用户/登出。
    - **使用视图顶栏必须移除**：环境标识（`host:port`）、**使用统计**、**API 令牌**、**导入 / 导出**、**新建**（这些都是管理动作）。
    - **使用视图左栏只作筛选**：文件夹树 / 标签可点击筛选；**不得**出现"新建 / 重命名 / 删除文件夹或标签"等管理按钮；**删除**解释性长句。
    - **管理视图**承载全部管理动作（新建 / 导入导出 / 使用统计 / API 令牌 / 文件夹与标签的增删改），并新增一个 **`设置`（或"关于"）** 入口。
  - **FR-42b 默认不解释；解释只出现在用户主动查看处**：
    - **从可见 UI 删除**（面向开发者/背景知识的解释性文案）：`DATA_DIR`、`/api/...`（接口路径）、`SQLite 单文件`、`监听 127.0.0.1:…`、
      `本次查询 xx ms`、`筛选命中 N 条`、`渲染不写库`、`XSS 净化 + 代码高亮，前端不重写`、`version_no 递增`、`点文件夹 = 只看该文件夹…` 这类操作说明。
    - **必须保留**（属"操作反馈 / 功能态 / 安全"，不算噪声）：`已复制` · `未填 N 个（预览与复制结果里保留原样占位符）` · 保存成功 · 错误提示 · **危险动作的二次确认（如 replace 清空警示）**。
    - 技术信息与背景说明**统一收进**「设置 / 关于」（管理视图内）：数据文件位置、备份方式、监听地址、版本号、依赖与许可摘要等，**用户主动查看才出现**；也可用 `title` tooltip 承载。
    - **卡片精简**：**删除内部 id**（`#1` 这类）；底部只留**一行极简元信息**（建议：版本 · 变量数 · 更新时间；取用次数可留），标签最多显示前 2 个 + 更多。
    - **底部状态条**：**从使用视图移除**；管理视图可保留一行极简（如"数据文件：/var/lib/promptmanager/pm.db · 共 6 条"），或同样并入"设置/关于"。
  - **新增固定 `data-testid`（供 AC 复验）**：`pm-settings`（设置/关于入口按钮）、`pm-about`（设置/关于面板）。

- FR-43 **取消「使用 / 管理」双模式；管理动作并入表格视图（用户 2026-09-19 反馈）**——
  用户原话：「**实际上我们并不需要一个管理页面，目前的管理页面也只是提供了编辑删除按钮，完全可以放表格视图里面**」。
  - **移除**：`pm-mode-use` / `pm-mode-manage` 两个模式开关、以及"模式记忆"（localStorage 中相应项一并清理）。
  - **管理动作落到表格视图**：表格行内提供「**编辑 / 删除**」（删除需二次确认）+ 复制/收藏；卡片与列表视图保持"用"为主
    （可有轻量编辑入口，但**不常驻删除按钮**）。
  - ~~**顶栏（唯一一栏）只保留**：品牌 · 主题图标按钮 · `＋新建` · `⋯更多` · 用户/登出；
    `⋯更多` 内放 导入/导出 · 使用统计 · API 令牌 · 设置（关于）。~~
    **【自 v20 起由 FR-47 / FR-48 修订】**：顶栏改为 **品牌 · `＋新建` · `⋯更多` · 主题图标 · 登出**（此顺序，**去掉用户信息**）；
    `⋯更多` 子项顺序改为 **使用统计 → API 令牌 → 导入/导出 → 关于 → 已登录信息 → 登出**，并**删除「文件夹与标签」入口**。
    **【v27 修订】**：其中「**已登录：<用户名>**」这一只读项**删除**，原位置改为「**修改密码**」⇒ 顺序变为
    **使用统计 → API 令牌 → 导入 / 导出 → 关于 → 修改密码 → 登出**（见 FR-67）。
  - ~~**左栏**仍是只读筛选；文件夹与标签的增删改入口收进 `⋯更多`。~~
    **【自 v20 起由 FR-49 / FR-50 修订】**：左栏**不再只读** —— 文件夹区标题行常驻 `+`（新建）、行内悬浮提供 新建子文件夹 / 重命名 / 删除（见 FR-49）；
    标签区改为**胶囊云**且**不提供增删改**（标签随 prompt 编辑产生，见 FR-50）。
- FR-44 **明暗主题切换：单个图标按钮（用户 2026-09-19 反馈）**——用户原话：「**我们还是需要明暗主题切换按钮，跟随主题是默认行为，明暗主题切换只需要一个图标**」。
  - 用一个**图标按钮**（`pm-theme-toggle`）切换：点击在 **亮 → 暗 → 跟随系统** 之间循环；**默认（首次进入 / 清空 localStorage）= 跟随系统**；
    当前模式通过图标形态与 `title` tooltip 表达（跟随系统用中性图标）。**移除**现在的文字标签「亮色 · 跟随系统」。

- FR-45 **FIX：新建 prompt「未保存即入库」（用户 2026-09-19 报的 bug）**——
  - **复现（host_manger 实测，临时实例）**：基线 1 条 → 点 `新建` → **记录立刻变 2 条**，新增条目标题为 `未命名 prompt`；
    `Esc` 关闭后该条目**仍在库里**；再次点 `新建` 又 +1（我测 2 次就留下 2 条垃圾）。
  - **要求**：**未点「保存」不得在服务端入库**。新建流程必须是"内存草稿 → 点保存才 POST 创建"。
    若实现方要保留"草稿不丢"，**只允许**把草稿存在**前端 localStorage**，**不得**在服务端留下记录；
    且**取消 / 关闭 / 刷新**都不得产生新记录。
  - 测试环境库（8767）已确认**干净**（6 条数据，无「未命名 prompt」残留），无需数据清理。（原措辞「生产库」按 D-27 更正为「测试环境库」）

- FR-47 **顶栏只留四块并固定顺序；删掉登录用户信息（用户 2026-09-19 反馈 ①）**——
  用户原话：「**菜单只需要保留新建，更多（菜单集），主题切换，登出这几块，并按照这个顺序排列，删除目前的登录用户信息**」。
  - 顶栏自左至右：**品牌** · **`＋新建`** · **`⋯更多`** · **主题切换图标** · **登出**（四块顺序不许变，`pm-topnav` 内按 DOM 顺序断言）。
  - **删除顶栏的登录用户信息**（不再出现头像首字母 `ad` 与用户名 `admin`）：断言 `pm-topnav` 的可见文本**不含** `admin`。
  - 登出 = 图标 + 文字「登出」的**无边框文字按钮**（`data-testid="header-logout"` 沿用）；主题按钮仍为单图标三态（FR-44 不变），只是位置移到「更多」右侧。
- FR-48 **`⋯更多` 的子项与顺序（用户 2026-09-19 反馈 ④）**——
  用户原话：「**菜单"更多"里面的子菜单顺序按照使用统计、API令牌、导入/导出、关于（目前的设置）、已登录信息、登出显示，去掉文件夹和标签管理，根本不需要**」。
  - 子项顺序固定：**使用统计 → API 令牌 → 导入 / 导出 → 关于 → 已登录信息 → 登出**（按 DOM 顺序断言，逐项相等）。**【v27 修订】其中「已登录信息」由「修改密码」取代**（见 FR-67 / AC-67）。
  - **删除「文件夹与标签」入口**（该管理面板不再需要）。
  - 原「设置」**文案改为「关于」**，但 **`data-testid="pm-settings"` 保持不变**（AC-40 按 testid 定位，仍然有效）。
  - 「已登录信息」= 只读展示（`已登录：<用户名>`，**不可点**、灰字小号）；「登出」保留危险色。
- FR-49 **文件夹区：新建入口 + 层级显示优化（用户 2026-09-19 反馈 ②）**——
  用户原话：「**左边文件夹这里显示一个+的按钮，用来新增文件夹，并且目前的文件夹的显示效果有点简陋，层级显示效果很差，请优化**」。
  - **新建入口**：分组标题行「文件夹」右侧**常驻 `+` 按钮**（`data-testid="folder-create"` 沿用）= 新建**顶级**文件夹；
    每个文件夹行**悬浮**显示 `＋`（在该文件夹下新建**子**文件夹）与 `⋯`（**重命名 / 删除**；删除**须二次确认**，含子项时提示）。
    testid：`pm-folder-rename` / `pm-folder-delete`（多实例，悬浮态出现）。**左栏不再只读**（撤销 FR-43 的"只读筛选栏"，见 D-24）。
  - **层级显示优化**（对照 PromptHub 的文件夹树）：① 有子项的文件夹显示**展开 / 收起三角**（点击折叠后子项**不可见**）；
    ② 层级**按级缩进**（子级相对父级 `left` 至少 +14px）；③ 文件夹图标区分**闭合 / 展开**两态；
    ④ 每行右侧显示**该文件夹（含子文件夹）的条目数**（浅灰小字，**不用彩色徽标**）；⑤ **当前筛选中的文件夹**有明确选中态（浅色底 + 左侧 3px 色条）；
    ⑥ 行悬浮高亮；⑦ 长名用省略号（`…`），面板宽度内不得出现单字成行、横向滚动或裁切。
- FR-50 **标签区改为「胶囊云」（用户 2026-09-19 反馈 ③，参考 PromptHub）**——
  用户原话：「**左边标签的显示，不要显示成目前这种效果，请参考 prompthub 的标签效果**」。PromptHub 参考（我方实图核对）：标题行 `标签` + 右侧 `全部 14`；
  标签 = **浅灰底圆角胶囊、`#` 前缀、深灰字、多列流式换行（每行约 3 个）、无数字计数**。
  - 标题行「标签」右侧显示 `全部 N`（N = 标签总数）；不需要齿轮 / 设置入口。
  - 标签改为**胶囊 chip 流式换行**（`flex-wrap`），**删除当前的竖排列表与右侧计数徽标**；形状圆角胶囊、浅灰底深灰字（暗色主题用等价低对比底色）、
    高 24–28px、横向内边距 8–12px；文本以 **`#` 前缀**（如 `# 115`）；**视觉上不得出现计数**（需要时只允许 `title` tooltip）。
  - 选中态（筛选生效）= 主色描边或深色底；悬浮态轻微变色；窄屏自然折行，**不得**横向滚动、单字成行或裁切；空态一句话 `还没有标签`。
  - testid：容器 `data-testid="pm-tag-cloud"`，每个胶囊 `data-testid="pm-tag-chip"`（多实例）。**点胶囊 = 原筛选行为不变**（再点取消）。
  - **不做**标签的增删改（标签随 prompt 编辑产生/清理，见 D-24）。

- FR-59 **品牌图形全站统一：同一枚图标，只换尺寸（用户 2026-09-19 第三次定调）**——
  用户原话：「**算了，我们不要插画，所有地方都使用你生成的icon，使用不同的尺寸就行，这样你更容易把握好图的显示效果**」。
  → **作废**此前两版草案（用户提供的文件夹插画、host_manger 自绘插画**均不使用**）；**全站只有这一枚图标**（提示符 `>_`，方案 A，用户已认可）。
  - **资产（已定稿，仓库外交接点）**：`/root/greenhouse/assets/promptmanager-icon/`（先读同目录 `INSTALL.md`）。尺寸阶梯位图
    `promptmanager-{16,20,24,32,40,48,64,72,96,128,144,180,192,256,512}.png`；矢量 `promptmanager-icon.svg`（彩色）、
    `promptmanager-icon-glyph.svg`（单色 `currentColor`，**本期不用**，随包备用）；favicon 家族 `favicon.svg` / `favicon.ico`。
  - **尺寸映射表（唯一权威，不得自创尺寸、不得拉伸位图）**：

    | 场景 | 用什么 | 尺寸 |
    | --- | --- | --- |
    | 浏览器标签页 | `favicon.svg`（矢量）＋ `favicon.ico`（16/32/48/64） | — |
    | iOS 主屏 | `promptmanager-180.png` → `apple-touch-icon.png` | 180 |
    | PWA / manifest | `promptmanager-192.png` → `icon-192.png`；`promptmanager-512.png` → `icon-512.png` | 192 / 512 |
    | **顶栏品牌 mark** | `promptmanager-icon.svg`（矢量） | **26** |
    | **登录页**（表单上方居中） | `promptmanager-96.png` | 96 |
    | **列表空态**（无 prompt 时） | `promptmanager-72.png` | 72 |
    | **关于页顶部** | `promptmanager-48.png` | 48 |
    | 预留 | `promptmanager-128.png` / `-256.png` | 128 / 256 |

  - **硬规则**：① 同一枚图标、只换尺寸 —— 不重画/不改色/**不加阴影·发光·描边·旋转·动效**，**不得**引入第三种图形；
    ② 位图不得放大超过原生像素（要更大就用更大那张或直接用 SVG）；③ 装饰位（登录页/空态/关于页）`aria-hidden="true"`、**无点击、无动效**，
    空态**有数据时不显示**；④ 顶栏 mark 替换现在的字母方块，FR-51 的「点 logo 回主页」仍成立；
    ⑤ `web/index.html`：`<title>` 严格等于 `PromptManager`，加 `<link rel="icon" type="image/svg+xml" href="/favicon.svg">`、
    `<link rel="icon" href="/favicon.ico" sizes="any">`、`<link rel="apple-touch-icon" href="/apple-touch-icon.png">`、`<meta name="theme-color" content="#5e6ad2">`；
    ⑥ 不引外链/CDN/web font；不把 SVG 以 base64 塞进 CSS；单张引用图 ≤250KB。

- FR-60 **注释与用户可见文案的「漂移」清理（用户 2026-09-19 要求"一起处理"遗留项）**——
  背景：v17~v22 连续改了导航、菜单、视图与品牌，代码里留下少量**与现状不符的注释/文案**（扫描证据见下）。**本项只清理表述，不改任何行为。**
  - **必清清单（已定位）**：
    1. `web/src/components/Workspace.tsx:73` 的注释仍写「…导入导出 / 使用统计 / API 令牌 / **文件夹与标签** / **设置** 收进顶栏 `⋯更多`」——
       与现状不符（FR-48 已删该入口；「设置」已改名「关于」；且管理动作现在也在**详情面**里）。改为描述现状的注释。
    2. `web/src/styles/app.css` 中仍在使用 **v17 前的旧术语「使用视图 / 管理视图」**（如「使用视图的卡片网格」「移动端：使用视图的复制按钮」）——
       自 FR-43 取消双模式后这两个名字已废止，现行为**分栏 / 表格 / 卡片**三档视图。统一改为现行说法。
    3. 用户可见文案里的小写品牌名：`web/src/pure.ts` 的导入报错文案「不是 **promptmanager** 的导出文件」→ 改为「不是 **PromptManager** 的导出文件」。
       ⚠️ **只改展示文案**，`app: 'promptmanager'` 这个**契约值不许动**（动了会破坏已有导出文件的兼容）。
  - **顺手全仓扫同类（只改注释与用户可见文案）**：搜索以下**已废止概念**在 `web/src` / `src` 中出现的地方并逐一核对：
    `pm-mode-use`、`pm-mode-manage`、`模式记忆`、`列表视图`、`pm-view-list`、`pm-kpi-row`、`pm-filter-row`、`pm-statusbar`、
    「管理视图」、「使用视图」、「文件夹与标签」、「拿来就用」（若作为 `aria-label`/placeholder 保留语义则**合法**）、菜单名「设置」、
    小写 `promptmanager`（作为**品牌名**时）。
  - **明确不许改（白名单，属历史记录或契约）**：
    ① `BRIEF.md` / `PROGRESS.md` / `VERIFY.md` / `QUESTIONS.md` 等**按时间记录的历史文本**（含"已作废/已移除"的表述、旧 AC 注解）；
    ② 导入导出的 **`app` 标识值** `promptmanager`；③ 文件名/路径（如 `/promptmanager-96.png`、`promptmanager-icon.svg`）；
    ④ 测试中对历史行为的断言（如"旧键会被清理"这类测试）；⑤ `package.json` 的包名。
- FR-61 **构建体积：主 chunk 瘦身（长期 P1，本次一并处理）**——
  实测（v22 构建）：`dist/web/assets/index-*.js` = **1,265 KB（gzip 400 KB）**，超过 Vite 默认 500 KB 告警阈值，**每次构建都报警**。
  - 目标：**主 chunk ≤ 500 KB（未压缩）**；总 gzip 体积不增（理想是下降）。
  - 允许手段：路由/重组件**动态 import 懒加载**（编辑器、Markdown 预览、版本 diff、导入导出、使用统计、令牌抽屉等只在需要时加载）、
    Vite `build.rollupOptions.output.manualChunks` 合理分包（antd / react 等 vendor 分离）、移除未使用依赖与死代码。
  - **禁止**：引入 CDN 外链或任何外部资源（前端硬约束）；为瘦身而删功能；改接口契约。
  - 自证：贴**前后**构建输出（每个 chunk 的体积与 gzip）+ 首屏 Network 主 chunk 体积；并说明分包策略与理由。

- FR-62 **编辑器支持全屏（用户 2026-09-19 追加需求，与清理/体积同批交付）**——
  用户原话：「**编辑页面支持全屏，方便编辑和预览内容比较大的 prompt**」。
  动机：正文很长时，现在的编辑区是"小框里滚大文本"，看不全、对不齐；要的是**把屏幕真的让给内容**，而不是只把外壳藏起来。
  - **入口**：编辑器顶栏（与「返回详情 / 返回列表」同排）提供**全屏切换按钮**（图标 + tooltip；`data-testid="editor-fullscreen"`，激活态可换图标）。
  - **进入方式**：优先调用 **Fullscreen API**（`element.requestFullscreen()`）；若浏览器拒绝或不支持 → **自动退回「应用内全屏」**（铺满整个视口、隐藏外壳），**不得报错、不得什么都不发生**。
  - **全屏期间必须隐藏的外壳**：顶栏（`pm-topnav`）、左侧筛选栏（`pm-sidebar`）、中间列表（`pm-split-list`）——三者 `offsetParent === null`；
    编辑器区域宽度 ≥ **视口 95%**，且 `data-fullscreen="true"` 标记在编辑器容器上（供 AC 机械判定）。
  - **大内容体验（本条的核心，不能只做壳）**：
    1. **编辑区高度 ≥ 视口 70%**（长文本不再挤在小框里）；
    2. **编辑区与右侧（预览 / 变量填值 / 版本历史）各自独立滚动**：滚动编辑区时整页不动，滚动右栏时编辑区不动；
    3. 全屏下 **工具栏常驻**（保存 / 返回 / 全屏切换 不被滚走）；
    4. **无横向滚动条**（除非正文里真有超宽代码块，此时只在编辑区内横滚）；
    5. 右栏三个模块（预览 / 变量 / 版本）仍按 FR-56 的顺序，且在全屏下可用（不因空间变化被裁掉）。
  - **退出路径（三条都要能用）**：① 再点同一个按钮；② 按 `Esc`（**只退出全屏，不关闭编辑器、不丢未保存内容、不触发"返回"**）；③ 用户用浏览器自身方式退出全屏时，UI 状态要**同步**回非全屏（监听 `fullscreenchange`，不出现"按钮显示全屏中但实际已退出"的错位）。
  - **不得回归**：详情面的「全屏展开」（`pm-detail-fullscreen`，预览用）与本次的**编辑全屏**是两回事，各自保留、互不顶替；
    移动端/窄屏全屏时退化为单栏（编辑 / 预览 二选一，可切换），不得出现横向溢出。
      **【v25 修订】**：本条的「浏览器全屏 + 隐藏外壳」形态由 **FR-63 / AC-63 取代** —— 改为只隐藏编辑器内部 `editor-list`，编辑区与右栏 **1:1**，且**不调用** Fullscreen API。

- FR-63 **编辑器全屏改回「应用内全屏」：只藏内部列表栏 + 左右平分；按钮对齐详情页（用户 2026-09-19 反馈 ①）**——
  用户原话：「**编辑页面的全屏并不是为了让浏览器显示全屏，而是想要页面的内容显示关键信息，全屏的时候不要显示【Prompt 列表】左边栏即可，然后平均分配两边部分。
  并且编辑页面的全屏按钮和显示效果目前不好，可以参考详情页的全屏按钮效果和排版**」。
  - **进入全屏后**：只隐藏**编辑器内部左栏**「Prompt 列表」（`editorList`，本次补 `data-testid="editor-list"`），
    把剩余空间**平均分给编辑区与右栏**（编辑栏宽 : 右栏宽 = **1 : 1**，容差 ±5%）；**不调用** `requestFullscreen()`，
    断言 `document.fullscreenElement === null`（不再进入浏览器全屏）。
  - **按钮对齐详情页**：编辑页全屏按钮改为与详情面 `pm-detail-fullscreen` **同款** —— 同 Button 类型/尺寸、
    `icon` = `ExpandOutlined`（未全屏）/ `CompressOutlined`（已全屏）、文案「**全屏展开**」/「**退出全屏**」；`editor-fullscreen` testid 保留；
    位置放在编辑器工具栏（与 返回 / 删除 / 保存 同排）。
  - **退出**：再点按钮；或按 `Esc`（编辑态处于全屏时，`Esc` **只退出全屏**，不返回、不丢未保存内容；不在全屏时 `Esc` 维持原行为）。
    退出后**左栏恢复**、两栏宽度回到原布局。
  - **不做**：不改详情面 `pm-detail-fullscreen`；不改编辑器其它行为。
- FR-64 **FIX：版本 diff 把「备注没改」显示成有变动（用户 2026-09-19 反馈 ②，有版本历史的页面全中）**——
  用户原话：「**版本对比里面的 notes 内容必然会显示成有变动，即使两个版本之间没有改过备注也会显示，有版本历史的页面都有这个问题**」。
  - **我方复现证据（临时实例，`/api/prompts/:id/diff` 原样输出）**：
    ```
    只改 user_prompt（备注相同）→ 正常：hunk 只含 [user_prompt] 的 -U1/+U2
    仅"备注尾部换行"不同（'N' 对比 'N\n'）：
      @@ -5,4 +5,4 @@
       [system_prompt]
       S
       [notes]
      -N
      \ No newline at end of file
      +N          ← 两边文本完全相同，却被判为"有变动"
    ```
  - **根因**：`src/services/versions.ts` 的 `snapshotText()` 用 `\n` join 四段、**文本不以换行结尾**，且字段值**尾部换行未归一化** →
    jsdiff（`createTwoFilesPatch`）的「No newline at end of file」语义把**文件尾部的换行差异**当成内容变更；`[notes]` 恰是**最后一段**，所以**必然中招**。
  - **修复要求**：① `snapshotText()` 输出**以 `\n` 结尾**；② 每个字段值**尾部空白归一化**（如 `replace(/\s+$/, '')`）；
    ③ **加单测**：两版仅"备注尾部换行不同" → diff **不得**出现该字段的 `-`/`+` 行；两版备注**真的不同** → 仍要正确显示变更；
    ④ 修复后 `\ No newline at end of file` 标记应基本不再出现。
- FR-65 **FIX：「复制」在内网 HTTP 下失效（用户 2026-09-19 反馈 ③）— 评估结论：是 bug（机制限制本有兜底，但有入口漏接）**——
  用户原话：「**详情页面全屏模式下，无法进行任何复制操作，点击复制都是显示"浏览器拒绝了剪贴板访问，请手动选中复制"，这是机制还是bug需要你评估后决定处理方式**」。
  - **评估（host_manger）**：**机制上确有约束，但项目已设计了绕过它的兜底，只是有入口没接上 ⇒ 判为 bug。**
    实测证据：用**内网 IP** 访问时 `window.isSecureContext === false` 且 **`navigator.clipboard === undefined`**（`writeText` 不存在）；
    浏览器只在**安全上下文**（HTTPS / localhost / 127.0.0.1）提供 async clipboard API —— 内网用 IP + HTTP 访问时它根本不存在。
    代码现状：`web/src/use-copy.ts` **已经有** `navigator.clipboard` → `document.execCommand('copy')` 兜底（注释里明确写了内网 http 不是安全上下文），
    但 **`web/src/components/VariablePanel.tsx` 直接 `await navigator.clipboard.writeText(...)`，绕过了兜底** → 凡走该路径的复制**必然失败**并弹出那句提示。
  - **修复要求**：① **所有复制入口统一到同一实现**（把 `writeClipboard` 从 `use-copy.ts` 导出，`VariablePanel` 等一律改用它；
    全仓 `grep -rn "navigator.clipboard" web/src` **只允许 1 处** = `use-copy.ts`）；② 兜底也失败时才提示手动复制，成功仍提示「已复制X」；
    ③ **加单测**：模拟 `navigator.clipboard` 不存在 → 断言走 `execCommand` 且返回成功。

- FR-66 **详情页去掉冗余的「当前字段 + 预览」头部（待办池 R-1，用户 2026-09-19 提、2026-09-20 派）**——
  现象：分栏视图右侧详情面，字段页签下方还有一行「`当前字段` 下拉 + 👁 预览」——它是 `web/src/components/MarkdownPreview.tsx` **自带的头部**：
  编辑器页传 3 个字段（用户提示词/系统提示词/备注）时该下拉**有用**；而详情页只传 **1 个**字段、且 `label` 写死为「当前字段」⇒ 下拉退化成单选项、
  与上方页签**功能重叠**，而右侧「👁 预览」**并不是按钮**（是带 tooltip「在服务端完成渲染」的说明标签）。
  - **要求**：`MarkdownPreview` 在 **`fields.length === 1`** 时**隐藏头部**（字段下拉 + 「预览」说明标签）；`fields.length > 1`（编辑器页）**保持现状**。
    详情页字段切换仍由**上方页签**负责；「预览 / 源码」「显示纯文本」「全屏展开」**不变**。
  - 单字段场景下那条「服务端渲染」tooltip 一并隐藏（符合 D-20/FR-42b 的信息克制原则）。
- FR-67 **`⋯更多` 菜单：删「已登录」、新增「修改密码」弹窗（用户 2026-09-20 提出）**——
  用户原话：「**子菜单添加修改密码的选项，弹窗进行密码修改，并且把目前的那个已登录:admin去掉，新增按钮就放到那里**」。
  - **菜单**：删除「`已登录：<用户名>`」只读项；在**它的位置**新增「**修改密码**」（`key: password`、`LockOutlined` 图标、`data-testid="pm-menu-password"`、普通项，非 danger）。
    最终顺序（AC 逐项断言）：**使用统计 → API 令牌 → 导入 / 导出 → 关于 → 修改密码 → 登出**。
  - **弹窗**（antd `Modal`，`data-testid="pm-password-modal"`）：三个密码框（**当前密码 / 新密码 / 确认新密码**，均 `Input.Password`、`autoComplete="current-password"` / `"new-password"`）；
    底部「取消 / 确认修改」；`Esc`、点遮罩、取消都能关；提交中禁用按钮。
  - **前端校验**：新密码 **≥ 8 个字符**（Unicode 码点计数）且**不得与当前密码相同**；「确认新密码」必须一致（不一致 → 表单级错误提示、不提交）。
  - **新接口（契约变更）**：`POST /api/password`，body `{ "old_password": string, "new_password": string }`（**新增入参名，沿用 snake_case**）。
    - 成功 → **204**（无 body）；前端提示「密码已更新」并关闭弹窗。
    - **当前密码错误 → `400` `{"error":"invalid_old_password"}`**（**不得用 401**：前端把 401 当"会话失效"会把人踢回登录页，用户体验错误）。
    - 新密码不合规 → `400` `{"error":"invalid_password","message":"…"}`（message 说清规则）。
    - 未认证 → `401`（沿用现网闸门）。
    - **按现有登录限流机制计数**：当前密码连续错误 ≥ 5 次 / 60s → **429**（复用 `login_attempts` 的 username+IP 键；成功一次即清零）。
    - **不得回显密码**：响应体、日志、错误信息里**不得出现任何明文密码**（AC 会 grep 服务日志 + 断言响应体字段）。
  - **会话语义（实现方按此执行）**：改密码成功后 **保留当前会话**（本人不掉线）并**吊销该用户的其它所有会话**（其它设备/浏览器退出登录）；
    弹窗成功提示里补一句「已在其它登录会话中退出」。**同时**：CLI `user set-password` 的现有行为（删该用户全部会话）**保持不动**（那是本地管理动作，语义不同），
    但要在 `PROGRESS.md` 里写明两者差异。
  - **顺带**：`关于` 页「维护」区补一句：改口令**也可以**从界面 `⋯更多 → 修改密码` 完成（CLI 方式保留）。

- FR-68 **备注（notes）= 纯文本：不做预览、不走 Markdown 处理（用户 2026-09-20 提出）**——
  用户原话：「**备注不需要支持markdown渲染，直接按照文本输入输出即可，所以也不要做备注的预览和markdown处理**」。
  - **编辑器页**：右栏「Markdown 预览」的字段列表**去掉「备注」**（只留 **用户提示词 / 系统提示词**，`fields.length` 变 2 ⇒ 其头部下拉仍保留）；
    备注输入框仍是普通多行文本（不参与渲染、不送服务端渲染）。
  - **详情页**：字段页签选中「**备注**」时，正文区**按纯文本原样显示**（转义文本，不做 Markdown 渲染）：
    即便备注里写了 `# 标题`、`**粗体**`、`- 列表`、`[链接](…)` 等，也**原样呈现字符**，DOM 中**不得**出现 `<h1>`/`<strong>`/`<ul>`/`<a>` 这类渲染元素；
    且**不得**因查看备注而请求 `POST /api/render/markdown`（AC 用网络层断言）。`预览 / 源码` 两个按钮在备注页签下**等价**（都是纯文本），或由实现方选择在备注页签下隐藏这两个控件——
    若隐藏，需在 PROGRESS 说明理由。
  - **不得外溢**：列表/卡片的摘要仍取 `user_prompt`（不变）；变量填值与 `render` 结果仍只含用户/系统提示词（不变）；**导入导出格式不变**；
    版本 diff 里的 `[notes]` 段仍是**文本对比**（FR-64 的修复不受影响）。
- FR-69 **详情页：标题下方展示备注（用户 2026-09-20 提出，样式参考 PromptHub）**——
  用户原话：「**详情页的标题下面显示备注信息，文字效果注意按照备注的效果设计一下，可以参考prompthub**」。
  - **PromptHub 参考（host_manger 从其界面实测）**：标题下方紧跟一行**灰色描述文本**（比标题小约 4–6px、浅灰、**纯文本无 Markdown 效果**、与标题**间距紧凑**），其下方才是元信息行。
  - **落位**：在本详情面**标题行（★ + 标题 + v/时间/取用 + 去编辑）之下、字段页签行（`pm-detail-fields`）之上**新增一行备注：
    - 固定 `data-testid="pm-detail-notes"`；
    - **纯文本**渲染（与 FR-68 一致，不解析 Markdown）；**自动换行**；
    - 字号 **13px**（标题 18px，差 5px 落在 PromptHub 的 4–6px 区间）、颜色用**次级/三级灰**（`token.colorTextSecondary` 或等价，须与标题形成明显层级）、行高 1.6；
    - 与标题行间距**≤ 10px**（紧凑，参考 PromptHub）；
    - **超长处理**：最多显示 **2 行**后省略（`-webkit-line-clamp: 2` 或等价），并给该元素加 `title` 属性承载**全文**（悬浮可见）；
    - **备注为空时整行不渲染**（不占位、不留空白）。
  - **不做**：不加交互（不可点、不弹层）、不改「备注」字段页签（仍保留，用于阅读全文与切换正文区）。

- FR-70 **拖拽排序：卡片视图的条目 + 左栏文件夹树（用户 2026-09-20 提出，样式参考 PromptHub）**——
  用户原话：「**我希望卡片和目录都支持拖动排序，注意调节排版效果，不要因为加入的拖动效果就让排版变得不舒服，可以参考prompthub**」。
  - **范围（先做这一层，别扩）**：① **卡片视图**里的 prompt 条目**同层级重排**；② **左栏文件夹树**里**同一父级下**的文件夹重排。
    **不做**（留待以后）：跨文件夹拖动改变归属、跨父级移动文件夹、拖拽改变标签。
  - **排序模型（D-28）**：新增排序档「**自定义**」（前后端 `sort=custom`，按 `sort_order, id` 升序）：
    · `folders` 表**已有** `sort_order` + 索引 `idx_folders_parent(parent_id, sort_order, id)` → 复用；
    · `prompts` 表**没有** `sort_order` → **新增一次迁移**（`ALTER TABLE prompts ADD COLUMN sort_order INTEGER NOT NULL DEFAULT 0` + 需要时的索引），
      既有数据按当前顺序回填（保证升级后顺序不乱）。
    · **拖拽落库**：新增接口（契约变更，二选一由实现方定并在 BRIEF/PROGRESS 记录）：
      `PATCH /api/prompts/order`，body `{ "ids": number[] }`（按新顺序给出的**当前视图内的完整 id 序列**）；或等价的逐个 `sort_order` 更新接口。
      文件夹同理：`PATCH /api/folders/order`，body `{ "parent_id": number|null, "ids": number[] }`。
      要求：**服务端校验 id 集合合法**（不存在/越权/重复 → 400），且**幂等**。
    · **与现有排序档位的关系**：拖拽后**自动切到「自定义」档**（首次自动切换时给一次轻提示，例如「已切换为自定义排序」）；在
      「最近更新 / 最近使用 / 标题」档下拖拽 = 先切自定义、再按拖拽结果落库。刷新后顺序保持（`localStorage` 记忆 `pm-use-sort` 沿用）。
  - **依赖纪律**：拖拽用成熟库（建议 **`@dnd-kit/core` + `@dnd-kit/sortable`**，MIT、无 jQuery、与 antd 生态兼容）；
    **禁止自己手写拖拽引擎**；新依赖必须按既有规矩登记 `docs/dependencies.md`（版本 + 协议 + CVE/OSV 查询结果）并在 PROGRESS 记录理由。
  - **排版舒适度（硬要求，AC 机械判据）**：① 拖拽手柄**不得增加行高**（与收藏星标同排或 hover 显现；热区 ≥24×24；图标语义明确如 `HolderOutlined`）；
    ② 拖动过程**整页不得跳动**：被拖项让位用**占位符/平滑过渡**（过渡时长 ≤150ms），拖动中其它条目**不得**闪烁或重排两次；
    ③ 拖动结束后：**未被拖动的条目**的 `left/width/top/height` 与拖动前**差 ≤1px**；各条目的**行高、字号、间距与现状一致**；
    ④ 全程**不得**出现横向溢出、重叠、裁切；⑤ 光标与可拖提示到位（`cursor: grab`/`grabbing`）。
  - **键盘可达（有余力再做）**：拖拽手柄聚焦后可用方向键上下移动（做不到就在 PROGRESS 写明"未做"，不要留半个）。
- FR-71 **分栏视图：中栏条目精简为「标题 + 备注（固定两行）」+ 中栏宽度 -8%（用户 2026-09-20 提出，参考 PromptHub）**——
  用户原话：「**分栏模式下，列表页的卡片不要显示内容，而是显示标题+备注（固定两行，多余省略），不要显示其他信息，具体可以参考prompthub，然后列表的宽度减小一点，大概减小8%左右**」。
  - **条目内容（只留两项）**：**标题**（单行截断）+ **备注**（**固定两行**，超出省略）。
    **不显示**：`user_prompt` 摘要、版本号（`v1`）、取用次数、更新时间、文件夹名、变量数 等**一切元信息**（PromptHub 的列表项就是"标题 + 描述"两行）。
  - **保留两样**（不属"信息"而是操作/能力，若要再去掉请用户明确）：① **收藏星标**（用户此前明确要求"收藏要顺手"，FR-57）；② **拖拽手柄**（FR-70）。
  - **固定两行**的含义：备注区**恒定占两行高**（`-webkit-line-clamp: 2` + 最小高度 = 2 行），**备注为空时该区域留空但不塌陷**，保证所有条目**等高整齐**。
  - **中栏宽度**：基准宽度**减少约 8%**（现约 366px @1600 视口 → 约 337px；以**真实像素**断言比值落在 **0.90–0.94**）；右栏（详情）相应变宽；
    左栏筛选栏宽度**不变**；窄屏（<1200px）仍按现有响应式规则降级，**不得**因这次改动引入横向滚动。
  - **不影响**：卡片视图与表格视图**保持现状**（本项只针对分栏模式的中栏）；中栏条目仍**单击切换右栏**、滚动条行为不变。
- FR-72 **文件夹筛选 = 含子目录（inclusive），与侧栏计数一致（用户 2026-09-20 提出）**——
  用户原话：「**点击父目录显示的内容不包含子目录的卡片，这不合理，父子目录是包含关系，点击父目录肯定要显示所有属于这个目录下的卡片，跟卡片计数要保持一致**」。
  - **现状（我方源码实测定位，供参考）**：侧栏计数用递归含子目录（`web/src/components/FolderPanel.tsx` 的 `inclusiveCount` = 本级 + 全部后代），
    而列表过滤是**精确匹配**（`src/db/prompt-queries.ts`：`p.folder_id = <X>`）⇒ **计数与结果口径不一致**（计数含子目录、结果不含）。
  - **要求（语义变更）**：`GET /api/prompts?folder_id=X` 返回 **X 及其全部后代文件夹**里的 prompt（**任意深度**递归）；
    `total` 与实际 `items` 一致；与 `q` / `tag` / `favorite` 组合时同样 inclusive；三种排序档（`updated` / `recent_used` / `custom`）照常生效。
  - **一致性硬要求**：对任一文件夹，点击后列表 `total` **等于**侧栏该文件夹计数徽标的数值（同一口径 = 含子目录）。见 AC-72 ⑤。
  - **不改**：`folder_id` 缺省 / `null`（= 全部）语义不变；`folders` 表结构不变（**不加迁移**）；前端选中态 / 列表标题不受影响。
  - **拖拽语义收窄（明确，避免混乱）**：inclusive 视图（父目录 / 全部）下拖拽**仅在同 `folder_id` 内生效**；
    拖到**不同 `folder_id`** 的条目位置 → **忽略（回原位，不报错、不发请求）**。理由：`prompts.sort_order` 是**全局**列（D-28），
    允许跨目录重排会把子目录条目一并重编号、用户难以预期；与 FR-70 的「同层级重排」心智一致。
  - **须一并修订的既有断言**：AC-14 的 `?folder_id=X` 措辞（若夹具含子目录）、`tests/api-folders-tags.test.ts` / `api-prompts-crud.test.ts` / `api-search.test.ts` 中相关断言；
    修订要在 PROGRESS 逐条留痕（旧断言原文 → 新断言 + 理由）。
- FR-73 **卡片视图 FIX：无标签卡片的底部行不贴底（用户 2026-09-20 提出，布局一致性）**——
  用户原话：「**卡片模式，如果提示词没有标签，则显示效果跟其他不同，具体是复制那行信息不在最下面**」。
  - **现象**：卡片网格（`.pm-use-grid`：grid + `align-items: stretch`）里卡片**等高**，但卡片**内容未撑满**卡片高度 ⇒
    「元信息 + 复制按钮」那一行**贴不到卡片底边**；**有无标签**使内容高度不同 ⇒ 同一网格行内卡片底部参差。
  - **要求**：① 卡片最后一行（`metaLine` + 复制按钮）**恒定贴卡片底边**，距卡片底边 = 卡片内边距（**14px ±1px**）；
    ② **同一网格行内所有卡片**该行**底边对齐**（两两像素差 ≤1px）；③ **与有无标签无关**；
    ④ 标签区为空时**不留多余空隙**（若保留占位须在 PROGRESS 说明，且不得造成卡片高度差）；
    ⑤ 卡片高度 / 网格列数 / 字号 / 间距**不回归**（AC-70 ④ 像素回归继续通过）。
  - **不改**：数据与接口；纯组件结构 / CSS 层。
- FR-74 **表格视图支持拖拽排序 + 拖拽后「刷新感」优化（用户 2026-09-20 提出；第 ② 项为尽力项）**——
  用户原话：「**目前看到分栏和卡片模式都可以拖动调整卡片顺序，但是表格模式好像不支持，请也支持，并且我目前发现目前的拖动之后有一个比较明显的画面刷新的效果，这个效果有点明显，显得不是那么顺滑，如果可以的话，请优化（优先级不高，修改成本低就做，否则不要做）**」。
  - **① 表格拖拽（必做）**：表格视图**每行**提供拖拽手柄，复用同一套 `@dnd-kit`、**同一后端接口**（`PATCH /api/prompts/order`），
    **不新增依赖、不改接口契约**；行为与卡片档一致（拖后自动切「自定义」档 + 持久化 + 刷新保持 + **同目录内生效**，见 FR-72）。
    手柄**不得增高行高**、不得破坏表头 / 列宽 / 列对齐（像素回归见 AC-74 ①）。
  - **② 刷新感优化（尽力项，成本低才做）**：现状是拖拽结束后 `refresh()` / `applyFilters({ sort: 'custom' })` ⇒ **整列表重新请求 + loading 态** ⇒ 视觉闪烁。
    目标：拖拽结束后**本地立即生效新顺序、后端静默提交**（失败回滚 + 提示），**不出现 loading 骨架 / 整表重绘闪烁**。
  - **裁量边界（重要）**：若实现成本低（**仅前端乐观更新、不改接口契约、不引新依赖**）→ **做**；否则 → **跳过**，在 PROGRESS 写明理由与现状证据。
    **跳过 ② 不算失败**（不计入 AC-74 判定）。
- FR-75 **FIX 卡片拖拽在「全部 / 父目录」视图下失效（用户 2026-09-20 报「卡片拖动根本不生效」）**——
  用户原话：「**我发现有问题，上轮改完，目前我发现卡片拖动根本不生效，无法推动排序**」。
  - **根因（host_manger 在测试环境实测定位，两条叠加）**：
    ① **D-29 的跨目录限制把默认视图下的拖拽几乎全禁了**：`SortableList.canReorder` 在 inclusive 视图（全部 / 父目录）下
       要求 `active` 与 `over` **同 `folder_id`**，否则**静默 return**（顺序不变、不发请求、**无任何反馈**）。
       实测（测试环境真实数据）：7 条 prompt 分属 4 个目录（未归类 / folder2 ×2 / folder4 ×3 / folder1 ×1），
       相邻卡片几乎必然跨目录 ⇒ **7 张里只有 1 对可拖**，用户感知 = 「完全不生效」。
    ② **卡片本体不是拖拽激活点**：只有手柄 `pm-drag-card-<id>` 绑了 `listeners`（`setActivatorNodeRef`），
       拖卡片空白处 / 标题**毫无反应** ⇒ 进一步强化「根本不生效」的观感。
  - **要求**：
    1. **取消跨目录限制**：inclusive 视图（全部 / 父目录）下拖拽**总是生效**（提交「视图内完整顺序」）；
       **D-29 的「仅同目录内生效」作废**（见 D-30）。**任何被忽略的分支一律不得静默**——
       若确实还要保留某种拒绝情形，必须给出可见反馈（提示文本）。
    2. **后端 order 语义改为「槽位保持」**（顺带修掉一个既有缺陷）：`PATCH /api/prompts/order` 现在把 `ids` 一律设成
       `sort_order = 1..N` ⇒ 在**具体目录视图**内拖拽（只提交该目录的少数 id）会把它们**顶到全局最前**，
       并与其他条目**产生重复的 `sort_order`**。改为：
       **取这些 id 当前占据的 `sort_order` 集合（按 `sort_order, id` 排序），把新顺序依次分配回这些槽位** ——
       其他条目不受影响、不产生重复、这组条目不会被挪到全局最前。幂等性与负例（不存在 / 重复 / 空 → 400）不变。
    3. **卡片本体也可拖（成本低则做）**：让卡片本体成为拖拽激活点（dnd-kit 的 `activationConstraint: { distance: 5 }`
       已能保证单击 / 双击不被误判）；**若评估会与「单击选中卡片 / 双击开详情」冲突**，则保持手柄激活 +
       在 PROGRESS 写明理由，并让手柄**更易被发现**（常显 + `cursor: grab` + `title` 提示「拖动排序」）。
       **不作为阻塞项**（跳过不算失败，但必须在 PROGRESS 说明）。
    4. **手感不得回归**：沿用 AC-70 ④（未动项像素差 ≤1px、行高不变、无横向溢出）与 FR-70 的排版舒适度硬要求。
  - **不改**：`prompts.sort_order` 列与迁移（**无 schema 变更**）；三档视图的拖拽入口与手柄 testid 命名。
- FR-77 **表格模式支持批量操作（用户 2026-09-21 提出，参考 PromptHub 表格页）**——
  用户原话：「**表格模式应该支持批量操作，具体可以参考 prompthub 的页面**」。
  参考图（PromptHub）实测形态：**首列复选框 + 表头全选**；选中后表格**上方出现工具条**：
  左侧「**已选择 N 项**」、右侧「**批量收藏**」「**批量移动**」「**批量删除**」+ 最右「**取消**」。
  - **要求**：
    1. **复选框列**：表格**首列**新增复选框（含**表头全选/全不选**）；选中态清晰（行高亮可选）。
    2. **批量工具条**：有选中项时在表格上方显示；无选中时不占位。内容 = 「已选择 **N** 项」+ 三个动作 + 「取消」。
    3. **批量收藏**：把选中条目**设为收藏**（已是收藏的保持；语义 = 全部设为已收藏；若要"切换"请提 QUESTIONS）。
    4. **批量移动**：选择目标文件夹（**含「未归类」**）→ 把这批条目移到该文件夹。
    5. **批量删除**：**必须二次确认**（弹窗里写明"将删除 **N** 条 prompt，不可恢复"）；取消即不删。
    6. **落库方式（实现方定，二选一并在 PROGRESS 记录理由）**：① 新增**批量接口**（一次事务，建议 `POST /api/prompts/bulk`，body 形如
       `{"action":"favorite|move|delete","ids":[...],"folder_id":null|N}`，返回受影响条数；非法/不存在的 id → **400**）；
       ② 复用现有单条接口循环调用。**判据：一次批量操作只发 1 个请求**（若选 ② 则必须在 PROGRESS 说明并接受 N 个请求）。
    7. **与现有能力不冲突**：表格**行内操作**（编辑/删除/收藏/复制）、**行拖拽排序**、分页/排序/筛选均保持可用。
    8. **批量操作后**：列表与侧栏计数**同步刷新**；选中态清空；给出**成功/失败反馈**（如「已收藏 3 条」）。
  - **不改**：单条接口的既有契约（若走 ①，只**新增**接口，不动原接口）。
- FR-78 **分栏详情页显示「文件夹 + 标签」并可修改（用户 2026-09-21 提出，排版参考 PromptHub 详情页）**——
  用户原话：「**分栏模式下，详情页应该显示文件夹，并可以修改文件夹，还有标签，所以要调整一下内容排版**」。
  参考图（PromptHub）实测形态：标题行（标题 + 右侧图标操作）→ **紧贴标题下的备注（灰色弱化、无小标题）** →
  **元信息行：文件夹（下拉可改，带 `∨`）+ 标签（`#` 前缀胶囊，带 `✕` 可删）+ 可加标签入口** → 正文区块。
  - **现状（实测）**：`web/src/components/PromptDetail.tsx` 里**没有任何** folder/tag 的显示或编辑 ⇒ 本次**新增**。
  - **要求**：
    1. 详情页（右栏）在**标题 + 备注行之下、字段页签之上**新增**元信息行**：**文件夹** + **标签**。
    2. **文件夹可改**：下拉选择（选项 = 全部文件夹树 + 「未归类」）；选中即生效并给出反馈；改完**列表里的文件夹归属同步**。
    3. **标签可改**：以 `#` 前缀胶囊显示现有标签（每个带 `✕` 可移除）+ 一个**添加标签**入口（可选已有标签或输入新标签）；
       增删即生效并反馈。
    4. **排版**：与「标题 → 备注 → 元信息 → 页签 → 正文」的层级一致；元信息行**不抢视觉重心**（小字号、次级色）；
       窄屏（<1200px）不横向溢出。
    5. **与编辑器一致**：这里改的是**同一条 prompt 的同一批字段**（folder_id / tags），落库后与编辑器、卡片/表格视图**同源一致**。
  - **注意**：这是"详情页内联可编辑"的**新能力**（此前详情页是只读 + 编辑走编辑器）；**不要求**把其它字段（标题/正文）也变成内联可编辑。
- FR-79 **分栏详情页去掉「备注」字段页签（用户 2026-09-21 提出）**——
  用户原话：「**分栏模式下，详情已经直接显示的备注，所以跟用户提示词一起的那个备注预览可以去掉了，只需要保留用户提示词和系统提示词**」。
  - **现状（实测）**：字段页签有 **3 个**（`user_prompt` 用户提示词 / `system_prompt` 系统提示词 / `notes` 备注）。
  - **要求**：**只保留「用户提示词」「系统提示词」两个页签**；**删除「备注」页签**（备注已在标题下方直接显示，见 FR-69）。
  - **不改**：编辑器里的「备注」输入框**保留**（那里是编辑入口）；备注字段本身、接口契约、版本 diff 里的 `[notes]` 段都不动。
- FR-80 **分栏详情页去掉「变量填值」区块（用户 2026-09-21 提出）**——
  用户原话：「**去掉分栏模式下详情里面的变量渲染这一块的内容，不需要这部分**」。
  - **现状（实测）**：`PromptDetail.tsx` 里有「变量填值」区块（`LazyVariablePanel`，与「预览」「版本历史」同屏）。
  - **要求**：详情页**不再渲染变量填值区块**；「预览」「版本历史」保持。
  - **不改**：**变量填值能力本身**保留 —— 编辑器里的变量面板、以及**复制含变量的 prompt 时弹出的填值对话框**（`VarsDialog`，见 FR-81）
    都**必须保留**；后端 `/variables`、`/render` 接口不动。
- FR-81 **加大「变量填值对话框」的宽高（用户 2026-09-21 提出）**——
  用户原话：「**所有模式下，调整复制之后的弹窗大小，宽和高都加一些，目前的显示有点浪费空间**」。
  - **本次实现方要做的第一件事**：**确认"复制之后的弹窗"就是 `VarsDialog`（变量填值对话框）**——
    即复制**含变量**的 prompt 时弹出的填值弹窗（复制不含变量的 prompt 只出轻提示，无弹窗）。
    **若你判断指的是别的弹窗（如导入导出/修改密码），先写 QUESTIONS 停手，不要自行猜。**
  - **要求**：把 `VarsDialog` 的**宽度与高度各加大**（至少 **+15%**），让变量输入区更舒展、减少内部滚动与挤压；
    在**所有触发它的位置**都生效（分栏 / 表格 / 卡片 / 编辑器 —— 同一个组件，改一处即可）。
  - **不改**：弹窗内的字段、按钮、填值语义（渲染后复制）与错误提示；**不引 CDN/新依赖**。
- FR-82 **表格批量操作 UI 细化（用户 2026-09-21 提出；来源 = host_manger 阶段 27 验收时的看图观察）**——
  用户原话：「**处理那 5 条视觉改进建议**」（这 5 条由 host_manger 在验收阶段 27 时逐张看图提出，均为**视觉/健壮性**问题，不影响功能）。
  - **① 表头复选框缺 indeterminate 态**：勾选**部分**行时，表头复选框应显示**半选（indeterminate）**——现在是"实心方块"，
    用户无法一眼区分"全选"与"部分选中"。
    要求：部分选中 → `indeterminate`（antd `Checkbox` 的 `indeterminate` 属性 / DOM 上 `ant-checkbox-indeterminate` 存在）；
    全选 → 打勾；未选 → 空。**并让表头复选框与行内复选框尺寸一致**（实测现在表头那个更小）。
  - **② 批量工具条与表头行零间距**：工具条直接压在表头行上，视觉拥挤。
    要求：两者之间留出**至少 6px** 间距（真实像素断言）。
  - **不改**：批量操作的功能语义（选中/收藏/移动/删除/二次确认/取消）、工具条文案与顺序、请求数（仍须"一次操作 1 个请求"）。
- FR-83 **分栏详情页「元信息行」细化（用户 2026-09-21 提出；同上来源）**——
  - **③ 层级间距**：元信息行（文件夹 + 标签）**贴着备注行**、离字段页签行较远 ⇒ 读起来像"备注的第二行"。
    要求：把元信息行做成**视觉上独立的一行** —— 与**备注行**的间距拉开（**≥12px**），且**不大于**与字段页签行的间距（层级对称）。
  - **④ 换行保护（健壮性）**：现在元信息行**没有换行保护** —— 更长的文件夹名或多个标签会把「+ 添加标签」**顶出面板**。
    要求：**长内容（长文件夹名 + ≥5 个标签）**下，元信息行**不横向溢出**（`scrollWidth <= clientWidth + 2`）且
    「+ 添加标签」**仍在面板内可见**（允许换行/省略，但不得顶出或裁切）。
    ⚠️ **AC-79 ⑥ 只测了窄屏无溢出，未测长内容** —— 本条补上这个缺口。
  - **⑤ 标签 chip 样式统一**：详情页的标签 chip（浅蓝、无边框）与**左栏**标签列表里的同名 chip（近白、细边框）**样式不一致**。
    要求：同一实体（标签）在**详情页元信息行**与**左栏**采用**同一套 chip 视觉**（背景/边框/圆角一致，用 `getComputedStyle` 对比关键属性）。
  - **不改**：元信息行的**功能**（改文件夹、加/删标签、落库、三处同源一致）与位置（仍在标题+备注之下、字段页签之上）。
- FR-84 **`docs/` 只放「最终状态」，过程产物进 `tmp/`（用户 2026-09-21 定；依据 `/root/greenhouse/STANDARDS.md` §5.2）**——
  用户原话：「**docs 本身应该是项目的文档目录，输出的东西应该是最终状态的东西**，而且**截图也只需要保留一些关键页面的展示图**，
  这里的东西是给用户看的，包括开发者、使用者，**临时状态的东西应该放到 tmp 下面记录**，这样才不会提交到 git，本地又可以管理。」
  - **现状（host_manger 实测）**：`docs/` 下 **png 172 张 / 18M** —— 顶层 `docs/shots/*.png` 53 张（`ui-shots.sh` 的"当前状态"产出）；
    `docs/shots/stage22/23/24/27/29`（各阶段过程截图，其中 **stage22/23/24 是未被 git 跟踪的残留**）；
    `docs/dev-history/shots/stage18~25`（归档的阶段截图）。**过程截图占了 `docs/` 体积的绝大部分。**
  - **要求**：
    1. **`docs/shots/` 收敛为「关键页面展示图」一套（6–10 张）**，覆盖：登录 / 分栏 / 表格 / 卡片 / 编辑器 / 详情面 / 移动端 / 暗色；
       其余**全部移 `tmp/shots-archive/`**（不入库、本地可查）。
    2. **各阶段截图目录**（`docs/shots/stage*` + `docs/dev-history/shots/`）**整体移 `tmp/shots-archive/`**；
       **未被 git 跟踪的残留**（stage22/23/24）一并处理。
    3. **改 `tools/ui-shots.sh` 的约定**：**默认输出指向 `tmp/`**（阶段自证用）；只有"发版 / 交付前"才产一套关键展示图到 `docs/shots/`，
       且**只留一套**（旧的先归档到 `tmp/`）；脚本注释里写明这条约定。
    4. **把规范写进 `AGENTS.md`（英文）**：`docs/` 的定位（最终状态 · 给人看）、截图分级（关键展示图 vs 阶段过程截图）、
       工具约定（自证截图默认落 `tmp/`）、`tmp/` 不入库。
    5. **`docs/dev-history/` 的文档类保留**（用户 2026-09-21 选 A —— 验收凭据）—— **只移其中的截图**。
  - **不改**：文档内容本身（`brief-changelog` / `dependencies` / `search-zh` / `versioning` / `dev-history` 下的 md）；代码与接口契约。
- FR-85 **表格模式：「标签」列的标签之间要有间距（用户 2026-09-21 提出）**——
  用户原话：「**表格模式下，标签里面的标签直接目前都是拼在一起的，没有间隔，请优化，增加标签之间的间距**」。
  - **现状（实测根因）**：`web/src/components/UseView.tsx` 的表格列渲染是
    `render: (_v, prompt) => prompt.tags.map((tag) => <Tag key={tag}>{tag}</Tag>)` ——
    **多个 `<Tag>` 直接相邻、没有任何 gap** ⇒ 视觉上"拼在一起"。
  - **要求**：**标签之间留出可见间距**（建议与卡片视图一致：**相邻标签间隙 ≥4px**，用 `Space` / `gap` / `marginInlineEnd` 任一方式）；
    多标签（≥3 个）时**不换行溢出**、不撑破该列；**单个标签 / 无标签**时不受影响。
  - **不改**：标签的数据与接口；表格列宽（`width: 128`）与其它列；标签内容与顺序。
- FR-86 **版本保留策略：每个 prompt 最多保留「最近 10 个版本」（用户 2026-09-21 提出；**这是数据层改动，不只是显示**）**——
  用户原话：「**数据层：提示词的版本最多保留最近 10 个版本即可，这是数据层的改动，不仅仅是显示（需要有文案说明）**」。
  - **保留规则**：对每个 prompt，`prompt_versions` 里**最多保留 `version_no` 最大的 10 行**；超出部分**从表中删除**（真删，非隐藏）。
  - **触发时机**：**每次产生新版本之后**都检查并裁剪 —— 覆盖全部写入点：**新建**、**更新（PUT）**、**回滚**、**导入**。
    建议**抽成一个公共函数**（如 `pruneVersions(qe, promptId, keep = 10)`）在四处统一调用，避免遗漏。
  - **必须保证**：**当前版本**（`prompts.version_no` 指向的那一版）**永远在保留集合内**；裁剪后 `version_no` **不重编号**（只删行，不改号）。
  - **导入（`POST /api/import`）**：导入的版本若多于 10 个，**同样裁剪到最近 10 个**（"最多 10 个"是**数据层不变式**，导入后也必须成立）。
    ⚠️ 这与"`replace` 模式保留文件里的 id / 导出→导入→再导出应一致"存在**已知张力**：**以本 FR 为准**（版本上限优先），
    并在 `docs/` 或 `README.md` 的"已知限制"里**写明**这一点。
  - **回滚语义**：回滚**已存在**的版本 → 生成新版本 + 裁剪（仍是 ≤10）✅；回滚到**已被裁剪掉**的版本 → **404**（既有语义不变）。
  - **存量数据**：**不做一次性迁移清理**（不加迁移）；**在下次产生新版本时自然裁剪**。
  - **文案说明（用户明确要求）**：**版本面板**里要有可见说明，如「**最多保留最近 10 个版本**」
    （建议放在版本列表的标题行/提示处，`VersionPanel.tsx`）；`README.md` 的"已知限制"或对应小节也**写明这条策略**。
  - **不改**：`GET /api/prompts/:id/versions`、`/diff`、`/versions/:n/rollback` 的**契约与语义**（只是条数 ≤10）；
- FR-87 **FIX：CI 的 `typecheck:tests` 在干净环境必失败（用户 2026-09-21 报 GitHub CI 红）**——
  用户原话：「**CI问题，github上面ci报错**」（贴出的 CI 输出：`②b typecheck:tests rc=1 / 38 个 TS 错误`，而 `③ npm test rc=0 / 319 全过`）。
  - **根因（host_manger 已复现确认）**：`tools/ci-check.sh` 的**步骤顺序**有问题 —— 它的 **②b `typecheck:tests` 跑在 ④ `npm run build` 之前**，
    而测试文件 `import` 的是**构建产物** `../dist/**` ⇒ **干净环境（CI）里 `dist/` 尚不存在** ⇒ 38 个 `TS2307: Cannot find module '../dist/…'`。
    本地之所以"看起来正常"，是因为本地已有历史 `dist/`。**复现证据**：`rm -rf dist && npm run typecheck:tests` → **38 个错误**；
    `npm run build && npm run typecheck:tests` → **0 个错误**。（`npm test` 自身会先 `build`，所以 ③ 一直是绿的。）
  - **要求**：**调整 `tools/ci-check.sh` 的步骤顺序**，保证 **`typecheck:tests` 在构建产物存在之后运行**（推荐顺序：
    ① 依赖就绪 → ② **构建** → ③ `typecheck:web` + `typecheck:tests` → ④ `npm test` → ⑤ 体积预算）；
    若保留现有顺序，则**在 ②b 之前显式构建服务端**（`npm run build:server`）。**两种做法都要在脚本注释里写明"为什么必须先构建"**。
  - **判据（必须用"干净环境"验）**：**先删掉 `dist/`** 再跑 `bash tools/ci-check.sh` → **全绿**；并让 **GitHub Actions 实跑一次通过**。
  - **不改**：CI workflow 的触发条件 / Node 版本 / 检查项清单（仍与本地同一套脚本）。
- FR-88 **登录页简化：去掉默认用户名 + 去掉噪音信息（用户 2026-09-21 提出；其中"去默认 admin"是安全项）**——
  用户原话：「**登录页面目前很多噪音信息，根本没必要，简化内容，只显示登录信息，最重要的一点，目前默认输入了admin用户，必须去掉，登录页面应该是空白输入，不能暴露账号名**」。
  - **① 去掉默认用户名（P0，安全）**：现在 `web/src/components/LoginPage.tsx` 的表单有 `initialValues={{ username: 'admin' }}`
    ⇒ **页面加载即预填 `admin`，暴露了账号名**。**必须删除该预填**，用户名输入框**初始为空**（`value === ''`）；
    输入框的 `placeholder="admin"` 也要改成**中性占位**（如「用户名」/「请输入用户名」），**不得**再出现 `admin` 字样。
  - **② 简化内容：只显示"登录信息"**。**保留**：品牌图形（96px mark）、`PromptManager` 标题、**用户名 / 口令 / 登录按钮**（表单本体）。
    **删除**（噪音）：
    · `SELF-HOSTED · 单进程单端口`（装饰性技术标签）
    · 副标题「轻量自托管的 Prompt 管理器，数据只在本机。」
    · 「口令由本机 CLI 设置，网页不提供注册。」
    · 「除 `/healthz` 与登录接口外，全部接口未认证一律 401。」
    （这些技术说明属于「信息克制」原则下**该收进「关于」页**的内容；登录页只留"登录"这一件事。）
  - **③ 不得回归**：登录功能正常（错误提示、限流提示、`autoComplete`、亮暗主题、移动端可用、品牌图 `data-testid="pm-brand-art-login"` 保留）。
  - **不改**：登录接口契约与后端逻辑（纯前端展示层）。
    无 schema 变更（不加迁移）。


- FR-76 **顶栏品牌文字缩短为「PromptM」（用户 2026-09-20 提出；范围：**只改顶栏这一处**）**——
  用户原话：「**我觉得网真的logo名称目前PromptManager太长了，如果要缩短，你建议是什么**」→ 采纳 **`PromptM`**（Prompt + M(anager)，
  比全名短 6 字符、保留"管理器"语义且与原名强关联；已查证无同名产品撞名）。
  随后用户明确范围：「**我们只改这个logo的地方，其他地方都不要改**」。
  - **只改一处**：`web/src/components/AppHeader.tsx` 顶栏品牌区的文字 `PromptManager` → **`PromptM`**。
  - **明确不改（用户指定，防止"顺手全局替换"）**：`web/index.html` 的 `<title>`、`web/src/components/LoginPage.tsx`、
    `web/src/components/AboutModal.tsx`、`web/src/pure.ts` 的错误提示语 —— **四处一律保持 `PromptManager`**；
    且 `SUPPORTED_EXPORT_FILE.app = 'promptmanager'`（导出文件契约值）**一字不动**。
  - **不动**：品牌图标（「>_」mark，26×26、`data-testid="pm-brand-mark"`）；移动端行为（文字仍只在 `!isMobile` 时渲染，
    移动端仍只显示图标）；顶栏其他元素（新建 / 更多 / 主题 / 登出）的顺序与位置。
  - 理由（存档）：**顶栏是唯一空间受限处**，用简称；其他页面（登录 / 关于 / 浏览器标签页）空间充裕，保留全名以免品牌碎裂。





  - **验收纪律（写进 AC）**：**必须用「内网 IP」访问来验收复制**（如 `http://192.168.0.228:8767`），**不得只用 `127.0.0.1`** —— 后者是安全上下文，会**掩盖**此 bug。





- FR-51 **Logo 更名「PromptManager」+ 点击回主页（用户 2026-09-19 反馈 ①）**——
  用户原话：「**菜单的logo改成PromptManager，并且我希望任意页面点击logo这里可以回到主页**」。
  - 顶栏品牌文字改为 **`PromptManager`**（`P`/`M` 大写），不得再是全小写 `promptmanager`。
  - **logo 整块可点**（`cursor:pointer` + hover 反馈 + `role=button`（或 `<a>`）+ `aria-label="回到首页"`）；
    点击 = **回主页**：视图切回默认（**分栏**）、清空搜索框、清空筛选（文件夹 / 标签 / 只看收藏）、取消条目选中、列表滚动回顶。
- FR-52 **「关于」页内容与排版重构（用户 2026-09-19 反馈 ②）**——
  用户原话：「**"关于"页面的内容值得重构，目前的内容和排版有点不友好，还有内容也需要重新整理一下**」。
  现状（我方实测）：一张 `Card`（标题「服务自检」+「重新探测」按钮）+ 一个大块 `Result`（图标/标题/副标题）+ `Descriptions` 12 行；
  **问题**：① 有**重复条目**（两行「监听」内容重复）；② 混入**开发者内部信息**（「需求合同见 BRIEF.md；验收标准见其第 8 节（阶段 12：AC-37…）」，且已过期）；
  ③ 「服务自检」与「界面/依赖与许可」两类受众混排；④ 大 `Result` 块占位大、信息密度低。
  - 重构要求（**面向"日常使用者"**，不是给开发者看的）：
    1. **顶区**：一行状态条取代大 `Result` 块 —— 品牌 `PromptManager` + 版本号 + 「后端在线」状态徽标 + `重新探测` 按钮（保持功能）。
    2. **分区（≥3 个，用 `Descriptions`/`Divider`/`Collapse` 明确分区）**：
       ① **服务**：版本 / 状态 / 访问地址（含端口）/ 数据文件位置 / 备份方式（含"去哪里导出"）；
       ② **使用**：常用操作指引（新建·编辑·复制·变量填值、导入导出入口在 `⋯更多`、CLI 与 MCP 怎么用、快捷键 `/`·Esc）；
       ③ **维护**（**默认折叠**）：改登录口令命令、日志查看命令、重启/回滚命令、依赖与许可清单指向 `docs/dependencies.md`。
    3. **必须删除**：重复条目、对 `BRIEF.md` / 验收标准 / 阶段编号的内部引用、任何已过期的阶段信息。
    4. **排版**：窄屏（<768px）单列不横向滚动；长路径/命令可一键复制；标签与数值对齐，不得出现空 `th`/空行。
  - 断言（AC-52）：`pm-about` 内**无重复 label**、**不含** `BRIEF` / `AC-` / `阶段` 字样；分区数 ≥3；窄屏无横向滚动。
- FR-53 **删除「拿来就用」装饰标题（用户 2026-09-19 反馈 ③）**——
  用户原话：「**首页有个"拿来就用"的块，这个完全就是浪费空间，请评估，这个出现的必要性，没必要就删除，不要浪费屏幕空间**」。
  - **评估结论（host_manger，2026-09-19）：删除**。实测该块 = 一个只含标题自身的 `flex` 容器（`H3`，高 **25px** + 下边距 **12px** = **约 37px 纯装饰**），不含搜索框或任何控件；
    其语义（"这是拿来就用的列表"）已由视图本身与搜索框 placeholder 表达 ⇒ **无信息增量，纯占屏**。
  - 要求：移除该标题及其空容器；若需保留语义，只能用 `aria-label` 或搜索框 `placeholder` 承载，**不得**再占一整行。
  - 断言（AC-53）：DOM 中不再有文本为 `拿来就用` 的元素；顶栏/工具栏到列表首行的垂直空白比修改前**减少 ≥30px**。
- FR-54 **FIX（P0 bug）：非空文件夹删除静默失败（用户 2026-09-19 反馈 ④「弹窗点不动」）**——
  用户原话：「**bug：文件夹里面点击的删除按钮，显示的弹窗根本无法点击，只是一个点击后的显示，根本无法进去弹窗操作**」。
  - **我方复现证据（临时实例，真鼠标事件，非 JS `.click()`）**：
    ```
    ① 空文件夹（运维）：hover 行 → 点删除图标 → 弹窗「删除文件夹「运维」？删除后不可恢复。」→ 真点「删除」→ folders 3 → 2 ✅ 正常
    ② 非空文件夹（Agent管理，含 1 个子文件夹）：
       hover 行 → 点删除图标 → 弹窗文案「…含 1 个子文件夹，需先清空；服务端会拒绝删除（409）。」→ 真点「删除」
       → **弹窗关闭、文件夹仍在、页面上没有任何提示**（唯一可见提示是无关的变量说明）
       → 同时 `DELETE /api/folders/1` 实际返回 `409 {"error":"folder_not_empty"}`（服务端语义正确，**前端把它静默吞了**）
    ```
  - 要求：
    1. **不得静默失败**：任何非 2xx 都要有**用户可见反馈**（`message.error` / 弹窗内 `Alert`），文案说清**原因 + 下一步**（先清空子文件夹 / 先移走其中的 prompt）；
    2. **前置阻断（更好）**：非空时把弹窗内「删除」按钮置 `disabled`，或改为引导按钮（如「先清空」）—— 二选一，但**必须有可见结果**；
    3. **弹窗必须真可点**：用**真鼠标事件**端到端自证（移动到行 → hover → 点删除图标 → 鼠标移到弹窗上 → 点按钮），并确认
       ① 弹窗不被侧栏 `overflow` 裁切、② 不被其他层遮挡（z-index/层叠上下文）、③ 鼠标离开行时弹窗**不误关**、④ 移动路径经过其他元素时不丢 hover；
    4. 顺带自查：**所有 Popconfirm/Popover 类浮层**（文件夹删除/重命名、标签、条目删除）在窄屏与侧栏滚动下都要可点。
  - 断言（AC-54）：非空删除 → 出现**可见错误反馈**且 folders 数不变；空文件夹删除 → 真鼠标点击后 folders **-1**（成功路径不得回归）。
- FR-55 **编辑页 → 详情 的返回入口（用户 2026-09-19 反馈 ⑤）**——
  用户原话：「**编辑页面似乎没有能跳回详情页面的按钮或者导航，不符合操作习惯**」。
  - 现状：编辑器左上有「返回列表」，但**没有回详情**的路径（详情 → 点「去编辑」进入编辑后就回不去详情，只能回列表再点回来）。
  - 要求：编辑态顶部提供**按来源返回**的入口：从详情进入 → 显示 `← 返回详情`；从列表/新建进入 → 显示 `← 返回列表`；
    建议同时给出可点的面包屑（`列表 / <条目标题>`，点条目标题 = 回详情）；`Esc` 行为与之一致（不丢未保存修改：有改动时先确认）。
  - 断言（AC-55）：从详情点「去编辑」进入编辑态 → 顶部存在文本含 `返回` 的按钮 → 点击后 `pm-detail` 重新出现且标题为该条目。
- FR-56 **编辑页右栏模块顺序改为「预览 → 变量 → 版本」（用户 2026-09-19 反馈 ⑥）**——
  用户原话：「**编辑页面的右边，应该把markdown预览放到最上面，然后是变量填值，最后才是版本历史，类似的模块排序应该按照使用优先排列**」。
  - 现状（实测 top 值）：版本历史 `y=68` → 变量填值 `y=384` → Markdown 预览 `y=704`（**完全反了**）。
  - 要求：右栏自上而下 = **Markdown 预览 → 变量填值 → 版本历史**；并以此为准绳检查其它页面的模块顺序（使用频率高的在上）。
  - 断言（AC-56）：三者容器 `getBoundingClientRect().top` **严格升序**：预览 < 变量 < 版本。
- FR-57 **收藏入口显性化（用户 2026-09-19 反馈 ⑦）**——
  用户原话：「**prompt能收藏的地方不太方便，目前好像只有编辑页面最下面可以进行收藏，这明显不顺手，收藏应该是一个很容易做到的操作，请评估后安排优化**」。
  - 评估（我方实测）：收藏星标当前分布在 分栏列表项（`span.anticon-star`，**不是 button**）、卡片视图、详情栏；
    **表格视图行内没有收藏**（只有 复制 / 编辑 / 删除）。
  - 要求：**四屏都能一键收藏**且**常驻可见**（不依赖 hover）：
    1. 分栏列表项 · 卡片 · **表格行（补上）** · 详情栏标题行 · 编辑页；
    2. 星标必须是**真正的可点控件**（`<button>` 或 `role=button` + `cursor:pointer` + `aria-label="收藏 / 取消收藏"`），
       **不得**是不可点的装饰 `span`；热区 ≥24×24；
    3. 点击**即时反馈**（图标填充 + 轻提示），并与 `GET /api/prompts` 的 `favorite` 一致（刷新后不丢）；
    4. 悬浮时给出「收藏 / 取消收藏」tooltip。
  - 断言（AC-57）：四处均存在可点星标（元素为 button/role=button 且 `cursor=pointer`）；真鼠标点击后 `favorite` 翻转；再点恢复。
- FR-58 **主题「跟随系统」图标替换电脑图标（用户 2026-09-19 反馈 ⑧，优先级低）**——
  用户原话：「**目前的主题切换的按钮，跟随系统的图标是一个电脑，这个图标有误导性，无法体现是主题切换，看看是否有太阳月亮一起的那种图标，当然，这个优先级不高**」。
  - 现状（实测）：跟随系统 = `anticon-desktop`（电脑）；亮 = `anticon-sun`；暗 = `anticon-moon`。
  - 要求：三态改用**同一视觉体系**的图标 —— 亮 = 太阳、暗 = 月亮、**跟随系统 = 太阳 + 月亮并排组合**（`@ant-design/icons` 的 `SunOutlined` + `MoonOutlined`，两枚小尺寸图标紧邻、整体一个按钮）；
    若实现方找到更贴切的**同一个**图标（如 `ContrastOutlined` 之类），可在 PROGRESS 里说明理由后采用，并附截图。tooltip 文案不变。
  - 断言（AC-58）：跟随系统态按钮内**不再**出现 `anticon-desktop`，且**同时**包含 `sun` 与 `moon` 两个 icon（或经说明后采用的自选图标）；亮/暗态仍分别为 sun / moon。



- FR-46 **分栏视图（PromptHub 式三栏）+ 删除列表视图（用户 2026-09-19 反馈）**——
  用户原话：「**我希望增加一个 prompthub 这种分栏视图，然后去掉目前的列表视图**」，并补充三点：
  ① 分栏为**默认**落地视图；② 右栏**复用现有详情页内容**（无需新增模块）；③ 视图开关按 **分栏 → 表格 → 卡片** 顺序显示三档。
  - **布局（三栏，左 → 右）**：
    1. **左栏 = 现有只读筛选栏**（`pm-sidebar`：文件夹树 + 标签计数 + 收藏筛选），**保持只读**，行为不变；
    2. **中栏 = 列表**（`pm-split-list`）：每条 `pm-split-item` = **标题 + 摘要**（正文首行、去标记、单行截断）+ **元信息 2–3 项**
       （文件夹 / 标签 / 版本 / 取用次数 / 更新于）；**单击即切换右栏**（不需要双击）；当前项有**明确选中态**（背景或左侧色条）；
    3. **右栏 = 现有详情面**（`pm-detail` 及其全部子组件与 testid，**原样复用**）：`去编辑`、字段页（用户提示词 / 系统提示词 / 备注）、
       `预览 / 源码` 与 `显示纯文本`、**变量填值 + 渲染 + 清空填值**、`版本历史`（含 diff 对比）、`全屏展开`、
       底部固定操作条 `pm-detail-actions`（`复制提示词` / `版本历史` / `删除`）。**未选中任何条目时显示空态**（一句提示，不留白板）。
  - **默认与记忆**：默认视图 = **分栏**（取代原「默认卡片」）；沿用 `localStorage` 键 `pm-view-mode`，取值 `split` / `table` / `card`；
    读到旧值 `list` 时**回退到 `split`**（不报错、不留空白）。
  - **视图开关**：`pm-use-viewmode` 三段，**按「分栏 / 表格 / 卡片」顺序**（用户指定）；**删除「列表」档** ——
    `pm-view-list` 从实现中移除（`grep -rc 'pm-view-list' web/src` → `0`）。
  - **宽度与响应式**：≥1200px 三栏同屏（左 240–280 / 中 320–400 / 右自适应且 ≥420）；768–1200px 保留「中栏 + 右栏」（左栏收成抽屉/图标）；
    <768px **单栏降级**：只见列表，点条目进入详情、可返回列表 —— **不得**出现三栏挤压或横向滚动。
  - **不变的部分**：卡片 / 表格视图行为不变（表格行内仍有 `编辑 / 删除`；卡片不常驻删除）；顶栏、`＋新建`、`⋯更多`、主题开关、
    搜索与快捷键（`/` / Esc / 双击）均不变；移动端复制按钮 ≥44×44（AC-35）在分栏下同样成立。
  - **本期不做**（用户未要求 / 我方裁定）：PromptHub 的 `AI 测试 / 多模型对比`、`关系图谱`、`自定义输出格式`，
    以及最左侧的**多产品图标栏**（Prompts / Skills / Rules）—— 我们是单一应用，不做空壳导航。


**P1（有余力再做，本次先不做）**

- FR-20 卡片/表格多视图切换、拖拽排序、批量操作（多选删除/移动/打标签）
- FR-21 媒体（图片/视频）上传与预览
- FR-22 PromptHub JSON 导出格式的兼容导入
- FR-23 意图内的搜索增强（高亮片段 snippet、按标签/文件夹的检索范围限定）
- FR-24 快捷键与键盘可达性（列表 → 编辑器 → 保存 → 搜索 全键盘）
- （原 FR-25 使用记录已按用户 2026-09-18 拍板**提升为 P0**，见 FR-19）

**P2（明确不做）**

- FR-30 关系树 / 语义关系面板 / 输出格式序列 / 看板 / 图谱
- FR-31 私有加密文件夹、AI 改写与多模型测试、图片反推
- FR-32 外部同步（WebDAV/S3）、多端、i18n 多语言、桌面/移动打包

- FR-89 **GitHub Actions：构建容器镜像并推送到 Docker Hub（用户 2026-09-21 提出）**——
  用户原话：「**github action 添加构建镜像推动到docker hub的配置**」。
  - **要交付**：新增 **`.github/workflows/docker.yml`**（**不改动** `ci.yml` 的任何检查项）：
    - **触发**：① `push: tags: ['v*']`（**主用途**：发版 tag 即构建并推送）；② `workflow_dispatch`（手动补跑）；
      ③ `push: branches: [main]` —— **只构建、不推送**（尽早发现 Dockerfile 被改坏；`push: false`）。
    - **权限最小化**：`permissions: contents: read`（不需要 `packages: write`，Docker Hub 与 GITHUB_TOKEN 无关）。
    - **步骤**：`actions/checkout` → `docker/setup-buildx-action` → `docker/login-action`（凭据只从 **GitHub Secrets** 取）
      → `docker/metadata-action`（算 tag）→ `docker/build-push-action`。**官方 action 一律 pin 到大版本**（如 `@v4`/`@v3`/`@v6`）。
    - **镜像名**：`${{ secrets.DOCKERHUB_USERNAME }}/promptmanager` —— **不得硬编码任何用户名/命名空间**。
    - **镜像 tag 规则（由 metadata-action 自动算）**：推 `v1.0.1` 时产出 **`1.0.1` + `1.0` + `latest`**；
      `main` 分支构建**不推送**；手动触发按当前 ref 算 tag。
    - **平台**：**`linux/amd64`**（与生产 106 一致）。**`linux/arm64` 本期不做**（要加 QEMU/buildx 多平台，代价翻倍；列为后续可选）。
    - **构建上下文 = 仓库根**、`Dockerfile` = 仓库根 `Dockerfile`（与 106 现行构建命令等价：`docker build -t promptmanager:<ver> .`）。
  - **安全红线（硬）**：① **任何 step 都不得 echo/打印/回显 secret**（含 `set -x`、`run: echo ${{ secrets.* }}`、日志打印）；
    ② 凭据**只**出现在 `docker/login-action` 的 `with` 里；③ **不得**把用户名/token 写进仓库任何文件、文档、镜像或 commit；
    ④ 文档里只写 **secret 名**（`DOCKERHUB_USERNAME` / `DOCKERHUB_TOKEN`），**不写值**；⑤ 不给 workflow 加 `pull_request_target` 等危险触发器。
  - **文档（交付物）**：`README.md` 增「**从镜像运行**」一节（`docker pull <ns>/promptmanager:<ver>` + `docker run` 的完整命令，
    含 `-v <宿主数据目录>:/data`、`-e TRUST_PROXY/PUBLIC_ORIGIN` 的可选说明）；`deploy/container.md` 增「**镜像发布（Docker Hub）**」节：
    触发方式、tag 规则、**需要在 GitHub 仓库配哪两个 secret（只写名字）**、失败怎么看日志。
  - **不改**：`Dockerfile`（除非只加 `LABEL org.opencontainers.image.*`，可选）；`ci.yml`；任何运行时行为/接口/数据模型。
  - **本机事实（写规格时必须知道）**：**228 上没有 Docker**（dsh 无法本地 `docker build`）⇒ 等价构建由 host_manger 在 **106/203** 上做（见 AC-91 ③）。

- FR-90 **分栏视图：手机端「提示词列表」宽度没有撑满（用户 2026-09-21 提出）**——
  用户原话：「**分栏模式，在手机端，提示词列表显示的宽度没有全屏，需要调整成全屏**」。
  - **实测根因（host_manger 已用真实 390×844 量过，贴数）**：`web/src/components/SplitView.tsx` 里**中栏 Card 的宽度样式与是否移动端无关** ——
    `style={{ flex: '0 0 clamp(276px, 31.3%, 350px)', minWidth: 0 }}` ⇒ 390 宽时 `31.3%` ≈ 112px，被 **clamp 下限抬到 276px**，
    而该容器**可用宽度是 358px**（= 390 − 2×16 页面内边距）⇒ **右侧空 82px**。
    **同宽度对照（同一实例同一时刻实测）**：**卡片视图 358、表格视图 358**（两者都撑满）⇒ **只有分栏中栏没撑满**。
  - **要求**：**移动端（<768px）分栏中栏必须撑满可用宽度**（= 容器宽度 **358** = 视口 390 − 2×16 内边距），
    右边缘与卡片/表格视图**对齐**；移动端仍是**单栏降级**（不渲染右栏详情）。
  - **不得回归（桌面）**：≥768px 仍是三档 clamp（min 276 / 31.3% / max 350）—— 阶段 22 的 **FR-71「中栏 −8%、比值 0.90–0.94」** 口径不变。
  - **不得回归（移动）**：点条目光标打开详情抽屉、拖拽手柄与收藏星标保留、3 条条目正常渲染、**无横向溢出**。
  - **不改**：其它视图、接口、数据模型；不引入新的断点体系（沿用现有 `isMobile`）。

- FR-91 **README 重写为「给用户看的」文档 + 两种部署方式（用户 2026-09-21 提出）**——
  用户原话：「**修改 README.md 文档，目前的 README.md 根本不是给用户看到，请改成给用户看的，并且部署方式给源码和 docker 运行两种方式，
  README.md 很多信息都是开发者信息，如果有必要可以移到 AGENT.md 中**」。
  - **定位改变**：`README.md` = **用户文档**（受众：**使用者 / 自部署者**，**不是本项目开发者**）。要覆盖：
    ① 一句话定位 + **能做什么**（功能清单，用用户语言，不出现实现细节）；
    ② **两种部署方式，两种都必须"照着能跑通"**：
       **A. Docker（推荐）**：`docker pull` + `docker run`（数据卷 / 端口 / 环境变量）+ **compose 示例** + 首次设口令 + 自检；
       **B. 源码运行**：Node 24 + `npm ci` → `npm run build` → `npm run migrate` → 设口令 → 启动；常驻（systemd）只给要点并指向 `deploy/README.md`；
    ③ **怎么用**：登录、三档视图（分栏/表格/卡片）、常用操作（新建/编辑/复制/变量填值/版本历史/导入导出）、快捷键、备份与升级/回滚；
    ④ **常见问题（FAQ）**：忘记口令怎么办 / 数据存在哪 / 端口被占 / **反代时 `TRUST_PROXY` + `PUBLIC_ORIGIN`** / 内网 HTTP 下复制提示 / 升级后数据是否兼容；
    ⑤ **已知限制**（用户视角）；⑥ **文档索引**（指向 `docs/`、`deploy/`、`AGENTS.md`）。
  - **开发者信息必须移出 README**（用户明确要求）：`## 代码质量检查`、`## 怎么验证`、项目结构与构建细节、**AC / 阶段号等内部术语**。
    - **迁移落点（D-33 已定）**：**新建 `docs/development.md`（中文）** = 开发者文档（代码质量检查、验证脚本清单、构建与测试命令、项目结构、验收体系、CI 说明）；
      **新建 `docs/api.md`（中文）** = 原 README 的**接口 / CLI / MCP 参考**（curl 示例那一整段）。
    - ⚠️ **为什么不塞进 `AGENTS.md`**：`STANDARDS.md §5.1` 规定 **`AGENTS.md` 是「纯 AI 指令文件」，必须全英文**、受众是 AI 不是人；
      把中文开发者文档放进去会**违反该政策**，而且人会去翻一个"给 AI 看的英文文件"。⇒ **`AGENTS.md` 保持英文，只加一行英文指引**
      （"Developer docs live in `docs/development.md`; API reference in `docs/api.md`"）。
  - **顺带修掉 backlog R-5**：README 里**重复的两节**（`## 代码质量检查` / `## 怎么验证` **各 2 份**，其中一份还写着**根本不存在的** `tools/ac-stage9.sh`）随本次重写**一并消除**。
  - **硬约束**：README **不得**出现内部术语（`AC-xx` / `FR-xx` / `阶段 N` / `BRIEF` / `D-xx`）；**所有相对链接必须指向真实存在的文件**；
    README 保持**中文**、`AGENTS.md` 保持**英文**（政策）；`docs/` 只放**最终状态**文档（阶段 30 规范，过程产物进 `tmp/`）。
  - **不改**：代码（除 FR-90 的那一处样式）、接口、数据模型、`ci.yml` / `docker.yml`。

- FR-92 **移动端：视图档位顺序改为「卡片 / 表格 / 分栏」，且默认落在卡片（用户 2026-09-21 提出）**——
  用户原话：「**我希望移动端的模式顺序变成卡片，表格，分栏，因为移动端卡片效果更好**」。
  - **① 档位开关顺序（移动端）**：`web/src/components/UseView.tsx` 的 `Segmented`（`data-testid="pm-use-viewmode"`）
    在 **<768px** 时选项**依次为** `卡片` / `表格` / `分栏`（= 现值 `分栏 / 表格 / 卡片` 的**倒序**）；
    **桌面（≥768px）保持 `分栏 / 表格 / 卡片` 不变**。
  - **② 默认档位（移动端）**：`web/src/components/Workspace.tsx` 的 `readPref('pm-view-mode', 'split', …)` ⇒
    **移动端首次进入（`localStorage` 无该键）默认 `card`**；**桌面默认仍是 `split`**；
    **用户已有本地偏好时一律不覆盖**（在桌面选过 `table` 的用户，到移动端也仍是 `table`）。
  - **③ 不得回归**：三档在移动端都能正常切换与渲染；`pm-view-split` / `pm-view-table` / `pm-view-card` 三个 testid 不变；
    旧值 `list` 仍回退到 `split`（AC-45 ⑤）；**桌面档位顺序、默认值与三栏布局一字不变**；
    **不新增第四个档位**；不改 `localStorage` 键名与取值集合（仍是 `split` / `table` / `card`）。
  - **不改**：三个视图自身的实现（除 FR-90 的那一处宽度）、接口、数据模型。

## 5. 技术约束（硬性）

- **依赖策略**：优先成熟生态包；**禁止手搓** HTTP 路由/框架、SQLite 驱动、markdown 渲染、代码高亮、diff 算法、
  密码哈希、session 管理、前端构建、ORM/SQL 拼装等基础设施（用户原则：站在巨人肩上，别重复造轮子）。
- **运行环境**：228（CentOS Stream 9）/ **Node 24.18.0**（系统自带，不要换版本、不要装 nvm）/**无 Docker**（不要试图用容器）。
- **语言**：TypeScript（strict）+ Node ESM。
- **后端**：成熟 HTTP 框架（推荐 `fastify`，实测最新 5.12.5；也可换等价成熟框架，须在 `docs/dependencies.md` 说明理由）。
- **数据库**：**SQLite 单文件**，驱动二选一（在 `docs/dependencies.md` 写选型理由）：
  - `better-sqlite3`（已验收实测：13.0.3 预编译安装 5 秒、SQLite 3.53.4、FTS5 + trigram 通过）；
  - Node 内置 `node:sqlite`（已实测：SQLite 3.53.1、FTS5 + trigram 可用、零依赖；但 API 标注实验性）。
  必须开启 `journal_mode=WAL` 与 `foreign_keys=ON`；迁移用 `migrations/*.sql` + 幂等执行（`npm run migrate` / `bin/pm.mjs migrate`）。
  本项目**不使用任何外部数据库/缓存/队列服务**。
- **前端**：React 19 + TypeScript + Vite（实测最新 vite 8.3.0 / react 19.3.0）。
- **前端组件库（硬性，用户 2026-09-18 要求：「前端页面必须使用框架的组件库，不要自己造轮子」）**：
  - **UI 一律使用 Ant Design（`antd`，实测最新 **6.6.4**，MIT，peer `react >=18` → React 19 原生支持，**不需要**
    `@ant-design/v5-patch-for-react-19` 那个 v5 补丁包**），图标用 `@ant-design/icons`（实测 6.3.4，MIT）。
  - **必须用组件库的组件**（不许自建、不许拷源码改）：Button / Form / Input / Input.TextArea / Select / Switch /
    Tree / Table / Pagination / Modal / Drawer / Tabs / Tag / Badge / Tooltip / Popconfirm / message / notification /
    Upload / Layout / Space / Flex / Grid / Typography / Divider / List / Card / Empty / Spin / Result / Dropdown / Menu。
  - 亮暗主题用 antd 的 `theme.algorithm`（`defaultAlgorithm` / `darkAlgorithm`，跟随系统 `prefers-color-scheme`）；
    中文界面用 antd 内置 `zh_CN` locale（`ConfigProvider`）。
  - **不要引入第二套样式体系**（默认**不引入 Tailwind**；布局用 antd 的 `Layout`/`Flex`/`Space`/`Grid` 与主题 token）。
    确有必要引入时，先写 `QUESTIONS.md`。
  - **不许引 CDN**：组件库及其样式必须随构建本地打包。
  - 组件库与版本在**阶段 1 就固定**（安装 + `npm run build` 能过 + 登记 `docs/dependencies.md`），不要拖到前端阶段才选型。
- **Markdown/高亮/净化**：成熟库（`marked` 或 `markdown-it`）+ 服务端 `DOMPurify` + `highlight.js`（实测版本见 §6.8）。
- **Markdown 净化硬化（v4 新增；阶段 6 前端渲染前必须完成）**：`DOMPurify` 的**默认配置**允许 `style` 属性与
  `<style>` / `<form>` / `<input>` / `<math>` / `<mtext>` 等标签 —— **我实测这些会原样存活**
  （例：`<div style="position:fixed;inset:0;background:#fff">` 可整页覆盖、伪装 UI；`<style>body{display:none}</style>` 也存活）。
  这**不构成脚本执行**（`<script>`、`on*` 事件属性、`javascript:`/`data:text/html` URI 均已被消除 —— 12 类 payload 实测干净），
  但阶段 6 会把这些 HTML 注入 DOM，且**阶段 5 的导入会让非自写内容进库** → 必须显式收紧：
  `DOMPurify.sanitize(html, { FORBID_ATTR: ['style'], FORBID_TAGS: ['style','form','input','button','math','mtext','link','meta','base'] })`
  或等价的显式白名单；并把收紧后的行为写成断言（**输出 HTML 里不得出现 `style=` 属性与上述标签**）。
- **认证**：口令哈希用成熟库（`argon2` 优先，`bcrypt` 亦可）；会话 = 服务端 `sessions` 表 + `HttpOnly; SameSite=Lax` cookie。
- **外部客户端就绪（v5 新增，用户 2026-09-18 拍板）**：
  - **API Token**：`Authorization: Bearer <token>`；① 明文**只在创建时返回一次** ② 库中只存 `sha256(token)`（hex，64 字符）
    ③ 可列出（**不含明文**）与撤销，**撤销后立即失效** ④ 记录 `last_used_at`。单用户前提下 token 与本人等价（不做 scope / 权限分层）。
  - **CORS**：**默认关闭**（同源）；用 env `CORS_ORIGINS`（逗号分隔白名单）显式开放，**禁止 `*`**；
    只允许必要方法与头（`GET/POST/PUT/DELETE/OPTIONS`、`Authorization, Content-Type`）；**不使用 `Access-Control-Allow-Credentials`**（Bearer 不需要凭证）。
  - **CLI 通道（v7 明确口径，解决"先有鸡还是先有蛋"）**：
    - `pm get` / `pm render`：**只有 HTTP 一条路**（`PM_API_URL` + `PM_API_TOKEN`）；连不上或没有 token → **非 0 退出、stdout 为空**，
      **不许静默回退直连 DB**。
    - `pm token create|list|revoke`：**双通道** —— 设了 `PM_API_URL` **且** `PM_API_TOKEN` → 走 HTTP `/api/tokens`（连不上就报错，不回退）；
      两者都没设 → 走**本机引导路径**（与 `user set-password` / `migrate` / `export` 同类的本机管理命令，经服务层而非裸 SQL），
      **必须把所用通道打印到 stderr**（如 `(bootstrap: local admin path …)`），stdout 只输出明文 token。
      理由：**没有第一个 token 就无法用 HTTP 创建 token**，引导路径是唯一自洽解；但**必须显式、不许静默**。
  - **MCP**：**stdio 传输**（由 agent 拉起），**不新增任何监听端口**（台账 8765–8770 不动）；工具面**只读**；数据访问**必须经 API + token**，**不直连 DB**。
  - **协议版本对齐（硬要求，来自 nextblog 阶段 5 的真实返工）**：开工前先确认真实对端（至少 dsh 所在宿主与 host_manger 的 Python MCP 客户端）
    支持的协议版本并写进 `PROGRESS.md`；AC 一律以「**真实对端能完成 initialize → tools/list → tools/call**」为准，
    **不以「按某版规范写对了」为准**（实现方用自家 SDK 自测通过不构成证据）。
- **使用记录契约（FR-19，v6 新增）**：
  - **记录时机（固定）**：仅当下列**读**操作成功时写一条 —— `GET /api/prompts/:id`（打开详情）、`POST /api/prompts/:id/render`（渲染取用）、
    以及 MCP 的 `prompt_get` / `prompt_render`。**列表/搜索不记**（避免浏览刷表）。
  - **通道取值（固定）**：`session`（浏览器 cookie 会话）/ `token`（`Authorization: Bearer`）/ `mcp`（MCP 通道）。
  - **副作用约束（硬性）**：写 usage **不得**产生新版本、**不得**修改 `prompts.updated_at`（否则污染版本历史与"最近更新"排序）。
  - **Prompt 对象新增只读字段**：`last_used_at`（ISO 8601 或 `null`）与 `use_count`（整数）。
  - **接口**：`GET /api/usage/summary?days=<N>`（默认 30）→ `{"days":N,"total":M,"by_channel":{"session":a,"token":b,"mcp":c},"top":[{"prompt_id","title","count","last_used_at"}]}`（top 按 count 降序、最多 20）；
    列表支持 `?sort=recent_used`（最近使用倒序，**从未使用过的排最后**，同档按 `updated_at` 倒序）。
  - **导出不含 usage**：`ExportFile`（§6.4）结构**不变**，usage 不参与导入导出（保证 AC-10 的往返一致性不被破坏）。
- **部署形态就绪（v6 新增：内网与公网两种形态都要支持）**：
  - **服务本身不做 TLS**（由反代终结）；提供三个显式开关并在文档写清用途：
    `TRUST_PROXY`（**默认关闭**——只在确有反代时开启，否则 `X-Forwarded-For` 可被伪造、限流形同虚设）、
    `PUBLIC_ORIGIN`（设置后会话 cookie 追加 `Secure`；**未设置时不得加 `Secure`**，否则内网 HTTP 会登不上）、
    `CORS_ORIGINS`（见上）。
  - **Node 服务的沙箱加固必须允许 `AF_NETLINK`（v10 新增，来自 2026-09-18 首次真实部署）**：unit 的
    `RestrictAddressFamilies` **必须包含 `AF_NETLINK`**。原因：`os.networkInterfaces()`（Fastify 启动打印监听地址时会调用）
    在 Linux 上经 libuv 的 `uv_interface_addresses` 需要 netlink socket；缺了这一条会**启动即崩**
    （`SystemError: uv_interface_addresses returned Unknown system error 97` / `errno 97 EAFNOSUPPORT`）。
    实证与处置（host_manger 用 drop-in 先行修补）见 `VERIFY.md` 的「部署记录」节。
    `deploy/README.md` 必须包含这一故障的**排查条目**（症状 → 根因 → 修法）。
  - **交付反代样例**（`deploy/reverse-proxy.example.conf`，占位符，不含真实域名/证书路径）：HTTPS 终结 + 转发
    `X-Forwarded-For` / `X-Forwarded-Proto`；`deploy/README.md` 必须覆盖「内网直连」与「公网反代」**两条部署路径**与回滚方法。
  - **不改监听地址默认值**（仍 `0.0.0.0:8767`），**不新增端口**；公网暴露动作由 host_manger 执行。
- **依赖双闸门**：安全（无未修高危 CVE、维护活跃、pin 版本 + 提交 lockfile）+ 协议（MIT/Apache/BSD/ISC 直接用；
  MPL/LGPL 可用；**GPL/AGPL/无协议 → 先提 QUESTIONS**）。
- **运行、部署文件与部署（默认，2026-09-15 用户约定）**：
  - **部署相关文件是交付物 + 验收项**：交付到 `deploy/` —— **systemd unit**
    （`promptmanager.service`，含 `User`/`Group`/`WorkingDirectory`/`EnvironmentFile`/`Restart`，
    **Node 服务不得开 `MemoryDenyWriteExecute`**）+ **`promptmanager.env.example`（口令类变量值留空）** + **部署/回滚说明**（`deploy/README.md`）；
    自检用 `systemd-analyze verify`（228 已实测可用，systemd 250）。**交付 ≠ 已部署（不得声称已部署）**。
  - **实际部署单独立项**：装到系统 / 开机自启 / 反代 / 对外暴露**只在用户明确要求时由 host_manger 执行**；你不动系统配置。
  - **服务供验收**：由你临时跑起来，或把 AC 写成"自起自停"（推荐）；验收不依赖服务常驻。
  - **备份不默认做**：只在用户单独提要求时由 host_manger 执行。
- **视觉重设计的实现边界（v11）**：见 §4 的 FR-40 —— 只允许 `antd` token/组件级定制 + CSS 变量 + 少量自定义 CSS，
  **不得**引入第二套样式体系、**不得**自建/改写组件库组件、**不得**引 CDN、**不得**复制外部站点的内容与品牌元素；
  重设计**不得**改变任何接口契约、数据模型、路由语义与既有功能行为（纯表现层）。
- **监听与端口**：服务监听 `0.0.0.0:8767`（内网可达）；端口 **8767**（台账分配，见 §6.8 实测）；
  **必须有认证**（除 `/healthz` 与登录接口外，所有 `/api/*` 必须 401 拒绝未认证请求）。
  **端口纪律**：开工前先 `ss -ltn` 实测；若 8767 被占 → 在 8765–8770 内换一个并把理由 + `ss` 输出写进 PROGRESS.md；
  候选全被占 → 写 `QUESTIONS.md` 停手（**不许自行扩大端口范围**）。
- **网络**：允许 `npm install`（228 已实测可直连 registry）；**运行时不对外发请求**（无遥测、无 CDN、无外部 API）。
- **数据与媒体路径**：`DATA_DIR`（默认 `<项目>/data`）下 `pm.db`（SQLite）与 `media/`（P1 用，先建空目录）；
  `data/` 必须写入 `.gitignore`，不得入库。
- **命名**：目录与文件小写 + 连字符。
- **禁止**：装全局包、改系统配置（systemd/nginx/防火墙）、写 `projects/promptmanager` 以外目录、改 `BRIEF.md`、
  碰别人的项目（`projects/` 下的 `nextblog`/`postkit`/`agent-session-audit`/`homelab-checkup` 一律不许动）、
  把 `_env/` 内容写进代码/日志/提交、`git add -A`。

## 6. 接口契约

> 名字固定，不许自由起名（AC 要能被逐条原样复现）。

### 6.1 HTTP API（全部 JSON、UTF-8）

| 方法 + 路径 | 说明 | 成功 | 失败 |
| --- | --- | --- | --- |
| `GET /healthz` | 健康检查（**无需认证**） | 200 `{"status":"ok","version":"<版本>"}` | — |
| `POST /api/login` | 登录，body `{"username","password"}` | 200 `{"ok":true,"username":"<u>"}` + `Set-Cookie: pm_sid=…; HttpOnly; SameSite=Lax; Path=/` | 401 `{"error":"invalid_credentials"}`；429 `{"error":"rate_limited"}`（+ `Retry-After`） |
| `POST /api/logout` | 登出（清会话） | 204 | 401 |
| `GET /api/me` | 当前用户 | 200 `{"username":"<u>"}` | 401 `{"error":"unauthorized"}` |
| `GET /api/prompts` | 列表/检索，query：`q`、`folder_id`（**含该文件夹及其全部后代**，v30；缺省/空 = 不筛选）、`tag`、`favorite`（`true`/`false`）、`limit`（默认 50，上限 200）、`offset`（默认 0） | 200 `{"total":N,"limit":L,"offset":O,"items":[Prompt,…]}` | 401 |
| `POST /api/prompts` | 新建，body = 可写字段 | 201 `Prompt`（`version_no`=1） | 400 `{"error":"invalid_body","details":[…]}`；401 |
| `GET /api/prompts/:id` | 单个 | 200 `Prompt` | 404 `{"error":"not_found"}`；401 |
| `PUT /api/prompts/:id` | 更新（可写字段的子集）→ **产生新版本** | 200 `Prompt` | 400/404/401 |
| `DELETE /api/prompts/:id` | 删除 | 204 | 404/401 |
| `GET /api/prompts/:id/versions` | 版本列表（**升序**，含首版） | 200 `{"items":[{"version_no":N,"created_at":"…","title":"…"},…]}` | 404/401 |
| `GET /api/prompts/:id/diff?from=N&to=M` | 两版本 unified diff | 200 `{"diff":"<unified diff 文本>"}` | 400（缺参/越界）/404/401 |
| `POST /api/prompts/:id/versions/:n/rollback` | 回滚到第 n 版（**生成新版本，不删历史**） | 200 `Prompt` | 404/401 |
| `GET /api/prompts/:id/variables` | 提取变量 | 200 `{"variables":["a","b"]}` | 404/401 |
| `POST /api/prompts/:id/render` | 渲染变量，body `{"values":{…}}` | 200 `{"user_prompt":"…","system_prompt":"…","missing":[…]}` | 400/404/401 |
| `POST /api/render/markdown` | Markdown → 净化 HTML，body `{"markdown":"…"}` | 200 `{"html":"…"}` | 400/401 |
| `GET /api/folders` | 文件夹列表 | 200 `{"items":[{"id","name","parent_id","sort_order"},…]}` | 401 |
| `POST /api/folders` / `PUT /api/folders/:id` / `DELETE /api/folders/:id` | 文件夹增改删 | 201 / 200 / 204 | 400/404/409（有子项或非空）/401 |
| `GET /api/tags` | 标签列表（含计数） | 200 `{"items":[{"id","name","count"},…]}` | 401 |
| `POST /api/tags` / `PUT /api/tags/:id` / `DELETE /api/tags/:id` | 标签增 / 改名（**与已存在标签同名时合并**）/ 删 | 201 / 200 / 204 | 400/404/401 |
| `GET /api/export` | 全量导出（§6.4） | 200 `ExportFile` | 401 |
| `POST /api/import` | 导入，body `{"mode":"replace"\|"merge","data":ExportFile}` | 200 `{"mode":…,"imported":{"folders":n,"tags":n,"prompts":n}}` | 400 `{"error":"invalid_import","details":[…]}`；401 |

**Prompt 对象**（字段名固定，DB 列名可不同）：

```json
{"id":1,"title":"…","user_prompt":"…","system_prompt":"…","notes":"…",
 "folder_id":null,"tags":["a","b"],"favorite":false,
 "created_at":"2026-09-18T02:00:00.000Z","updated_at":"2026-09-18T02:00:00.000Z","version_no":3}
```

时间一律 **ISO 8601 UTC**（以 `Z` 结尾、毫秒精度）。`folder_id` 为 `null` 表示未归类；`tags` 为字符串数组（按标签名升序）。

### 6.2 CLI（`bin/pm.mjs`，名字固定）

```bash
node bin/pm.mjs migrate                          # 执行 migrations/*.sql（幂等）；输出 ok: schema at v<N>
node bin/pm.mjs user set-password --username <u>  # 口令从 stdin 读；用户不存在则创建
                                                 # 输出 ok: user <u> password updated（绝不打印口令）
node bin/pm.mjs export --out <path>              # 导出与 GET /api/export 同格式（可选命令，用于备份）
```

**退出码语义**：`0` 成功 / `1` 运行时错误（DB 不可写、迁移失败等）/ `2` 用法错误（参数缺失或未知选项）。
CLI 与 HTTP 共用同一 `DATA_DIR` 与 `PORT`（CLI 不需要监听端口）。

### 6.3 环境变量与数据文件

| 变量 | 默认 | 说明 |
| --- | --- | --- |
| `PORT` | `8767` | 监听端口 |
| `HOST` | `0.0.0.0` | 监听地址（**不许默认 127.0.0.1**） |
| `DATA_DIR` | `<项目>/data` | 数据目录（不存在则创建）；内含 `pm.db` 与 `media/` |
| `SESSION_TTL_HOURS` | `720` | 会话有效期 |
| `LOGIN_MAX_FAILURES` | `5` | 登录失败阈值（窗口内） |
| `LOGIN_WINDOW_SECONDS` | `60` | 登录失败统计窗口 |

数据库文件固定为 `$DATA_DIR/pm.db`；`pm.db` 不存在时启动自动迁移到最新 schema。

**package.json scripts（名字固定）**：`npm run build`（前端构建 + 服务端编译产物）、`npm start`（起服务）、
`npm test`（全量测试）、`npm run migrate`。

### 6.4 导出文件格式（`ExportFile`）

```json
{"app":"promptmanager","schema_version":1,"exported_at":"2026-09-18T02:00:00.000Z",
 "folders":[{"id":1,"name":"运维","parent_id":null,"sort_order":0}],
 "tags":[{"id":1,"name":"交接"}],
 "prompts":[{"id":1,"title":"…","user_prompt":"…","system_prompt":"…","notes":"…",
   "folder_id":1,"tags":["交接"],"favorite":false,
   "created_at":"…","updated_at":"…",
   "versions":[{"version_no":1,"title":"…","user_prompt":"…","system_prompt":"…","notes":"…","created_at":"…"}]}]}
```

- **确定性**：`folders`/`tags` 按 `id` 升序、`prompts` 按 `id` 升序、`versions` 按 `version_no` 升序、`tags` 数组按名称升序；
  `exported_at` 是唯一不具确定性的字段。
- **`replace` 模式**：清空现有 prompt/folder/tag/版本后按文件内容重建，**保留文件里的 `id`**（导出 → 导入 → 再导出应除 `exported_at` 外完全一致）。
- **`merge` 模式**：不清库；同名 folder/tag 复用，prompt 一律新建并重新分配 `id`（用于合并场景）。
- 校验：`app` 必须是 `promptmanager`、`schema_version` 必须 ≤ 当前支持版本，否则 400 `invalid_import` 且**不得改动任何数据**。

### 6.5 变量语法契约（FR-8）

- 占位形式：`{{` + 可选空白 + 变量名 + 可选空白 + `}}`
- 变量名：1–64 字符，允许 Unicode 字母/数字/下划线/连字符（**含中文**），**不含空白**；空名（`{{}}`/`{{  }}`）不算变量
- 提取顺序：按**首次出现位置**升序，去重
- 转义：`\{{name}}` → 字面文本 `{{name}}`（不提取；渲染时去掉前导反斜杠）
- 渲染：提供的值替换；**未提供值的变量保持原样** `{{name}}` 并列入 `missing`（数组，按提取顺序）
- 渲染只返回文本，**不写库**

### 6.6 检索语义契约（FR-6）

- 匹配范围：`title` + `user_prompt` + `system_prompt` + `notes`
- 英文大小写不敏感
- **长度归一化（关键）**：查询词去掉首尾空白后，按 **Unicode 码点计数**：
  - **≥3 个字符** → FTS5 `trigram` 索引 `MATCH`（按相关性 `bm25` 排序，同级按 `updated_at` 倒序）
  - **<3 个字符（如中文两字词"交接"）** → `LIKE '%q%'` 兜底扫描（按 `updated_at` 倒序）
    —— 原因：**trigram 分词器对 <3 字符的查询返回 0 命中（228 实测）**，中文两字词极常见，必须兜底
- 查询词一律当**字面短语**处理：`"`、`*`、`-`、`(`、`)`、`%`、`_` 等字符不得导致 500 或语法错误
  （FTS5 路径需加引号并转义内部引号；`LIKE` 路径需转义 `%`/`_`）
- `q` 为空/缺省 → 返回全部（`updated_at` 倒序）
- 索引必须随 prompt 的增删改**同步维护**（含版本回滚与导入后的重建）

### 6.7 目录结构（建议，可微调）

```
bin/pm.mjs            # CLI（migrate / user set-password / export）
src/server/           # HTTP 层（路由、认证、错误处理）
src/db/               # 迁移执行、查询、FTS 维护
src/services/         # prompt/folder/tag/version/variable/markdown 业务
migrations/001_init.sql
web/                  # 前端源码（React + Vite）
dist/                 # 构建产物（gitignore）
tests/                # 测试（≥8 个文件，见 AC-16）
deploy/               # systemd unit + env.example + 部署/回滚说明（交付物）
docs/dependencies.md  # 依赖清单 + 版本 + 协议 + 用途
docs/search-zh.md     # 中文检索方案实测报告（阶段 1 产出）
docs/shots/           # 界面自证截图（阶段 6 产出）
tools/ui-shots.sh     # 界面截图脚本（阶段 6 产出，AC-13 调用）
README.md             # 是什么 / 怎么跑 / 怎么验证 / 已知限制
```

### 6.8 实测事实（host_manger 在 228 实测，2026-09-18；dsh 开工前须复核版本号）

**环境**

- `node -v` = **v24.18.0**；`npm -v` = 11.16.0；`pnpm -v` = 11.22.0；`python3` = 3.14.7；`gcc` 11.5.0；`make` 4.3
- 无 Docker；npm registry 可达（直连 registry.npmjs.org，10 个包版本一次取到）
- 磁盘：`/` 30G，已用 14G，可用 15G

**SQLite / 中文检索（本项目最大风险点，已实测）**

- Node 内置 `node:sqlite`：`sqlite_version = 3.53.1`，`create virtual table … using fts5(body, tokenize='trigram')` **成功**（无需 flag）
- `better-sqlite3@13.0.3`：`npm install` **5 秒**（预编译），`sqlite_version = 3.53.4`，FTS5 trigram 同样通过
- **trigram 实测**（5 条中文语料）：`"会话交接"`→1 命中、`"交接文档"`→1、`"rebase"`→1、`"FTS5"`→1、`"无关词汇"`→0
- **unicode61 对照（默认分词器）**：**不能做中文子串检索** —— 它只在**分词边界**偶然命中。
  我（host_manger）POC 语料下 `"会话交接"`/`"交接"`/`"上下文"` 全 **0 命中**；
  dsh 阶段 1 复测（`docs/search-zh.md`）用 `会话交接：把工作上下文交给下一个同学` 时 `会话交接` = **1 命中**，
  原因是**全角冒号是分词分隔符**，`会话交接` 恰好被切成一个完整 token（换语料立刻 0 命中）。
  → **数字随语料变，"0 命中"不是普适断言**；不变的是结论：**必须用 trigram + <3 字符 LIKE 兜底**（D-3）。
  权威记录：`docs/search-zh.md` §2（含该差异的原因与复核方式）。
- **⚠️ trigram 的长度下限（关键坑）**：2 字符查询 **0 命中** —— `"交接"`→0、`"会话"`→0、`"AI"`→0；
  而同样语料下 `LIKE '%交接%'` → 命中，且 **2000 行只要 0.3 ms**（`LIKE '%会话%'` 0.2 ms）→ 见 §6.6 兜底策略
- 特殊字符（`"`、`*`、`-`、`a-b`、`C++`、`100%`）作为 `MATCH` 短语（加引号 + 转义）**不报错**，返回 0 命中
- 规模基线：2000 条中文记录，`MATCH "会话交接"` → 2000 命中 / **0.3 ms**

**界面自证能力**

- headless 浏览器（**Playwright 缓存，不在 PATH**）：
  `ls -d /root/.cache/ms-playwright/chromium_headless_shell-*/*/chrome-headless-shell` →
  `/root/.cache/ms-playwright/chromium_headless_shell-1234/chrome-headless-shell-linux64/chrome-headless-shell`
  与 `…-1243/…` 两个版本（本目录 `ls -d /root/.cache/ms-playwright/*/` 也可列出）
- 命令行截图**已实测可用**（无需装 playwright）：
  `<bin> --headless --no-sandbox --disable-gpu --hide-scrollbars --window-size=1280,800 --screenshot=<out.png> <url>`
  → 输出 `19996 bytes written to file …`（dbus 报错属正常噪音，可忽略）
- ⚠️ 探测口径：`/root` 下带通配符的命令**必须让 glob 在 root 的 shell 里展开**
  （`sudo bash -c 'ls -d /root/.cache/ms-playwright/*/'`）；写成 `sudo ls -d /root/.../*/` 会因 ops 无权限展开而**误判成"没有"**
- CJK 字体：仅 `/usr/share/fonts/google-droid-sans-fonts/DroidSansFallbackFull.ttf`（Droid Sans Fallback）→ 截图中文可渲染，字形一般

**端口与周边**

- **8767 实测空闲**（`ss -ltn | grep -E ':(876[5-9]|8770)\b'` 仅 8769 被 izone 本地开发实例占用）
- `systemd-analyze` 可用（systemd 250）

**依赖版本（`npm view <pkg> version` 实测，2026-09-18；开工前复核）**

| 包 | 版本 | 包 | 版本 |
| --- | --- | --- | --- |
| fastify | 5.12.5 | react | 19.3.0 |
| typescript | 7.0.2 | vite | 8.3.0 |
| **antd** | **6.6.4** | **@ant-design/icons** | **6.3.4** |
| better-sqlite3 | 13.0.3 | kysely | 0.29.6 |
| marked | 18.0.13 | dompurify | 3.4.15 |
| highlight.js | 11.12.0 | zod | 4.6.5 |

## 7. 交付物清单（路径级）

- `bin/pm.mjs`（CLI）、`src/**`（服务端）、`web/**`（前端源码）、`migrations/*.sql`
- `tests/**`（≥8 个测试文件）
- `docs/dependencies.md`（依赖 + 版本 + 协议 + 用途）、`docs/search-zh.md`（中文检索方案实测报告）
- `docs/shots/*.png` + `tools/ui-shots.sh`（界面自证）
- `package.json` + `package-lock.json`（包管理器固定用 **npm**，系统自带 11.16.0；AC 用 `npm ci` 复现，**不要用 pnpm**）
- `bin/pm-mcp.mjs`（**MCP stdio 入口，名字固定**）+ `src/mcp/**`（MCP 工具面实现）
- `deploy/mcp-register.example.json`（给 MCP 客户端用的注册片段示例；token 用占位符，**不许写真实值**）
- `deploy/reverse-proxy.example.conf`（公网反代样例：HTTPS 终结 + `X-Forwarded-For`/`X-Forwarded-Proto` 转发；**占位符，不含真实域名/证书路径**）
- `deploy/promptmanager.service` + `deploy/promptmanager.env.example`（口令类值留空）+ `deploy/README.md`（安装/验证/回滚）
- `README.md`（四要素：是什么 / 怎么跑 / 怎么验证 / 已知限制）
- `.gitignore`（必须含 `node_modules/`、`dist/`、`data/`、`.env`、`_env/`）
- `tools/ac-stage*.sh` + `tools/ac-stage*-probe.mjs`（**逐阶段 AC 自检脚本**，当前 25 个阶段 + 各阶段 probe）
- `tools/ui-shots.sh` + `tools/ui-shot.mjs`（**界面自证截图**：自起自停临时实例 + headless chromium）
- `docs/shots/*.png`（**当前状态**的界面截图，顶层 53 张；各阶段过程截图归档在 `docs/dev-history/shots/`）
- `docs/dev-history/**`（**开发期过程文档归档**：PROGRESS / VERIFY / QUESTIONS-history / design / shots）
- `docs/brief-changelog.md`（BRIEF 变更历史 v1–v32；从 §12 外移）
- `CHANGELOG.md`（**人可读**的版本变更记录，与 semver 对齐）
- `.github/workflows/*.yml`（**CI 代码质量检查**；同一套检查必须能在本地执行，命令写进 README）
- **容器化部署文件**：`Dockerfile`、`docker-compose.yml`、`.dockerignore`、`deploy/container.md`
- **`AGENTS.md`**（**项目级 AI 代理操作指南** —— 给"在这个仓库里干活的 AI 代理 / 新会话"看：
  常用命令、结构地图、代码约定、数据与迁移、测试、验证与自证、红线、**本项目特有的坑**、文档地图、协作约定。
  ⚠️ **必须全英文**（`AGENTS.md` 是纯 AI 指令文件、受众是 AI；见 `/root/greenhouse/STANDARDS.md` §5.1 文档语言规范）。
  **由实现方根据自己对项目的真实了解撰写**，且**每一节都要经过验证**（命令真跑、路径真查），见 AC-83）

  —— **由 host_manger 交付**（不在 dsh 的实现范围；dsh 只负责在 README 里注明"即将提供容器化部署"）


## 8. 验收标准（可执行，逐条）

> 每条必须能被 dsh 和我独立复现；dsh 收尾时把**实际输出**贴进 `PROGRESS.md`。
> **AC 自检要求**（写 AC 时踩过的）：① 断言退出码时别让 `$?` 被管道吞；② AC 之间无隐式顺序依赖（各自造夹具）；
> ③ 改状态的 AC 只用临时 `DATA_DIR`/临时端口，**绝不改生产文件**；④ "只读/无副作用"要写可执行证据。
> **AC 里引用的每个子命令 / 字段 / 路径 / 退出码，都必须先在 §6 契约里出现过。**

**统一夹具约定**（每条 AC 自带，互不依赖）：

```sh
AC_DIR=$(mktemp -d /tmp/pm-ac-XXXXXX)          # 临时数据目录，跑完删
export DATA_DIR="$AC_DIR" PORT=8767            # HOST 不设 → 走默认 0.0.0.0（这正是要验收的行为）
export AC_PW='ac-fixture-pw-20260918'          # 夹具口令（仅为测试用，不是真实凭据）
printf '%s\n' "$AC_PW" | node bin/pm.mjs user set-password --username admin
```

> 夹具实例**自起自停**：每条 AC 跑完必须 `kill` 自己起的服务（`ss -ltn | grep :8767` 核验为 0 行），
> 不留常驻进程、不留临时数据（`rm -rf "$AC_DIR"`）。

- **AC-1 冷装与构建**
  - 命令：`rm -rf node_modules dist && npm ci && npm run build; echo "rc=$?"`
  - 期望：`rc=0`；`dist/` 存在；`git status --short` 无新增未跟踪的构建产物（产物已被 `.gitignore` 覆盖）
  - 实测：（dsh 填）
- **AC-2 启动与监听**
  - 命令：`DATA_DIR=$AC_DIR PORT=8767 npm start &` 后 `sleep 3; ss -ltn | grep ':8767'; curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8767/healthz; curl -s http://127.0.0.1:8767/healthz`
  - 期望：`ss` 显示 `0.0.0.0:8767`（**不是 127.0.0.1:8767**）；HTTP `200`；body 含 `"status":"ok"` 与 `"version"`
- **AC-3 未认证一律拒绝**
  - 命令：`curl -s -o /dev/null -w '%{http_code} ' http://127.0.0.1:8767/api/prompts; curl -s -o /dev/null -w '%{http_code} ' -X POST -H 'Content-Type: application/json' -d '{}' http://127.0.0.1:8767/api/prompts; curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8767/api/export`
  - 期望：`401 401 401`
- **AC-4 登录 / 会话 / 限流**
  - 命令：`curl -s -c /tmp/pm-jar -o /dev/null -w '%{http_code} ' -X POST -H 'Content-Type: application/json' -d "{\"username\":\"admin\",\"password\":\"$AC_PW\"}" http://127.0.0.1:8767/api/login` → 期望 `200`；
    `curl -s -b /tmp/pm-jar -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8767/api/prompts` → 期望 `200`；
    错误口令 → `401`；连续 5 次错误口令后第 6 次 → `429`
  - 期望：`200 200 401 429`；cookie 名为 `pm_sid` 且带 `HttpOnly`
- **AC-5 prompt CRUD 与版本号**
  - 命令：建一条（`title`/`user_prompt`/`system_prompt`/`notes` 全填、带 1 个标签）→ `201` 且返回 `version_no=1`；
    `GET /api/prompts/:id` → 全字段与提交一致（**逐字段比对**）；`PUT` 改 `notes` → `version_no=2`；`DELETE` → `204`；再 `GET` → `404`
  - 期望：字段逐项一致（用 `python3` 比对并打印 `MATCH`/`DIFF`）
- **AC-6 中文检索（三种长度 + 特殊字符）**
  - 夹具：建 5 条含固定中文语料的 prompt（其中 1 条含 `会话交接`、1 条含 `交接文档`、全部含 `上下文`）
  - 命令与期望：
    - `?q=会话交接` → `total=1` 且命中目标 id（**≥3 字符走 FTS5 路径**）
    - `?q=交接` → 命中 ≥2 条（**2 字符走 LIKE 兜底**，这是 trigram 单独做不到的）
    - `?q=不存在的词` → `total=0`
    - `?q=%22%2A-%25_`（特殊字符）→ HTTP `200`（不得 500）
  - 实测：（dsh 填）
- **AC-7 检索性能（规模基线）**
  - 夹具：脚本生成 **2000 条**中文 prompt（标题+正文含 `会话交接`）写入临时库
  - 命令：`for i in 1 2 3; do curl -s -o /dev/null -w '%{time_total}\n' "http://127.0.0.1:8767/api/prompts?q=会话交接"; done`
  - 期望：三次均 `< 0.5`（秒）且 `total=2000`
- **AC-8 变量提取与渲染**
  - 夹具：一条 `user_prompt` = `你好 {{ 姓名 }}，重复 {{姓名}} 与 \{{保留}} 以及 {{var-b}}`
  - 命令：`GET …/variables` → 期望 `{"variables":["姓名","var-b"]}`（**顺序 = 首次出现、去重、转义的不算**）；
    `POST …/render`（只给 `姓名`）→ `user_prompt` 中 `{{姓名}}` 被替换、`{{var-b}}` **原样保留**、`missing=["var-b"]`
  - 期望：变量数组逐元素一致；渲染后文本**逐字符**与预期串相等（用 `python3` 比较并打印 `MATCH`）
- **AC-9 版本历史：列表 / diff / 回滚**
  - 命令：建 prompt（v1）→ 改 2 次（v2/v3）→ `GET …/versions` → `[1,2,3]`；
    `GET …/diff?from=1&to=3` → 输出统一 diff，含 `-` 行与 `+` 行；
    `POST …/versions/1/rollback` → 200，`GET` 后正文 = v1 正文（逐字符），`versions` 变为 `[1,2,3,4]`（**v1..v3 仍在**）
- **AC-10 导出 → 导入(replace) → 再导出：一致性**
  - 命令：`curl -b jar -s http://127.0.0.1:8767/api/export > /tmp/a.json`；`POST /api/import {"mode":"replace","data":<a.json>}`；再导出 `/tmp/b.json`；
    `python3 -c` 去掉 `exported_at` 后比对两份 JSON → 打印 `EQUAL`/`DIFF`
  - 期望：`EQUAL`；且导入响应 `imported.prompts` 等于导出的 prompt 数
- **AC-11 非法导入不动数据**
  - 命令：记录当前 prompt 数 → `POST /api/import` body `{"mode":"replace","data":{"app":"other","schema_version":1}}` → 期望 HTTP `400` 且 body 含 `invalid_import`；再查 prompt 数 → **与导入前一致**
- **AC-12 Markdown 渲染 + XSS 净化 + 高亮**
  - 命令：`POST /api/render/markdown` body 含 `<script>alert(1)</script>`、`[x](javascript:alert(1))`、```` ```js\nconst a=1\n``` ````
  - 期望：响应 `html` **不含** `<script`、**不含** `javascript:`；含 `<pre` 或 `<code` 且带高亮 class（如 `hljs`）
- **AC-13 界面自证（截图 + 识图）**
  - 命令：`bash tools/ui-shots.sh`（服务自起自停）
  - 期望：产出 **≥6 张** png 到 `docs/shots/`，至少覆盖：登录页、prompt 列表、编辑器、搜索结果、移动宽度（390×844）、暗色主题；
    尺寸正确（桌面 1280×800、移动 390×844）；且 `PROGRESS.md` 含**逐张**识图结论（四问：文字重叠/遮挡、硬断词或单字成行、孤标题沉底、溢出裁切 + 是否达到可用视觉风格），
    **修完的再截一组（修前/修后成对）**
  - 提示：headless chromium 路径与命令行用法见 §6.8（已实测可截图）
- **AC-14 文件夹与标签**
  - 命令：建两个文件夹（父子）→ `GET /api/folders` 结构正确；建两个标签 → 把 prompt 归入文件夹 + 打标签 →
    `GET /api/prompts?folder_id=X` 与 `?tag=Y` 各命中 1 条；`PUT /api/tags/:id` 改名成已存在的名字 → 两标签**合并**（`GET /api/tags` 计数相加、无重名）；删除非空文件夹 → `409`
- **AC-15 重启数据持久**
  - 命令：建 prompt → `kill` 服务 → 用**同一 `DATA_DIR`** 重启 → `GET /api/prompts` 仍返回该 prompt 且正文逐字符一致；`sqlite3`-级别自查：`ls -l $AC_DIR/pm.db`（WAL 模式下 `pm.db-wal` 存在属正常）
- **AC-16 全量测试**
  - 命令：`npm test; echo "rc=$?"`
  - 期望：`rc=0`、0 失败；`ls tests/*.test.* 2>/dev/null | wc -l` **≥ 8**
- **AC-17 凭据与产物卫生**
  - 命令：`git check-ignore -v data/pm.db .env _env/x`（三条都必须有输出）；
    `git grep -nE "(password|passwd|secret|token)[[:space:]]*[:=][[:space:]]*['\"][^'\"]{8,}" -- 'src/**' 'bin/**' 'web/**' ':(exclude)tests/**'` → **0 命中**；
    `grep -cE '^\|.*\|' docs/dependencies.md` ≥ 直接依赖数（`npm ls --depth=0 --parseable | tail -n +2 | wc -l`，**口径：直接依赖，不含传递依赖与本项目自身**）
- **AC-18 部署文件（交付物验收）**
  - 命令：`sudo systemd-analyze verify deploy/promptmanager.service 2>&1 | grep -c error` → `0`；
    `grep -cE '^(User|Group|WorkingDirectory|EnvironmentFile|Restart)=' deploy/promptmanager.service` → `5`；
    `grep -c MemoryDenyWriteExecute deploy/promptmanager.service` → `0`；
  - 期望：`0` / `5` / `0` / `0`
  - **⑤（v10 新增，来自首次真实部署的缺陷）** 沙箱允许列表必须含 netlink：
    命令：`grep -cE '^RestrictAddressFamilies=.*AF_NETLINK' deploy/promptmanager.service` → 期望 `1`；
    `grep -c 'AF_NETLINK' deploy/README.md` → 期望 `≥1`（排查条目已写）。
    ⚠️ **运行时**验证（以真实 systemd unit 真起一次）由 host_manger 在部署时执行 ——
    `systemd-analyze verify` **只验语法、拦不住运行时不兼容**（2026-09-18 实测：verify 全绿，服务却在 systemd 下启动即崩）。
- **AC-19 端口纪律与文档一致**
  - 命令：`grep -rn '8767' package.json src bin deploy README.md | head`；`ss -ltn | grep -c ':8767'`（服务在跑时应为 1）；
    `grep -n '8767' PROGRESS.md README.md` 均有命中
  - 期望：默认端口在**配置默认值 / README / PROGRESS** 三处一致，且 README 写明监听地址 `0.0.0.0`
- **AC-20 前端组件库落地（用户 2026-09-18 硬性要求：不许自己造轮子）**
  - 命令：
    ① `npm ls antd @ant-design/icons --depth=0`
    ② `grep -rnE "<(button|input|select|textarea|table|dialog)[ >/]" web/src --include='*.tsx' | wc -l`
    ③ `grep -rnoE "from ['\"]antd['\"]" web/src --include='*.tsx' | wc -l`
    ④ `grep -rnE "(cdn|unpkg|jsdelivr|googleapis)" dist/ web/src --include='*.html' --include='*.tsx' --include='*.css' | wc -l`
  - 期望：① 两者都在直接依赖里且版本与 `docs/dependencies.md` 一致（antd **6.6.4**、icons 6.3.4；**不得出现** `@ant-design/v5-patch-for-react-19`）；
    ② = `0`（**UI 一律来自组件库，源码里不出现原生表单/表格标签**；若确有无法避免的一处，须在 `PROGRESS.md` 写明并经 host_manger 认可）；
    ③ ≥ `5`（确实在用组件库，而不是只装不用）；④ = `0`（构建产物与源码**零 CDN 外链**）
  - 实测：（dsh 填）
- **AC-21 组件库是真的在渲染（不只看依赖）**
  - 命令：起服务后用 headless chromium 抓列表页/编辑器页 HTML，`grep -o 'ant-[a-z-]*' <html> | sort -u | wc -l`（服务端渲染或前端产物中的 antd 类名）
  - 期望：出现 ≥ `3` 个不同的 `ant-*` 类名（如 `ant-btn`/`ant-table`/`ant-form`）→ 证明界面由组件库渲染
  - 注：`AC-13` 的截图里也必须能看出组件库视觉（禁止"看着像手搓的原生控件"）

- **AC-22 API Token 认证（v5 新增）**
  - 命令：① `TOKEN=$(node bin/pm.mjs token create --name ac | tail -1)`（打印形如 `pm_…` 的一行明文；
    **未设 `PM_API_URL`/`PM_API_TOKEN` 时走本机引导路径**，stderr 会打印所用通道 —— 见 §5）
    ② `python3 -c "import sqlite3;print(sqlite3.connect('$DATA_DIR/pm.db').execute('select length(token_hash) from api_tokens').fetchone()[0])"` → 期望 `64`（sha256 hex）
    ③ `curl -s -o /dev/null -w '%{http_code}\n' -H "Authorization: Bearer $TOKEN" http://127.0.0.1:8767/api/prompts` → 期望 `200`
    ④ `curl -s -o /dev/null -w '%{http_code}\n' -H "Authorization: Bearer wrong-token" http://127.0.0.1:8767/api/prompts` → 期望 `401`；不带凭据 → `401`
    ⑤ `node bin/pm.mjs token list` → 输出**不含明文 token**；`node bin/pm.mjs token revoke <id>` 之后重跑 ③ → 期望 `401`
    ⑥ cookie 路径不回归：登录后带 cookie 访问 `/api/prompts` → `200`
  - 期望：如上；且 `grep -c "$TOKEN" <服务日志>` → `0`（明文不落日志）
- **AC-23 CORS（v5 新增）**
  - 命令：① **未配置** `CORS_ORIGINS` 时：`curl -s -D - -o /dev/null -H 'Origin: https://evil.example' http://127.0.0.1:8767/healthz | grep -ci 'access-control-allow-origin'` → 期望 `0`
    ② 配置 `CORS_ORIGINS=https://prompt.home.local` 重启后：来自该 origin 的请求 → 响应头为
    `Access-Control-Allow-Origin: https://prompt.home.local`（**精确 origin，不得是 `*`**）；白名单外的 origin → 仍 `0`
    ③ 预检：`curl -s -D - -o /dev/null -X OPTIONS -H 'Origin: https://prompt.home.local' -H 'Access-Control-Request-Method: GET' -H 'Access-Control-Request-Headers: authorization' http://127.0.0.1:8767/api/prompts | grep -ci 'access-control-allow-headers'` → 期望 `≥1`
    ④ 任何响应都**不得**出现 `Access-Control-Allow-Credentials: true`
- **AC-24 使用侧 CLI（v5 新增）**
  - 命令：`PM_API_URL=http://127.0.0.1:8767 PM_API_TOKEN=$TOKEN node bin/pm.mjs get '会话交接' --json` → 期望 JSON 数组且条数 = 夹具命中数；
    `… get --id <N> --json` → 单条；`… get --id 99999` → 退出码 `1`；
    `… render --id <N> --set 姓名=张三` → stdout 为渲染后的 `user_prompt`，**与 `/api/render` 的返回值逐字符一致**；
    用法错误 → 退出码 `2`；`PM_API_URL=http://127.0.0.1:1 node bin/pm.mjs get x` → **非 0 退出**（不得静默回退直连 DB）
- **AC-25 MCP 端到端（真实对端，v5 新增）**
  - 命令：用**真实 MCP 客户端**（在 `PROGRESS.md` 写清客户端名称 / 版本 / 协议版本）连 `node bin/pm-mcp.mjs`（stdio），完成 `initialize` → `tools/list` → `tools/call`
  - 期望：`tools/list` **恰好**包含 `prompt_search` / `prompt_get` / `prompt_render`（**名字固定**）；
    `prompt_search("会话交接")` 返回命中；`prompt_get(<id>)` 返回 user/system/notes/variables；`prompt_render(<id>, {"姓名":"张三"})` 返回渲染成品且与 API 一致
  - **注意**：开工前先与真实对端确认协议版本（教训见 §5）；对端连不上 = 需求未达成，**不许以「自测 SDK 通过」代替**
- **AC-26 MCP 形态与安全（v5 新增）**
  - 命令：① `ss -ltn | grep -cE ':(876[5-9]|8770)\\b'` → 与验收前一致（**不新增监听**）
    ② `tools/list` 中**不存在**写操作工具（create/update/delete/import 类）→ 期望 `0` 个
    ③ 未设置 `PM_API_TOKEN` 时调用工具 → 返回**明确错误**（不许静默返回空数组 / 空对象）
    ④ 停掉服务后调用工具 → 返回连接错误（证明**经 API 而非直连 DB**）


- **AC-27 使用记录（v6 新增，FR-19）**
  - 命令：① 用 token 调 `GET /api/prompts/<id>` 两次、`POST /api/prompts/<id>/render` 一次 → `GET /api/usage/summary?days=7`
    期望 `by_channel.token = 3`、`top[0].prompt_id = <id>`、`top[0].count = 3`
    ② 用 MCP 的 `prompt_get` 取一次 → `by_channel.mcp = 1`、`total` 增加 1
    ③ 用浏览器 cookie 打开该 prompt 详情一次 → `by_channel.session = 1`
    ④ **副作用断言**：上述取用之后，该 prompt 的 `version_no` 与 `updated_at` **均未变化**
       （命令：取用前后各 `GET /api/prompts/<id>`，比对 `version_no` 与 `updated_at` → 期望 `SAME`）
    ⑤ `GET /api/prompts?sort=recent_used` → 刚用过的排最前；**从未使用过的排在所有用过的之后**（同档按 `updated_at` 倒序）
    ⑥ 导出→导入往返（AC-10 的命令）仍然 `EQUAL`（usage 不参与导出）
  - 期望：如上；且 `use_count` / `last_used_at` 出现在 Prompt 对象里
- **AC-28 部署形态（内网直连 + 公网反代，v6 新增）**
  - 命令：① **未设 `TRUST_PROXY`**（默认）：连续 6 次错误登录、**每次带不同的 `X-Forwarded-For`** →
    期望第 6 次仍是 `429`（限流按真实来源 IP 计，**不被 XFF 绕过**）
    ② 设 `TRUST_PROXY=1` 重启：请求带 `X-Forwarded-For: 203.0.113.9` → 服务日志/请求记录中出现该地址
    （命令：`grep -c '203.0.113.9' <服务日志>` → 期望 `≥1`）
    ③ **未设 `PUBLIC_ORIGIN`**：登录响应 `Set-Cookie` **不含** `Secure`；设 `PUBLIC_ORIGIN=https://prompt.example.com` 重启后
    → `Set-Cookie` **含** `Secure`（内外网两种形态各自可用）
    ④ `deploy/reverse-proxy.example.conf` 存在，含 `X-Forwarded-For` 转发与 HTTPS 相关指令，且**不含真实域名/证书路径**
    （命令：`grep -cE 'prompt\.example\.com|/etc/letsencrypt' deploy/reverse-proxy.example.conf` → 期望 `0`）
    ⑤ `deploy/README.md` 同时覆盖「内网直连」与「公网反代」两条路径 + 回滚（命令：`grep -cE '内网|公网|反代|回滚' deploy/README.md` → 期望 `≥4`）
    ⑥ `ss -ltn | grep -cE ':(876[5-9]|8770)\\b'` → 与验收前一致（**不新增监听**、默认监听地址未改）


- **AC-29 视觉重设计的"方向已落地"证据（v11 新增）**
  - 命令：① `grep -rnE "token:\s*\{|components:\s*\{" web/src --include='*.tsx' | wc -l` → 期望 `≥1`（**主题 token 真的被定制了**，
    不是只有 `algorithm`）；② `git grep -nE "cdn|unpkg|jsdelivr" -- 'web/src' 'dist'` → `0`；
    ③ `grep -rnE "<(button|input|select|table)\b" web/src --include='*.tsx' | wc -l` → `0`（组件库约束不回归，AC-20 复跑）；
    ④ AC-21 复跑：新界面下真实 DOM 的 `ant-*` 类名种类 ≥ 3。
  - 期望：四条全过，并在 `PROGRESS.md` 贴原样输出。
- **AC-30 全站覆盖 + 界面自证（v11 新增）**
  - 命令：`bash tools/ui-shots.sh docs/shots`（服务自起自停）
  - 期望：**覆盖全部界面**：登录页 / prompt 列表 / 编辑器 / 版本历史 / 变量填值 / Markdown 预览 / 导入导出（含 replace 二次确认）/
    CLI·Token 相关抽屉或弹窗（如界面有）/ 移动宽度 390×844 / 暗色主题 —— 每项至少 1 张 **1280×800 或 390×844** 的 png，
    且 `PROGRESS.md` 含**逐张识图结论**（① 文字重叠/遮挡 ② 硬断词或单字成行 ③ 孤标题沉底 ④ 溢出/裁切 ⑤ **是否达到既定美学方向**）；
    **修前/修后成对**保留（`docs/shots/before/`）。
  - 说明：AC-13 的判据照旧沿用（本 AC 是它在"重设计"场景下的加强版）。


- **AC-31 结构/排版重设计已落地（v12 新增，机械判据）——【自 v17/v18 起部分作废】**：
  其中 `pm-kpi-row` / `pm-filter-row` / `pm-statusbar` 随 FR-43 取消管理页而**移除**（这些属于旧的管理视图骨架），
  **不再作为判据**；仍有效的部分：`pm-topnav` / `pm-sidebar` / 编辑器三栏 `pm-panel-*`（同时可见 + 详情面 `.ant-tabs-tab`=0）。
  （原条款正文保留在下方，仅作历史记录）
  - 命令（源码侧）：① `grep -rc 'data-testid="pm-panel-versions"' web/src --include='*.tsx'` → 期望 `≥1`；
    ② `grep -rc 'data-testid="pm-kpi-row"' web/src --include='*.tsx'` → 期望 `≥1`；
    ③ `grep -rc 'data-testid="pm-statusbar"' web/src --include='*.tsx'` → 期望 `≥1`。
  - 命令（运行时侧，服务自起自停 + CDP/Playwright；实现方与我方都用同一条）：
    ④ 打开任一 prompt 详情后 —— `document.querySelectorAll('[data-testid^="pm-panel-"]').length` → 期望 `3`；
    ⑤ **三块同时可见**：`[...document.querySelectorAll('[data-testid^="pm-panel-"]')].every(e => e.offsetParent !== null)` → 期望 `true`
       （证明是"常驻三栏"而不是标签页切换）；
    ⑥ 详情面上 `.ant-tabs-tab` 计数 → 期望 `0`（这三块不再由标签页承载）；
    ⑦ 列表面上 `[data-testid="pm-kpi-row"]` 内的数字元素数 → 期望 `≥4`。
  - 期望：①–⑦ 全过，原样输出贴进 `PROGRESS.md`；并在 `PROGRESS.md` 给一张「**设计稿结构 → 实现落点**（文件/组件）」对照表（≥6 行）。
- **AC-32 与设计稿的一致性 + 降级清单（v12 新增）**
  - 命令：`ls docs/shots/compare/`（或等价目录）
  - 期望：**≥4 组并排对照图**（设计稿截图 vs 实现截图，覆盖：列表页亮色、列表页暗色、编辑器、移动端），
    分组命名清晰；且 `PROGRESS.md` 含「**降级清单**」一节（每条：设计稿的点 → 实际做法 → 原因），**无降级则写明"无"**。


- **AC-33b 变量对话框 + 自动记忆（v14 新增）**
  - 命令（运行时）：选中含 `{{变量}}` 的条目 → 点 `pm-copy-<id>` → 断言 `[data-testid="pm-vars-dialog"]` 出现；
    **填其中一个变量（另一个留空）** → 断言 `[data-testid="pm-vars-preview"]` 的文本**随输入实时变化**，
    且**已填的变量被替换、未填的变量保持 `{{name}}` 原文**（v15：**不得**变成空串）；
    点「复制结果」→ 剪贴板内容与预览区文本**逐字符一致**（其中含未填变量的 `{{name}}` 原文）。
  - 自动记忆：关闭对话框后再次点复制 → **断言输入框已预填上次的值**（值持久化在 localStorage 即可）。
- **AC-33c 详情操作条（v14 新增）**
  - 命令：选中条目后断言 `[data-testid="pm-detail-actions"]` 存在，且其中**主按钮为复制**（按钮文案含"复制"、可见且可点）；
    并断言「版本历史」「删除」同为该条内的按钮（只断言存在，不触发删除）。
- **AC-33 一键复制可用且内容正确（v13 新增，最高优先级）**
  - 命令（运行时，服务自起自停 + CDP；实现方与我方同一条）：① 在使用视图找到夹具条目 → `Browser.grantPermissions` 授予剪贴板读权限；
    ② 点击 `[data-testid="pm-copy-<id>"]`；③ `navigator.clipboard.readText()` 读出内容；
    ④ 与 `GET /api/prompts/<id>` 的 `user_prompt` **逐字符比对** → 期望 `MATCH`。
  - 期望：复制内容逐字符一致；页面上出现 `已复制` 提示；`GET /api/usage/summary` 的 `by_channel.session` **计数 +1**（复制＝取用）。
- **AC-34 使用 / 管理 分离（v13 新增）——【自 v17 起作废，勿再执行】管理页已取消（FR-43），本条的「模式开关/模式记忆/使用视图内 delete=0」判定全部失效**
  - 命令：① `[data-testid="pm-mode-use"]`、`[data-testid="pm-mode-manage"]` 均存在；
    ② 首次进入（清 localStorage）默认落在**使用**视图 → `[data-testid="pm-use-grid"]` 存在；且 `[data-testid="pm-kpi-row"]` **不在使用视图内**；
    ③ 切到管理 → `[data-testid="pm-kpi-row"]` 出现；
    ④ 切回使用并刷新页面 → 仍是使用视图（`localStorage` 记忆生效）；
    ⑤ 使用视图内 `[data-testid^="pm-delete-"]` 计数 = **0**（管理操作不占位）。
- **AC-35 移动端一键复制（v13 新增）**
  - 命令：390×844 下 `getBoundingClientRect()` 断言 `[data-testid="pm-copy-<id>"]` **可见**（`offsetParent !== null`）且
    **宽高均 ≥ 44**；默认视图为使用视图。
- **AC-36 快捷与详情（v13 新增）**
  - 命令：① 按 `/` → `document.activeElement` 是 `[data-testid="pm-search-input"]`；② 双击 `[data-testid="pm-use-card"]` → `[data-testid="pm-detail"]` 出现；
    ③ 按 `Esc` → `pm-detail` 消失；④ 详情内存在常驻复制按钮（`pm-copy-<id>` 或等价 testid）。


- **AC-37 使用视图"纯净度"（v16 新增，文本级断言）**
  - 命令（运行时，服务自起自停 + CDP；在使用视图下取 `document.body.innerText`）：
    断言下列串**命中数均为 0**：`DATA_DIR`、`/api/`、`SQLite`、`监听`、`本次查询`、`筛选命中`、`使用统计`、`API 令牌`、`导入`、`导出`、`渲染不写库`、`version_no`，
    以及**内部 id 形态** `/#\d+/`（卡片上的 `#1`）。
  - 期望：上述全部 0 命中（原样贴输出）。
- **AC-38 导航归位（v16 新增，结构断言）**
  - 命令：① 使用视图下 `[data-testid="pm-topnav"]` 的可见文本中**不得出现**：`使用统计`、`API 令牌`、`导入`、`导出`、`新建`；
    ② 切到管理视图后，上述文本**必须都能找到**；
    ③ 使用视图 `[data-testid="pm-sidebar"]` 内**不得出现** `新建`、`重命名`、`删除`（筛选器可以点，但不得提供管理按钮）。
- **AC-39 反馈与安全提示未被误删（v16 新增）**
  - 命令：复制一条 → 断言 `已复制` 出现；含变量条目复制并在未填完时 → 断言 `未填` 提示出现；
    导入对话框切 `replace` → 断言**清空警示与二次确认仍在**（`pm-vars-dialog` 无关，指导入的确认）。
  - 期望：三项都在（**"去提示"不得波及操作反馈与危险确认**）。
- **AC-40 技术信息有落点（v16 新增）**
  - 命令：切管理视图 → 点击 `[data-testid="pm-settings"]` → 断言 `[data-testid="pm-about"]` 出现，且其中**能找到**数据文件路径与备份说明（断言含 `pm.db` 与 `备份`）。
  - 期望：技术信息**被移走而不是丢失**。


- **AC-41 无管理页 + 顶栏精简（v17 新增；**修订并取代 AC-38**；**AC-34 自 v17 起作废**）**
  - 命令（运行时）：① `[data-testid="pm-mode-use"]` 与 `[data-testid="pm-mode-manage"]` **均不存在**；
    ② 顶栏可见文本**含** `新建` 与 `更多`（或 `⋯`），且**含** 主题图标按钮 `[data-testid="pm-theme-toggle"]`；
    ③ 顶栏可见文本**不含** `使用统计`、`API 令牌`、`导入`、`导出`（它们在 `⋯更多` 菜单里）；
    ④ **表格视图**（`pm-view-table` 或切换"表格"后）行内出现 `编辑` 与 `删除`（删除按钮 `pm-delete-*` 存在）；
    ⑤ 卡片视图下 `pm-delete-*` **计数为 0**（**v19 修订**：原文写「删除只在表格里」，现放宽为「表格行内 + 详情面」——见 FR-46 / D-22）。
- **AC-42 主题图标切换（v17 新增）**
  - 命令：① 清空 localStorage 后进入 → 断言当前为**跟随系统**（`document.documentElement.style.colorScheme` 为空或 `light dark`，
    且按钮 `title`/状态体现"跟随系统"）；② 点一次 `pm-theme-toggle` → 变为**亮**（`colorScheme=light`）；
    ③ 再点 → **暗**（`colorScheme=dark`）；④ 再点 → 回到**跟随系统**；⑤ 刷新后保持上次选择（localStorage 记忆）。
- **AC-43 新建未保存不得入库（v17 新增，bug 回归断言）**
  - 命令：① 记下 `GET /api/prompts` 的 `total`；
    ② 点 `＋新建` → 等 2 秒 → 断言 `total` **不变**（这是本 bug 的核心判据）；
    ③ 在标题框输入文字但**不点保存** → 断言 `total` 仍**不变**；④ `Esc` / 取消关闭 → 断言 `total` **不变**；
    ⑤ 重新新建并**点保存** → 断言 `total` **+1** 且新条目标题等于输入值。
  - 期望：①–⑤ 全过；并附 `PROGRESS.md` 原样输出（含修复前的复现记录与本轮实测）。

- **AC-44 分栏视图存在且为默认（v19 新增）**
  - 命令（运行时）：① 清空 `localStorage` 后进入 → 断言 `pm-view-split` 存在，且**同屏可见** `pm-split-list` 与 `pm-detail`
    （两者 `getBoundingClientRect()` 的 `width`/`height` > 0，且**左右不重叠**：列表右边界 ≤ 详情左边界 + 容差）；
    ② `pm-split-item` 数 ≥1，且有**恰好一个**处于选中态；
    ③ 点第 2 个条目 → 右栏 `pm-detail` 的标题文本**随之改变**（等于该条目标题）；
    ④ 切到「表格」→ 再切回「分栏」→ 分栏结构恢复（`pm-split-list` + `pm-detail` 同屏）。
  - 期望：①–④ 全过。
- **AC-45 视图开关档位与「列表」档删除（v19 新增）**
  - 命令：① **[v45 修正] 档位顺序按断点断言** —— **桌面（≥768px）** `[data-testid="pm-use-viewmode"]` 内档位文本**依次**为
    `分栏`、`表格`、`卡片`；**移动端（<768px）依次为 `卡片`、`表格`、`分栏`**（两者都是顺序断言，不是集合断言）；
    ② **[v45 修正] 默认档位按断点** —— **桌面**清空 `localStorage` 后默认选中 `分栏` 且 `localStorage['pm-view-mode'] === 'split'`；
    **移动端**清空后默认选中 **`卡片`** 且 `localStorage['pm-view-mode'] === 'card'`；
    ③ 切到「表格」→ 刷新 → 仍落在表格（记忆生效）；
    ④ **不存在** `pm-view-list`；源码 `grep -rc 'pm-view-list' web/src` → `0`；
    ⑤ 手工把 `localStorage['pm-view-mode']` 写成旧值 `list` → 刷新 → 落回**分栏**且控制台**无报错**、页面不空白。
  - 期望：①–⑤ 全过。
- **AC-46 分栏右栏复用详情面能力（v19 新增；回归 FR-41e / AC-33b）**
  - 命令：① 在分栏右栏选中一条**带 ≥2 个变量**的条目 → 只填其中一个 → 断言预览里另一个变量**原样保留** `{{名称}}`，
    且点 `pm-detail-copy` 后**剪贴板内容与预览逐字符一致**（含未填占位符）；
    ② 断言右栏内存在且可点：`pm-detail-copy`、`pm-detail-version-jump`、`pm-detail-delete`、`pm-detail-edit`、`pm-detail-fullscreen`；
    ③ 点 `pm-detail-delete` → 出现**二次确认**，取消后该条目仍在（`GET /api/prompts` 的 `total` 不变）。
  - 期望：①–③ 全过。

- **AC-47 顶栏四块与顺序 + 无用户信息（v20 新增）**
  - 命令：① 取 `[data-testid="pm-topnav"]` 内四个控件的 `getBoundingClientRect().left`，断言顺序为
    `header-new` < `header-more` < `pm-theme-toggle` < `header-logout`（**按 x 坐标升序**，不是集合比较）；
    ② 断言 `pm-topnav` 的 `innerText` **不含** `admin`（用户信息已移入 `⋯更多`）；
    ③ 断言四者均**可见**且可点（`width>0` 且 `offsetParent!==null`）。
  - 期望：①–③ 全过。
- **AC-48 「⋯更多」子项顺序与删减（v20 新增）**
  - 命令：① 打开 `header-more` → 取菜单项文本数组，断言**逐项相等**于
    `['使用统计','API 令牌','导入 / 导出','关于','已登录：<用户名>','登出']`（前后缀差异允许，但**顺序必须一致**且每项都要在）；
    ② 断言菜单内**不存在** `文件夹与标签`（或 `文件夹` / `标签管理` 字样）；
    ③ 断言 `pm-settings` 元素存在且其文本为 `关于`（AC-40 仍按该 testid 定位）；
    ④ 断言「已登录：<用户名>」项**不可点**（无 role=menuitem 或无点击响应）。
  - 期望：①–④ 全过。
- **AC-49 文件夹区新建与层级（v20 新增）**
  - 命令（夹具：≥2 层文件夹，如 `Agent管理 / 会话管理`）：① `folder-create` 存在且可见，点击 → 新建 `AC49 新文件夹` → 断言界面出现该名且 `GET /api/folders` 数量 +1；
    ② 断言有子项的父文件夹存在展开三角，点击**折叠**后其子项**不可见**（`offsetParent===null`），再点展开恢复；
    ③ 断言子项行的 `left` 比父行大 **≥12px**（层级缩进）；
    ④ 断言文件夹行右侧有该文件夹的**条目数**，且该处**没有** `.ant-badge` 彩色计数徽标；
    ⑤ 悬浮一个文件夹行 → `pm-folder-rename` / `pm-folder-delete` 出现；点 `pm-folder-delete` → 出现**二次确认** → 取消后该文件夹仍在。
  - 期望：①–⑤ 全过。
- **AC-50 标签胶囊云（v20 新增）**
  - 命令（夹具：≥6 个标签）：① `pm-tag-cloud` 容器存在，`pm-tag-chip` 数 = 标签总数；
    ② 断言每个胶囊的文本**以 `#` 开头**；
    ③ 断言标签区内的 `.ant-badge`（计数徽标）数量 = **0**；
    ④ 断言胶囊的 `offsetTop` 至少有 **2 个不同值**（确实流式换行），且容器 `scrollWidth <= clientWidth + 2`（无横向滚动）；
    ⑤ 点一个胶囊 → 列表条数减少（筛选生效）；再点 → 恢复。
  - 期望：①–⑤ 全过。

- **AC-51 Logo 更名与回主页（v21 新增）**
  - 命令：① 断言 `pm-topnav` 文本**含** `PromptManager` 且**不含** `promptmanager`（全小写）；
    ② 取 logo 元素（品牌文字的可点祖先）→ 断言 `getComputedStyle(el).cursor === 'pointer'` 且 `role=button` 或 `<a>`；
    ③ 先制造"非主页状态"（切到「表格」视图 + 搜索框输入 `zzz` + 选中一个标签筛选）→ **真鼠标点击 logo** →
       断言：视图回到「分栏」（`localStorage['pm-view-mode']==='split'` 且 `pm-view-split` 存在）、搜索框 `value===''`、无标签筛选高亮、`pm-split-item` 数恢复为全部。
  - 期望：①–③ 全过。
- **AC-52 「关于」页重构（v21 新增）**
  - 命令：① 打开 `⋯更多` → 点「关于」→ 取 `pm-about` 内文本；
    ② 断言**不含** `BRIEF`、`AC-`、`阶段` 字样；
    ③ 断言 `Descriptions`/分区**没有重复 label**（取所有 label 文本，断言无重复）；
    ④ 断言可见分区数 ≥3（标题/分隔线/折叠标题计数）；
    ⑤ 窄屏（390×844）下 `scrollWidth <= clientWidth + 2`（不横向滚动）。
  - 期望：①–⑤ 全过。
- **AC-53 删除「拿来就用」装饰块（v21 新增）**
  - 命令：① 断言 DOM 中**不存在**文本等于 `拿来就用` 的元素；② 记录修改前后「列表工具栏顶部 y」与「页面顶部 y」的差（或列表首行 y - 顶栏底 y），断言**减少 ≥30px**。
  - 期望：①–② 全过（并附前后截图）。
- **AC-54 FIX：非空文件夹删除不得静默失败（v21 新增，P0）**
  - 命令（**必须用真鼠标事件** `Input.dispatchMouseEvent`，不得用 JS `.click()`）：
    ① 夹具：父文件夹（含 1 个子文件夹）；hover 父行 → 点 `pm-folder-delete` → 弹窗出现；
    ② 鼠标**移到弹窗上** → 断言弹窗仍在且**可见**、按钮仍可点（未因离开行而关闭/隐藏）；
    ③ 点「删除」→ 断言出现**用户可见的错误反馈**（`.ant-message` / 弹窗内 Alert / 按钮 disabled+原因文案之一）且 `GET /api/folders` 数量**不变**；
    ④ 空文件夹路径回归：hover → 删除 → 真鼠标点确认 → 断言 `GET /api/folders` 数量 **-1**；
    ⑤ 截图留证（弹窗打开态 + 错误反馈态）。
  - 期望：①–⑤ 全过。
- **AC-55 编辑页返回详情（v21 新增）**
  - 命令：① 在分栏选中一条 → 详情栏点 `pm-detail-edit` → 进入编辑态；
    ② 断言编辑态顶部存在文本含 `返回` 的按钮（或面包屑可点项）；
    ③ 真鼠标点击它 → 断言 `pm-detail` 重新出现且其标题等于该条目标题。
  - 期望：①–③ 全过。
- **AC-56 编辑页右栏模块顺序（v21 新增）**
  - 命令：进入编辑态 → 取「Markdown 预览」「变量填值」「版本历史」三个容器的 `getBoundingClientRect().top` → 断言**严格升序**（预览 < 变量 < 版本）。
  - 期望：顺序正确且附截图。
- **AC-57 收藏入口显性化（v21 新增）**
  - 命令：① 分栏 / 卡片 / **表格** / 详情栏 四处分别断言存在收藏控件，且元素为 `button` 或 `role=button`、`cursor=pointer`、带 `aria-label`（收藏/取消收藏）、热区 ≥24×24、**常驻可见**（不 hover 也可见）；
    ② 用**真鼠标**点表格行内星标 → 断言 `GET /api/prompts` 中该条 `favorite` **翻转**；再点一次 → 恢复；
    ③ 断言刷新后状态保持。
  - 期望：①–③ 全过。
- **AC-58 主题「跟随系统」图标（v21 新增）**
  - 命令：① 把主题切到「跟随系统」→ 断言 `pm-theme-toggle` 内**不含** `anticon-desktop`；
    ② 断言其内**同时**含 `anticon-sun` 与 `anticon-moon`（若采用自选图标，需在 PROGRESS 说明并断言该 icon 存在）；
    ③ 切到「亮」→ 仅 sun；切到「暗」→ 仅 moon。
  - 期望：①–③ 全过。

- **AC-59 品牌图形全站统一（v22 新增）**
  - 命令：① `curl -sI /favicon.ico` 与 `/favicon.svg` → **200** 且 Content-Type 正确（ico=`image/vnd.microsoft.icon` 或 `image/x-icon`，svg=`image/svg+xml`）；
    ② `index.html` 含 FR-59 的四条 link/meta，且 `<title>` **严格等于** `PromptManager`；
    ③ 顶栏 mark 存在且渲染尺寸 **26×26**（`getBoundingClientRect`），**亮色与暗色主题各截一张图**均清晰（不糊、与背景有对比）；
    ④ 三处装饰位存在且尺寸正确（**登录页 96 / 空态 72 / 关于页 48**），均 `aria-hidden="true"`；**真鼠标点击无任何动作**；空态在有数据时**不出现**；
    ⑤ `grep -rn "folder-art\|source-1254" web/` → **0**；页面引用图单张 ≤250KB。
  - 期望：①–⑤ 全过（附截图与命令原样输出）。

- **AC-60 注释/文案漂移清理（v23 新增）**
  - 命令：① `grep -rn "文件夹与标签" web/src` → **0**（注释已对齐现状）；
    ② `grep -rn "使用视图\|管理视图" web/src/styles/app.css` → **0**（旧术语已统一为「分栏 / 表格 / 卡片」等现行说法）；
    ③ `grep -rn "不是 promptmanager" web/src` → **0**，且 `grep -c "app: 'promptmanager'" web/src/pure.ts` → **仍为 1**（契约值未动）；
    ④ 全仓扫已废止概念（`pm-mode-use` / `pm-mode-manage` / `模式记忆` / `列表视图` / `pm-view-list` / `pm-kpi-row` / `pm-statusbar` / `管理视图` / `使用视图` / 菜单名「设置」）
       在 `web/src`、`src` 中的**注释与用户可见文案**命中数 = **0**（`BRIEF/PROGRESS/VERIFY/QUESTIONS` 等历史文档不计入，白名单见 FR-60）；
    ⑤ 全量回归通过（本项不得改任何行为）。
- **AC-61 构建体积（v23 新增）**
  - 命令：① `npm run build` 输出中**最大 chunk ≤ 500 KB**（未压缩；贴构建输出的 chunk 表）；
    ② 贴 `dist/web/index.html` 引用的首屏入口 chunk 体积；
    ③ 断言**无新增外链**：`grep -rnE '(https?://|cdn|unpkg|jsdelivr)' web/index.html dist/web/index.html` → 只允许本地相对路径；
    ④ 全量回归通过（懒加载不得造成功能丢失：编辑器 / Markdown 预览 / 版本 diff / 导入导出 / 使用统计 / 令牌 六处**逐一点开自证**并截图）。

- **AC-62 编辑器全屏（v24 新增，需用真鼠标 + 真实尺寸测量）**
  - 命令：① 打开一条 prompt 的编辑器 → 断言 `editor-fullscreen` 存在、可见、可点；
    ② **真鼠标点击**该按钮 → 断言：编辑器容器 `data-fullscreen="true"`（或 `document.fullscreenElement` 非空），
       且 `pm-topnav` / `pm-sidebar` / `pm-split-list` 三者 `offsetParent === null`（外壳已隐藏），编辑器宽度 ≥ `0.95 × 视口宽`；
    ③ **大内容测量**：往正文贴入 ≥ 5000 字（可用脚本直接 set value + 触发 input）→ 断言编辑区高度 ≥ `0.7 × 视口高`；
       断言编辑区 `scrollHeight > clientHeight`（确实可滚）且**滚动编辑区后 `window.scrollY` 不变**（互不抢滚动）；
       断言 `document.documentElement.scrollWidth <= clientWidth + 2`（无横向滚动）；
    ④ **退出路径**：按 `Esc` → 外壳三者恢复可见、`data-fullscreen` 移除、**正文内容与标题未被清空**（未保存内容仍在）；
       再点按钮进全屏 → **再点按钮**退出（按钮是切换态）；
    ⑤ 断言详情面的 `pm-detail-fullscreen` 仍然存在且可用（两者互不顶替）。
  - 期望：①–⑤ 全过（每步截图；②③ 的尺寸用 `getBoundingClientRect` 原样贴数值）。
  - **【v25 修订】本条"浏览器全屏"的形态要求已被 AC-63 取代（此处仅存历史）。**

- **AC-63 编辑器全屏（v25 修订版，取代 AC-62 的形态要求）**
  - 命令（**真鼠标**）：① 断言 `document.fullscreenElement === null`（**不得**进入浏览器全屏）；
    ② 真鼠标点 `editor-fullscreen` → 断言 `[data-testid="editor-list"]` 的 `offsetParent === null`（内部左栏已隐藏）、
       **编辑栏宽 / 右栏宽 ∈ [0.95, 1.05]**（平均分配，贴真实像素值）、`document.fullscreenElement` 仍为 `null`；
    ③ 按钮形态与详情页一致：`icon` 为 `anticon-compress`、文案「退出全屏」；
    ④ 按 `Esc` → 左栏恢复可见、两栏宽度回到原布局、**编辑器正文未丢**、未触发返回；
    ⑤ 再点进入 → 再点退出（双向切换）；⑥ 详情面 `pm-detail-fullscreen` 仍在且可用。
  - 期望：①–⑥ 全过（每步截图 + 贴宽度像素值）。
- **AC-64 版本 diff 不再把「未改动的备注」显示成变更（v25 新增）**
  - 命令：① 造两版：v1 备注 `N`、v2 备注 `N\n`（仅尾部换行不同，其余字段相同）→ 调 `GET /api/prompts/:id/diff?from=1&to=2` →
    断言 diff **不含** `[notes]` 段的 `-`/`+` 行（不得出现"相同文本被判变更"）；
    ② 反例（必须仍能工作）：v2 备注改成 `N2` → diff **必须**出现 `-N` / `+N2`；
    ③ 单测通过（`npm test` 中新增的该用例）；④ 界面自查：打开有 2+ 版本的 prompt → 「对比版本」diff 区**不出现** notes 假变更（截图）。
  - 期望：①–④ 全过。
- **AC-65 复制在内网 HTTP 下可用（v25 新增，必须用内网 IP 访问）**
  - 命令：① 用**内网 IP** 打开（如 `http://192.168.0.228:<端口>`）→ 断言 `window.isSecureContext === false` 且 `navigator.clipboard === undefined`（用户真实环境）；
    ② 在该环境下**真鼠标**点详情面复制入口（`pm-detail-copy` 与 变量面板里的「复制」）→ 断言**不出现**「浏览器拒绝了剪贴板访问」提示，且剪贴板内容与源文本一致（读回校验）；
    ③ 全仓 `grep -rn "navigator.clipboard" web/src` → **只允许 1 处**（`use-copy.ts`）；④ 单测：`navigator.clipboard` 缺失时走 `execCommand` 且成功。
  - 期望：①–④ 全过（**不得**只用 `127.0.0.1` 验收）。

- **AC-66 详情页头部去冗余（v27 新增，R-1）**
  - 命令：① 分栏视图选中一条 → 断言 `[data-testid="pm-detail"]` 内**不存在**文本 `当前字段`，且该处**没有** Select 与「预览」说明标签；
    ② 断言详情面**仍有**：字段页签（用户提示词/系统提示词/备注）、`预览/源码` 切换、`pm-detail-plain`（显示纯文本）、`pm-detail-fullscreen`；
    ③ 量出「页签行底部 → 正文区顶部」的间距，断言比修复前**减少 ≥ 30px**（贴前后数值）；
    ④ **编辑器页不受影响**：进入编辑器 → 断言 Markdown 预览里**仍有 3 选项下拉**（用户提示词/系统提示词/备注），切换后预览内容随之变化。
  - 期望：①–④ 全过（附截图）。
- **AC-67 ⋯更多「修改密码」（v27 新增；**必须用内网 IP 访问**）**
  - 命令：① 打开 `⋯更多` → 菜单项文本**逐项相等**于 `['使用统计','API 令牌','导入 / 导出','关于','修改密码','登出']`，且**不含**「已登录」；
    ② **真鼠标**点「修改密码」→ 弹窗出现（`pm-password-modal`），含三个密码框（当前/新/确认）；
    ③ **负例**：当前密码填错 + 合规新密码 → 提交 → 断言出现明确错误提示、**密码未变**（用旧密码仍能 `POST /api/login` 200）；连续错 5 次后第 6 次 → **429**；
    ④ **表单校验**：两次新密码不一致 → 前端拦截（不发起请求，贴网络请求数或提示文案）；新密码 < 8 字符 → 拒绝并说明规则；
    ⑤ **正例（真交互）**：填对当前密码 + 合规新密码（两端一致）→ 提交 → 断言弹窗关闭 + 成功提示；
       **用新密码 `POST /api/login` → 200**，**旧密码 → 401**（curl 原样贴）；
       **当前会话仍可用**（同一 cookie 访问 `/api/me` → 200）；**另一个会话被吊销**（用第二份 cookie 断言 401）；
    ⑥ **密码不回显**：`grep` 服务端日志无明文新/旧密码；响应体不含 `old_password`/`new_password` 字段回显；
    ⑦ 关于页「维护」区含界面改密码的说明；⑧ 回归：当前密码错误返回 **400 而非 401**（不得把人踢回登录页）。
  - 期望：①–⑧ 全过（附截图 + 命令原样输出；**验收后把密码改回原值并留痕**）。

- **AC-68 备注=纯文本（v28 新增）**
  - 命令：① 编辑器页 → Markdown 预览的字段下拉选项**恰好**为 `['用户提示词','系统提示词']`（**不含**备注）；
    ② 造一条备注含 Markdown 语法的夹具（如 `# 标题` + `**粗体**` + `- 列表` + `[链接](http://x)`）→
       详情面选「备注」页签 → 断言：正文区文本**原样包含** `# 标题` / `**粗体**` / `[链接](http://x)`，
       且该区域 DOM **不存在** `h1/h2/h3/strong/ul/a` 等渲染元素（`querySelectorAll` 计数为 0）；
    ③ **网络层断言**：从选「备注」页签到显示完成，**未发出** `POST /api/render/markdown`（CDP 记录请求，计数 0）；
    ④ 回归：切到「用户提示词」→ Markdown 渲染**仍正常**（DOM 出现渲染元素 / 或 `markdown-preview` 有内容）。
  - 期望：①–④ 全过（附截图与网络层输出）。
- **AC-69 详情页标题下备注行（v28 新增）**
  - 命令：① 选中一条**有备注**的条目 → 断言 `[data-testid="pm-detail-notes"]` 存在，其 `innerText` **等于**该条 `notes`（纯文本）；
    ② 样式断言（`getComputedStyle`）：字号 **≤14px**、颜色为次级/三级灰（与标题 color 不同且有明显层级）、行高 ≥1.5；
       并量出**标题行底部 → 备注行顶部**的间距，断言 **≤10px**（贴真实像素）；
    ③ 位置断言：备注行的 `getBoundingClientRect().top` **介于**标题行之下、`pm-detail-fields`（页签行）之上；
    ④ **空备注**：选一条 `notes` 为空的条目 → 断言 `pm-detail-notes` **不存在**（不占位）；
    ⑤ **长备注**：造一条 ≥200 字备注 → 断言渲染高度**不超过 2 行**（`scrollHeight` 与 2 行高度相当 / 或 `-webkit-line-clamp` 生效），
       且元素 `title` 属性包含完整备注文本；
    ⑥ 截图三态：有备注 / 无备注 / 长备注（亮色 + 暗色各一张）。
  - 期望：①–⑥ 全过。

- **AC-70 拖拽排序（v29 新增；用**真鼠标**拖拽，含持久化与排版回归）**
  - 命令：① 卡片视图：hover 条目 → 断言出现拖拽手柄（`data-testid="pm-drag-handle-<id>"` 或等价，热区 ≥24×24）；
    ② **真鼠标**把第 1 条拖到第 2 条之后（`mousePressed → 多次 mouseMoved → mouseReleased`）→ 断言 DOM 顺序变化（贴前后顺序）；
    ③ **刷新页面** → 断言顺序**保持**（持久化）；且排序下拉显示「**自定义**」；
    ④ **排版回归**：拖拽结束后，**未被拖动**条目的 `left/width/top/height` 与拖拽前**差 ≤1px**（贴数值对照）；拖动前后**行高不变**；
       拖动全程 `document.documentElement.scrollWidth <= clientWidth + 2`（无横向溢出）；
    ⑤ 左栏文件夹树：同层级拖拽重排 → 顺序变化 + 刷新保持（`PATCH /api/folders/order` 或等价）；
    ⑥ 接口负例：`PATCH /api/prompts/order` 传**不存在的 id** / **重复 id** → **400**（不得静默成功或 500）；
  - 期望：①–⑥ 全过（每步截图 + 贴真实像素与顺序）。
- **AC-71 分栏中栏条目精简 + 宽度 -8%（v29 新增）**
  - 命令：① 中栏条目文本断言：**含**标题与备注；**不含** `v\d`、`取用`、`更新于`、文件夹名、`变量` 等元信息（贴正则命中结果，期望 0）；
       且**不含** `user_prompt` 摘要（用一条"正文与备注明显不同"的夹具，断言条目文本里**不出现正文片段**）；
    ② **固定两行**：备注元素 `getBoundingClientRect().height ≈ 2 × lineHeight`（贴数值），`-webkit-line-clamp: 2` 生效；
       **备注为空**的条目：其条目高度与有备注条目**相同**（贴两条的高度，期望相等）；
    ③ **宽度**：贴改前/改后中栏真实像素，断言 **新宽/旧宽 ∈ [0.90, 0.94]**（≈ -8%）；右栏相应变宽；
    ④ 单击条目仍能切换右栏（贴切换前后详情标题）；滚动行为不变；
    ⑤ 横向滚动断言：`scrollWidth <= clientWidth + 2`；⑥ **卡片/表格视图不受影响**（贴两视图条目文本片段与改动前一致的特征，如卡片仍有正文摘要）。
  - 期望：①–⑥ 全过（附截图：分栏（有备注/无备注）、拖动中/拖动后）。
- **AC-72 文件夹筛选含子目录 + 与计数一致（v30 新增）**
  - 命令：① 造三层夹具 `A > B > C`，在 **A / B / C 各放 1 条** prompt，另放 1 条**未归类**；
    ② `GET /api/prompts?folder_id=A` → 断言 `total = 3`，且 items 的 id 集合 = {A、B、C 三条}（贴 id 列表）；
    ③ `?folder_id=B` → `total = 2`；④ `?folder_id=C` → `total = 1`；
    ⑤ **与侧栏计数一致**：UI 上文件夹 A 的计数徽标文本 = `3`（贴 DOM 文本），点击 A 后列表条目数 = `3`（贴 DOM 条目数）；
    ⑥ 组合筛选：只有 B 里的条目带标签 `T` → `?folder_id=A&tag=T` → `total = 1`；`?folder_id=A&q=<B 内正文关键词>` → `total = 1`；
    ⑦ 分页：`?folder_id=A&limit=2` → items 2 条、`total = 3`；`offset=2` → 第 3 条；
    ⑧ 负例：`?folder_id=<不存在 id>` → 200 且 `total = 0`（**不得 500**）；`?folder_id=abc` → 400；
    ⑨ 拖拽收窄：在 A 的视图里，把 B 的条目拖到 C 的条目位置 → **顺序不变**（忽略）且**未发出** `PATCH /api/prompts/order`（贴网络记录计数 0）。
  - 期望：①–⑨ 全过（贴 total / id 集合 / DOM 文本 / 网络记录）。
- **AC-73 卡片底部行贴底（v30 新增）**
  - 命令：① 造夹具：同一网格行内**至少 3 张**卡片 —— **无标签** ×1、**1 个标签** ×1、**3 个标签** ×1，标题长度不同；
    ② 断言三张卡片 `getBoundingClientRect().height` **相等**（两两差 ≤1px，贴三个数值）；
    ③ 断言各卡片「元信息 + 复制」行的 `bottom` 与卡片 `bottom` 之差 **相等且 ∈ [13,15]**（贴三个数值，期望 ≈14px = 卡片内边距）；
    ④ 断言该行底边**与卡片底边对齐**（`cardBottom - rowBottom ≤ 1px` 的卡片 ≥ 3/3）；
    ⑤ **无标签卡片**内标签区高度 = 0（或与有标签卡片等高，贴实际数值 + 实现说明），且**不产生额外空隙**；
    ⑥ 回归：`scrollWidth <= clientWidth + 2`；网格列数改前 / 改后一致（贴数值）；AC-70 ④ 像素回归仍过。
  - 期望：①–⑥ 全过（截图：无标签 / 1 标签 / 3 标签各一张 + 亮暗各一张）。
- **AC-74 表格拖拽 + 刷新感（v30 新增；③ 为尽力项）**
  - 命令：① 表格视图 hover 行 → 出现拖拽手柄（testid 如 `pm-drag-row-<id>`，热区 ≥24×24）；**表头与列宽**改前 / 改后一致（贴列宽数值）；
    行高与改前一致（贴数值）；
    ② **真鼠标**把第 1 行拖到第 3 行位置 → DOM 行顺序变化（贴前后顺序）+ 排序档变「**自定义**」+ **刷新页面后顺序保持**；
    ③ **刷新感（仅当 FR-74 ② 做了）**：拖拽结束后 **500ms 内** `pm-view-table` 容器**未出现** loading 骨架 / 空白（贴 DOM 采样或前后截图），
       未动条目像素差 ≤1px；最终顺序与后端一致（刷新校验）；
    ④ 与卡片 / 分栏**共用同一接口**：拖拽一次只发 **1 次** `PATCH /api/prompts/order`（贴请求记录）；`package.json` **未新增依赖**（贴 diff 或空输出）；
    ⑤ 负例沿用 AC-70 ⑥：`PATCH /api/prompts/order` 传不存在 id / 重复 id → **400**；
    ⑥ 回归：卡片档与分栏档拖拽仍正常（各贴一次真鼠标拖动结果）。
  - 期望：①②④⑤⑥ 全过；③ 若实现方按 FR-74 的裁量**跳过**，须在 PROGRESS 给出理由与现状证据 —— **③ 跳过不计失败**。
- **AC-75 卡片拖拽在全部 / 父目录视图下生效（v31 新增；真鼠标 + 槽位保持）**
  - 命令：① 造夹具：**同一视图内至少 4 条分属 3 个不同目录**（如 2 条在 A、1 条在 B、1 条未归类）；
    ② 卡片视图（**全部视图**，不带 `folder_id`）hover 第 1 张 → 出现手柄（`pm-drag-card-<id>`，热区 ≥24×24）；
    ③ **真鼠标**把手柄从第 1 张拖到第 3 张位置 → 断言 **DOM 顺序变化**（贴前后顺序）+ **恰好 1 次** `PATCH /api/prompts/order` + 刷新后保持；
    ④ **槽位保持（关键新增）**：在**具体目录**视图（如 A）内拖拽 2 条 → 断言这 2 条的 `sort_order` **只在这 2 条原先占据的槽位间交换**，
       且**其他目录条目的 `sort_order` 完全不变**（贴前后全量 `sort_order` 对照）；再回「全部」视图断言这 2 条**没有跑到最前**；
    ⑤ **无静默忽略**：断言拖拽路径上不存在「顺序不变 + 无请求 + 无提示」的情形（贴网络记录与提示文本）；
    ⑥ 本体可拖（若做了）：拖卡片本体 → 顺序变化；且**单击仍选中、双击仍打开详情**（贴前后状态）；
    ⑦ 手感回归：未动项像素差 ≤1px、行高不变、`scrollWidth <= clientWidth + 2`；
    ⑧ 负例沿用 AC-70 ⑥（不存在 / 重复 / 空 ids → 400）；
    ⑨ 回归：分栏档与表格档拖拽仍正常（各贴一次真鼠标拖动）。
  - 期望：①②③④⑤⑦⑧⑨ 全过；⑥ 若按 FR-75 第 3 条的裁量**跳过**，须在 PROGRESS 写明理由 —— **⑥ 跳过不计失败**。
- **AC-76 顶栏品牌文字 = 「PromptM」，且其余四处保持全名（v32 新增）**
  - 命令：① 桌面视口（≥1024px）断言顶栏品牌区文字 **精确等于** `PromptM`（贴 `innerText`），且**不等于** `PromptManager`；
    ② **反例断言（防"顺手全局替换"）**：`web/index.html` 的 `<title>` 仍为 `PromptManager`（贴源码行 + 运行时 `document.title`）；
       登录页品牌文字仍为 `PromptManager`；关于页仍为 `PromptManager`；`pure.ts` 错误文案仍含 `PromptManager`（**四处源码行原样贴**）；
    ③ **契约值不动**：`grep -n "app: 'promptmanager'" web/src/pure.ts` 有命中（贴行）；
    ④ 图标 mark：`[data-testid="pm-brand-mark"]` 存在、26×26、`src` 未变（贴属性）；
    ⑤ 移动视口（390×844）顶栏**不显示**品牌文字（只有图标）；
    ⑥ 回归：AC-51（点 logo 回主页）仍过；AC-47（顶栏按钮 `left` 坐标升序）仍过；
    ⑦ 顶栏无横向溢出（`scrollWidth <= clientWidth + 2`）。
  - 期望：①–⑦ 全过（附桌面 / 移动截图各一张）。
- **AC-78 表格批量操作（v34 新增；真鼠标 + 请求计数）**
  - 命令：① 表格视图（≥6 条夹具）断言**首列复选框**存在（含**表头全选**）；② 勾选 3 条 → 断言工具条出现且文本含「已选择 **3** 项」
    与「批量收藏」「批量移动」「批量删除」「取消」（贴文本）；③ **取消** → 断言选中清空、工具条消失；
    ④ **批量收藏**：勾选 2 条 → 点「批量收藏」→ 断言这 2 条 `favorite=true`（贴接口/库查询）+ 反馈文本 + **只发 1 个请求**（贴网络记录）；
    ⑤ **批量移动**：勾选 2 条 → 选目标文件夹 → 断言这 2 条 `folder_id` 变为目标值（贴前后对照）+ 侧栏计数同步；
    ⑥ **批量删除**：勾选 2 条 → 点「批量删除」→ 断言**出现二次确认**（文本含将删除的条数与"不可恢复"）→ 先点**取消**（断言未删）→ 再确认（断言已删、total 减 2）；
    ⑦ **全选**：点表头复选框 → 断言当页全部选中；⑧ 回归：行内操作、行拖拽排序、分页/排序/筛选仍正常；
    ⑨ 负例：批量接口传不存在 id / 空 ids → **400**（若走批量接口）。
  - 期望：①–⑨ 全过（附截图：未选中 / 选中 3 条工具条 / 批量删除二次确认）。
- **AC-79 分栏详情页的「文件夹 + 标签」显示与修改（v34 新增）**
  - 命令：① 分栏视图选中一条 prompt → 断言右栏在**备注行之下、字段页签之上**出现元信息行，**含当前文件夹名**与**全部标签**（贴 DOM 文本）；
    ② **改文件夹**：下拉选另一个文件夹 → 断言落库 `folder_id` 变化（贴前后）+ 左栏计数同步 + 有成功反馈；
      再选「未归类」→ 断言 `folder_id=null`；③ **加标签**：点添加入口选/输入一个标签 → 断言落库 `tags` 增加（贴前后）；
    ④ **删标签**：点某标签的 `✕` → 断言落库 `tags` 减少；⑤ **一致性**：在详情页改完后，切到卡片/表格视图断言显示一致；
      再打开编辑器断言字段一致；⑥ 排版：元信息行字号**小于标题**、颜色为次级色；窄屏（<1200px）`scrollWidth <= clientWidth + 2`；
    ⑦ 截图：有文件夹+多标签 / 未归类+无标签 两态（亮暗各一张）。
  - 期望：①–⑦ 全过。
- **AC-80 分栏详情页字段页签只剩两个（v34 新增）**
  - 命令：① 分栏视图选中任一条 → 断言右栏字段页签**恰好 2 个**且文本 = 「用户提示词」「系统提示词」（贴 `.ant-tabs-tab` 文本列表）；
    ② 断言**不存在**文本为「备注」的页签（贴计数 0）；③ 回归：两个页签切换后内容正确渲染（Markdown 渲染仍生效）；
    ④ 编辑器里的「备注」输入框**仍在**（贴 DOM 证据）。
  - 期望：①–④ 全过。
- **AC-81 分栏详情页不再有「变量填值」区块（v34 新增）**
  - 命令：① 分栏视图选中一条**含变量**的 prompt → 断言右栏**不存在**「变量填值」区块（贴文本/DOM 计数 0）；
    ② 断言「预览」「版本历史」仍在（贴证据）；③ **能力未丢**：编辑器里的变量面板仍可用（贴证据）；
    ④ **复制含变量的 prompt 仍弹出填值对话框**（`VarsDialog` 出现，贴证据）。
  - 期望：①–④ 全过。
- **AC-82 「变量填值对话框」宽高加大（v34 新增；真实像素）**
  - 命令：① 先**确认**该弹窗 = 复制含变量 prompt 时弹出的 `VarsDialog`（贴触发步骤）；
    ② 量**改前/改后**的 `getBoundingClientRect()`（贴真实像素），断言**宽、高各增加 ≥15%**；
    ③ 断言弹窗内**无内容裁切/无横向溢出**（`scrollWidth <= clientWidth + 2`），变量输入区可见；
    ④ 断言在**分栏 / 表格 / 卡片**三档触发时尺寸一致（同一组件）；
    ⑤ 截图：改前/改后各一张（亮色）。
  - 期望：①–⑤ 全过。
- **AC-83 项目 `AGENTS.md`（v35 新增；AI 代理操作指南 —— 受众是"新会话/新代理"，不是产品介绍）**
  - 命令：① 文件存在于**项目根目录** `AGENTS.md` 且**已入库**（贴 `git ls-files AGENTS.md` + 行数）；
    ② **命令可跑（最重要）**：逐条执行文档「常用命令」一节里的命令 —— **全部 rc=0**（贴每条命令 + 原样输出；
       至少覆盖：装依赖 / 构建 / 跑测试 / 类型检查 / **本地质量检查 `tools/ci-check.sh`** / 迁移 / 启动 / 设管理员口令）；
    ③ **结构与实际一致**：文档「项目结构」一节点到的**每个路径都真实存在**（逐个 `test -e` 断言并贴结果）；
    ④ **无过期信息**：`grep` 断言不含已废弃/过时的表述（如「列表视图」、顶栏品牌名写成 `PromptManager`、
       旧体积 `1.25 MB`、旧用例数 `149` / `248`、`阶段 1–9` 之类），命中数 **0**（贴命令与结果）；
    ⑤ **含「本项目特有的坑」≥5 条**：从本项目历史（`PROGRESS.md` / `docs/dev-history/PROGRESS.md` / `VERIFY.md`）提炼，
       每条要**指向证据**（例如：Node 服务 unit 缺 `AF_NETLINK` 会启动即崩；`better-sqlite3` 需编译故容器 builder 必须带工具链；
       `node --test` 并发下子进程要 `detached` 否则 flaky；内网 HTTP 下 `navigator.clipboard` 不存在、验收必须用内网 IP；
       Markdown 渲染的 jsdom 单例占内存；`folder_id` 筛选含全部后代……）；
    ⑥ **不重复既有文档**：与 `BRIEF.md` / `README.md` / `docs/versioning.md` / `deploy/README.md` 重叠的内容用**指向**而非复制
       （贴 2–3 处示例，说明"这里为什么指向而不是抄"）；
    ⑦ **可上手性自检**：以"**一个没有任何上下文的新会话**"视角回答 —— 「我只有这份 AGENTS.md + 这个仓库，
       能不能在 5 分钟内把它跑起来、并知道该遵守什么？」把自检发现的缺口**就地补掉**，过程与结论写进 PROGRESS。
    ⑧ **必须全英文**：`AGENTS.md` **不得含任何 CJK 字符**（含中文标点）——
       判据：`grep -cP '[\x{4e00}-\x{9fff}\x{3000}-\x{303f}\x{ff00}-\x{ffef}]' AGENTS.md` → **0**
       ⚠️ **不要加 `LC_ALL=C`**（**v37 修正** —— 由实现方在阶段 28 实测发现并报告）：`LC_ALL=C` 会把 PCRE2 压进**字节模式**，
       此时 `\x{}` 的上限是 `0xFF` ⇒ 原命令**永远报错 rc=2**、拿不到 0；而"顺手修"成 `LC_ALL=C grep -cP '\p{Han}'` 会**假绿**
       （对**纯中文**文件实测也返回 0）。**正确判据** = 本行命令（去掉 `LC_ALL=C`、保留码点区间，含中文标点），
       或 `grep -cP '\p{Han}'`（**不含**中文标点）。
       （依据 `/root/greenhouse/STANDARDS.md` §5.1：**AI 代理指令文件一律英文**，便于 AI 识别与更新）。
  - 期望：①–⑧ 全过（附文件路径、行数、各步原样输出；⑧ 附 grep 命令与命中数）。
- **AC-84 表格批量 UI 细化（v38 新增；真鼠标 + 真实像素）**
  - 命令：① **indeterminate**：勾选 **3 行（非全选）** → 断言表头复选框为**半选态**
    （`[data-testid="…"] .ant-checkbox-indeterminate` 存在，或 `input[aria-checked="mixed"]`；贴 DOM/属性证据）；
    再点表头复选框**全选** → 断言变为**打勾**（`ant-checkbox-checked` 存在、`indeterminate` 消失）；再取消全选 → 断言**两者皆无**；
    ② **尺寸一致**：量表头复选框与行内复选框的 `getBoundingClientRect()` → 断言宽高**差 ≤1px**（贴数值）；
    ③ **工具条间距**：量「批量工具条底部」与「表头行顶部」的距离 → 断言 **≥6px**（贴像素）；
    ④ 回归：批量收藏/移动/删除（含二次确认）与"一次操作 1 个请求"仍成立（沿用 AC-78 的断言，贴关键行）；
    ⑤ 截图三态：未选 / 部分选（半选）/ 全选。
  - 期望：①–⑤ 全过。
- **AC-85 分栏详情页元信息行细化（v38 新增；含长内容健壮性）**
  - 命令：① **层级间距**：量「备注行底部 → 元信息行顶部」与「元信息行底部 → 字段页签行顶部」两个间距 →
    断言前者 **≥12px** 且**不大于**后者（贴两个数值）；
    ② **长内容不溢出**：造夹具（**长文件夹名**（≥20 字）+ **5 个标签**）→ 断言元信息行 `scrollWidth <= clientWidth + 2`，
    且「+ 添加标签」控件**仍在面板可视区内**（`getBoundingClientRect().right <= 面板 right + 1`；贴数值与截图）；
    ③ **chip 样式一致**：取详情页元信息行里某个标签 chip 与**左栏**同名标签 chip 的 `getComputedStyle`
    （`backgroundColor` / `border` / `borderRadius`）→ 断言三者**一致**（贴两处数值对比）；
    ④ 回归：改文件夹/加标签/删标签仍落库、三处（卡片/表格/编辑器）同源一致（沿用 AC-79 断言，贴关键行）；
    ⑤ 截图：常规（有文件夹+多标签）/ 长内容 / 亮暗。
  - 期望：①–⑤ 全过。
- **AC-86 `docs/` 只留最终状态（v39 新增）**
  - 命令：① `find docs -name '*.png' | wc -l` → **≤10**（关键展示图一套；**贴改前 172 → 改后 N**）；
    ② `find docs -type d -name 'stage*'` → **空**（阶段截图目录已不在 `docs/` 下）；
    ③ **归档可查**：`ls tmp/shots-archive/ | head`（贴输出）+ `git ls-files tmp | wc -l` → **0**（不入库）；
    ④ **工具约定已改**：贴 `tools/ui-shots.sh` 中"默认输出 `tmp/`、发版前才产一套到 `docs/shots/`"的注释/默认值；
    ⑤ **`AGENTS.md` 已写明**：贴英文段落（`docs/` 定位 + 截图分级 + 工具约定 + `tmp/` 不入库）；
    ⑥ `git ls-files docs | grep -c '\.png$'` → **≤10**（**贴改前 94 → 改后 N**）；
    ⑦ 回归：`npm test` 全绿（当前 **307**）+ `bash tools/ci-check.sh` 全绿；`git status` 干净。
  - 期望：①–⑦ 全过（附各步命令与原样输出）。
- **AC-87 表格标签间距（v41 新增；真实像素）**
  - 命令：① 造一条**含 3 个标签**的 prompt → 表格视图 → 断言该行「标签」列里相邻 `<Tag>` 之间的**水平间隙 ≥4px**
    （用 `getBoundingClientRect()` 量相邻两个 tag 的 `left - (prev.left + prev.width)`，贴三个间隙值）；
    ② 断言与**卡片视图**同一 prompt 的标签间隙**一致**（贴两处数值对比）；
    ③ **不溢出**：该单元格 `scrollWidth <= clientWidth + 2`，且**不撑破列宽**（贴列宽前后对照）；
    ④ **单标签 / 无标签**不受影响（贴证据）；
    ⑤ 截图：表格视图多标签行（亮色）。
  - 期望：①–⑤ 全过。
- **AC-88 版本保留策略：最多最近 10 个（v41 新增；**数据层**，直接查库）**
  - 命令：① **数据层**：建 1 条 prompt → **连续 PUT 更新 15 次**（每次改不同内容）→ 直接查库
    （`SELECT COUNT(*) FROM prompt_versions WHERE prompt_id=?`）→ 断言 **恰好 10**（贴 SQL 与输出）；
    ② **保留的是最近的**：查 `SELECT version_no FROM prompt_versions WHERE prompt_id=? ORDER BY version_no` →
    断言 = **最新的 10 个连号**（如 `[6..15]`），且**最旧的（1..5）已不存在**（贴列表）；
    ③ **当前版本在保留集内**：`prompts.version_no` ∈ 上述集合（贴数值）；
    ④ **不重编号**：保留行的 `version_no` 与裁剪前**逐字一致**（贴对照）；
    ⑤ **回滚**：回滚到一个**已存在**的版本 → 断言生成新版本且**仍 ≤10**；回滚到**已被裁剪**的版本 → **404**（贴两处证据）；
    ⑥ **导入**：构造一个含 **15 个版本**的导出文件 → `POST /api/import` → 断言该 prompt 最终 **≤10** 个版本（贴 SQL 计数）；
    ⑦ **边界**：恰好 10 个时**不删**；产生第 11 个时**删最旧的那个**（贴前后 `version_no` 列表）；
    ⑧ **文案**：版本面板里**存在**"最多保留最近 10 个版本"字样（贴 DOM 文本）；`README.md` 里也写明（贴行）；
    ⑨ 回归：`npm test` 全绿（当前 **307**，只增不减）+ `bash tools/ci-check.sh` 全绿；
       版本列表 / diff / 回滚 / 导出导入 的既有 AC 全部复跑通过。
  - 期望：①–⑨ 全过（数据层断言必须**直接查库**，不能只看界面）。
- **AC-89 CI 在干净环境通过（v42 新增；必须先删 dist）**
  - 命令：① **模拟 CI 的干净环境**：`rm -rf dist` → `bash tools/ci-check.sh` → 断言 **rc=0 且 6 项全绿**（贴原样输出）；
    ② 断言顺序正确：贴 `tools/ci-check.sh` 中「构建」与「typecheck:tests」的**行号先后关系**（构建必须在先）；
    ③ 复现实验对照：`rm -rf dist && npm run typecheck:tests` 的错误数（改前 38 → 改后 0，贴两次数值）；
    ④ **GitHub Actions 实跑**：推送后 Actions 的 `ci` workflow **通过**（贴 run 的结论截图或日志尾部）。
  - 期望：①–④ 全过。
- **AC-90 登录页简化（v42 新增；含"不暴露账号名"）**
  - 命令：① **无预填**：打开登录页（**未登录态**）→ 断言用户名输入框 `value === ''`（贴 DOM 值）；
    ② **不出现 admin**：断言登录页**源码与运行时 DOM** 均不含 `admin` 字样（`grep -c` 源码 → 0；贴运行时 `innerText`/`innerHTML` 检索 → 0）；
    ③ **只剩登录信息**：断言页面含 品牌图（`pm-brand-art-login`）、`PromptManager`、用户名/口令/登录按钮；
      且**不含**这四条（逐条 `grep` 命中 0）：`SELF-HOSTED`、`数据只在本机`、`网页不提供注册`、`未认证一律 401`；
    ④ **功能不回归**：真鼠标填用户名+口令 → 登录成功进主界面（贴前后）；错误口令 → 有可读错误提示；亮暗各一张截图；
      移动端（390×844）不横向溢出（`scrollWidth <= clientWidth + 2`）。
  - 期望：①–④ 全过。



- **AC-77 上线准备（v33 新增；分三批：文档整理 / 产物清理 / 质量与版本）**
  - 命令：① **文档整理**：`README.md` 与实际一致（逐条核：进度=**阶段 25**、测试用例数=实际、bundle 体积=实际、验证章节含**全部** `tools/ac-stage*.sh`、
    AC 编号范围正确、界面章节按现状〔分栏默认 + 三档 + 拖拽排序 + 品牌 `PromptM`〕）；
    `PROGRESS.md` 已归档到 `docs/dev-history/PROGRESS.md` 且根目录留有「当前状态 + 阶段索引」；`QUESTIONS.md` 清为模板（旧 Q-1 已归档）；
    `docs/dependencies.md` 与 `package.json`/lock **逐条一致**（含 `@dnd-kit/*`）；`docs/design/` 已归档；`deploy/README.md` 与实际一致；
    ② **产物清理**：`tmp/` **不存在**；`var/cache/` **不存在**；`docs/shots/{before,before-stage8,compare,evidence}` **不存在**；
    `docs/shots/stage18`~`stage25` 已归档到 `docs/dev-history/shots/`；**`docs/shots/*.png` 顶层 53 张仍在**；
    `git ls-files` **不含** `tmp/`、`dist/`、`data/`、`_env/`；
    ③ **质量与版本**：`.github/workflows/` 有 CI 配置且**本地可执行同一套检查**（给出命令并贴输出）；
    `package.json` 的 `version` = **`1.0.0`**；`/healthz` 返回的 version **从 `package.json` 单一来源读取**（贴证据，不得两处各写一份）；
    有 `CHANGELOG.md`（人可读）；**版本管理规划**写进文档（semver 规则 + tag 约定 + 何时升版本）；
    ④ **零代码改动证明**：`git diff` 在 `src/`、`web/src/`、`tests/`、`migrations/` **无改动**（`package.json` 只允许 `version` 字段变化）；
    `npm test` 仍全绿（当前 285/285）。
  - 期望：①–④ 全过（每步贴命令与原样输出）。















- **AC-91 Docker Hub 镜像构建推送（v43 新增）**
  - ① **YAML 静态断言**（无 Docker 也能跑）：`.github/workflows/docker.yml` 能被 YAML 解析；断言 **触发条件**（`tags: ['v*']` + `workflow_dispatch` + `branches: [main]`）、
    `permissions: contents: read`、五个官方 action 的 `uses` 都 pin 了大版本、镜像名引用 `secrets.DOCKERHUB_USERNAME`、
    登录引用 `secrets.DOCKERHUB_TOKEN`；**负向断言**：全文**不得**出现 `echo` + `secrets`、不得出现任何明文 token/用户名、
    不得出现 `pull_request_target`。（贴命令与输出）
  - ② **与 106 现行构建的一致性**：断言 context/Dockerfile 路径与 tag 方案（`1.0.1`/`1.0`/`latest`）；
    并给出对照：106 上现行部署用的等价命令 `docker build -t promptmanager:1.0.1 .`（**该命令已由 host_manger 在 106 上实测成功、36 秒**）。
  - ③ **等价构建实测（由 host_manger 执行，dsh 记录到 PROGRESS 即可）**：本机无 Docker ⇒ 用 106 的实测结果作为"镜像能构建"的证据；
    断言 workflow 的 build 参数与之等价（context=`.`、`file=Dockerfile`、无自定义 target）。
  - ④ **端到端实跑（需要用户先配 secrets；由 host_manger 在阶段验收时执行）**：用户在 GitHub 仓库配好
    `DOCKERHUB_USERNAME` / `DOCKERHUB_TOKEN` 后 → 手动 `workflow_dispatch`（或推一个 tag）→ **Actions 绿** →
    `docker pull <ns>/promptmanager:<ver>` **能拉下来** → 在 106 上 `docker run` 起容器 → `/healthz` 返回 `version` 正确。
    ⚠️ **本条在"用户配好 secrets 之前"不算完成**：dsh 交付时须写明"①②③ 已完成、④ 待用户配 secrets 后由 host_manger 触发验证"。

- **AC-92 分栏中栏在手机端撑满（v44 新增；真实像素）**
  - ① **390×844**：`.pm-split-list` 宽度 == **容器宽度 358**（= 390 − 2×16）、`right == 374`；
    **改前实测值必须一并贴出**（host_manger 量过：**w=276 / right=292 / 容器 358 / 右侧空 82**）。
  - ② **与卡片/表格视图一致**：同宽度下三者的内容区都应是 **358 / right 374**（贴三者对照表）。
  - ③ **无横向溢出**：`document.documentElement.scrollWidth == 390`。
  - ④ **桌面不回归**：1600×900 下中栏 **350**、中栏/可用宽 **比值 0.90–0.94**（阶段 22 FR-71 口径），右栏仍在、三栏布局不变。
  - ⑤ **移动端行为不回归**：`.pm-split-detail` **不存在**（单栏降级）、点条目打开详情抽屉、拖拽手柄与收藏星标在、条目数与接口一致；
    截图 **390 亮 / 390 暗** 各一张并**识图**（五问口径）。
- **AC-93 README 用户化 + 两种部署方式（v44 新增）**
  - ① **结构断言**：README 有「两种部署方式」对应章节（**Docker** 与 **源码** 都能找到）；
    **负向断言**：`grep -c '^## 代码质量检查'` = **0**、`grep -c '^## 怎么验证'` = **0**、`grep -cE 'AC-[0-9]|FR-[0-9]|阶段 [0-9]+'` = **0**；
    **任意 `## ` 标题不得重复**（脚本断言）。
  - ② **两种方式"照着能跑"（必须真跑，贴命令与输出）**：
    ⚠️ **执行方分工（228 上没有 Docker，不要试图在本机跑 Docker）**：**源码方式由实现方在 228 的临时目录真跑**；**Docker 方式由 host_manger 在 106/203 上按 README 原样真跑**（实现方只需在 PROGRESS 里写明「该半由 host_manger 验证」，**不得伪造**）。
    **Docker 方式**：按 README 原样 `docker pull` + `docker run`（临时端口 + 临时数据目录）→ `/healthz` 200 且版本正确 → 收尾清理；
    **源码方式**：在**临时目录**按 README 原样（`npm ci` → `npm run build` → `npm run migrate` → 设口令 → 起服）→ `/healthz` 200 → 收尾清理。
    ⚠️ **不得**用 8767 / 106 生产实例做实验；也**不得**改动它们的配置。
  - ③ **链接可达性**：README 里**所有相对链接**（`](path)`）指向的文件/目录**都存在**（贴脚本与输出）。
  - ④ **迁移断言**：`docs/development.md` 与 `docs/api.md` **存在**且**确实承接了被移走的内容**
    （development：代码质量检查 + 验证脚本清单；api：接口/CLI/MCP 参考）；`AGENTS.md` 里有指向这两份的**英文**指引。
  - ⑤ **用户视角抽查**：README 通读一遍**不出现**内部术语与"开发者才关心"的命令（如 `tools/ac-*.sh`、`ci-check.sh` 作为主推命令）；
    功能描述与当前实现一致（不得写已删除的能力）。

- **AC-94 移动端档位顺序与默认档位（v45 新增；按断点分别断言）**
  - ① **移动端 390×844**：`[data-testid="pm-use-viewmode"]` 内档位文本**依次**为 `卡片`、`表格`、`分栏`（**顺序断言**，不是集合断言）。
  - ② **桌面 1600×900**：同一控件内档位文本**依次**为 `分栏`、`表格`、`卡片`（**不得回归**）。
  - ③ **默认档位按断点**：清空 `localStorage['pm-view-mode']` 后 ——
    **移动端**落在 **`卡片`**（`pm-view-card` 存在、`pm-view-split` 不存在、`localStorage['pm-view-mode'] === 'card'`）；
    **桌面**落在 **`分栏`**（`pm-view-split` 存在、`localStorage['pm-view-mode'] === 'split'`）。
  - ④ **不覆盖已有偏好**：预置 `localStorage['pm-view-mode'] = 'table'` → **移动端与桌面都**落在表格。
  - ⑤ **三档在移动端都可切**：依次点 `卡片` / `表格` / `分栏` → 对应 `pm-view-*` 出现、**无 JS 异常**、`document.documentElement.scrollWidth === 390`。
  - ⑥ **回归**：AC-45 的 ③④⑤ 仍过（记忆生效 / 无 `pm-view-list` / 旧值 `list` 回退分栏）；**AC-92（分栏中栏 358）不回归**；
    `npm test` 全绿（**既有断言档位顺序/默认档位的测试必须同步更新为"按断点"**，不得删断言了事）。

## 9. 已定决策（不要再问）

- **D-1 技术栈**：Node 24 + TypeScript + SQLite 单文件 + React/Vite 前端（**UI 用 Ant Design 组件库**），**单进程单端口**（前端产物同进程托管）。
  理由：228 环境实测可用、单服务最轻、验收可命令化；上游"五端 + 外部服务"的复杂度全部去掉。
- **D-11 前端组件库 = Ant Design（`antd` 6.6.4 + `@ant-design/icons` 6.3.4，均 MIT）**（用户 2026-09-18 明确要求，
  已同时写入 `STANDARDS.md` §4）：界面组件**一律来自组件库**，禁止自建基础组件、禁止拷源码进仓库改；
  React 19 原生支持（peer `>=18`，**不要装** v5-patch-for-react-19）；**默认不引 Tailwind**（不叠加第二套样式体系）。
- **D-12 组件库在阶段 1 落地**：阶段 1 就要安装 antd 并让 `npm run build` 通过（含一个用组件库渲染的最小页面），
  并在 `docs/dependencies.md` 登记版本与协议——避免阶段 6 才发现组件库与 React 版本不兼容。
- **D-2 数据库**：SQLite（`better-sqlite3` 或内置 `node:sqlite` 二选一），**不使用任何外部 DB/缓存服务**。
  理由：单用户场景；备份=拷文件；两者在 228 均已实测可用（含 FTS5 + trigram）。
- **D-3 检索**：FTS5 `trigram` 分词器 + **<3 字符查询走 `LIKE` 兜底**（见 §6.6）。
  理由：默认 `unicode61` 对中文 0 命中、trigram 对 <3 字符 0 命中，两者**均已实测**。
- **D-4 端口 8767**：台账无历史分配行、现有项目代码/配置零引用（8766/8768/8770 是其他项目的主端口、
  8765 是 nextblog 的验收备用端口、8769 正被 izone 开发实例占用；实测 8767 空闲）。
- **D-5 clean-room 自研**：**不抄 PromptHub 的任何代码/数据模型/UI 文案**（它是 AGPL-3.0）。
  只做功能对等（本文档 §4 就是功能基线）。
- **D-6 认证**：单用户 + 服务端会话 + HttpOnly cookie（不用 JWT、不做多用户）。
- **D-7 变量语法**：`{{name}}`，见 §6.5（不支持条件/循环/函数，不是模板引擎）。
- **D-8 导入导出**：自有 JSON 格式（§6.4），`replace` 保留 id、`merge` 重新分配；**不做** PromptHub 格式兼容（P1）。
- **D-9 数据落盘位置**：`DATA_DIR`（默认 `<项目>/data`）；媒体目录本期只建空目录，不做上传（P1）。
- **D-10 依赖锁定**：提交 lockfile；依赖与协议登记 `docs/dependencies.md`；GPL/AGPL 依赖**禁止**（先问）。
- **D-13（v5）API Token 与 cookie 并存**：Bearer 给外部客户端、cookie 给浏览器；token 只存 `sha256`、可撤销、记 `last_used_at`；单用户下不做 scope 分层。
- **D-14（v5）CORS 默认关闭**，只按 env 白名单开放，**禁 `*`**，不使用 credentials。
- **D-15（v5）MCP = stdio + 只读工具面**（`prompt_search`/`prompt_get`/`prompt_render`），经 API + token，**不新增端口**。
- **D-21（v17）不设独立管理页**：管理动作（编辑/删除）落在**表格视图**；顶栏只留 品牌 · 主题图标 · `＋新建` · `⋯更多` · 用户；
  使用统计/API 令牌/导入导出/设置 收进 `⋯更多`（用户 2026-09-19 决定，取代 D-20 里"使用/管理双模式"的部分）。
- **D-28（v29）排序模型 = 自定义顺序 + 仅同层级重排**：新增排序档「自定义」（`sort=custom`，按 `sort_order, id`）；拖拽后**自动切到自定义**（首次给一次轻提示）；
  **只允许同层级重排**（卡片视图内的 prompt 条目、同一父级下的文件夹），**不做**跨文件夹改归属与跨父级移动（留待以后）。
  `folders.sort_order` 已存在（复用）；`prompts.sort_order` 新增迁移，既有数据按当前顺序回填。
- **D-27（v26，用户 2026-09-19 明确定义）环境划分 = 三层，术语以此为准**：

  | 环境 | 实例 | 数据归属与可动性 | 说明 |
  | --- | --- | --- | --- |
  | **开发环境** | **实现方（dsh）开发时自起的临时实例**（如 8768 等备用端口、临时 `DATA_DIR`） | **数据可随意重建/删除**；由 **dsh 管理**（host_manger 必要时也可操作） | 只服务于"把功能做出来"，不做长期数据留存 |
  | **测试环境** | 228 上 **systemd `promptmanager.service`**（`/opt/promptmanager` + `/var/lib/promptmanager/pm.db` + `0.0.0.0:8767`） | **数据由 host_manger 管理，可随意改动**；**用户验证时同样会改动** ⇒ 数据变动属正常，**不必追问、不必视为异常** | 用于验收、演示、用户试用；**不是生产**，其数据**不是资产**、无需备份承诺 |
  | **生产环境** | **未来部署到公网**（届时才建立） | **数据主要由用户操作**；必要时由用户提供信息授权 host_manger 操作 | 到那时才启用"生产级"的谨慎度（备份、变更审批、只读巡检） |

  - **由此产生的口径变化**：① 本文件与 PROGRESS/VERIFY 里此前的「生产 / 生产库 / 上线」措辞，凡指的是 8767，一律理解为**测试环境**；
    ② 8767 **数据可自由增删改**（host_manger 验收/演示时可直接改，不必先备份）；
    ③ 但因为用户也会用它验证，**自动化压测/破坏性验收仍优先在临时实例（开发环境端口）上跑**，避免把用户正在看的界面搅乱；
    ④ **"上线"一词在本项目现阶段 = 同步到测试环境**；真正的"上线"要等公网生产环境建立。
- **D-26（v22）品牌图形只有一枚图标，只换尺寸**：全站（favicon / 顶栏 / 登录页 / 空态 / 关于页）**共用同一枚**提示符 `>_` 图标，
  按 BRIEF 的尺寸映射表取不同尺寸；**插画方案（第三方图与我方自绘）均已作废**。
  理由（用户原话）："这样你更容易把握好图的显示效果"——单一来源最不容易走形。
- **D-25（v21）「关于」页的受众 = 日常使用者，不是开发者**：内部信息（`BRIEF.md` / 验收标准 / 阶段编号 / 依赖清单细节）**不上面**；
  按「服务 / 使用 / 维护（默认折叠）」三级分区，状态用一行徽标而不是大块 `Result`；长命令与路径可一键复制。
- **D-23（v20）顶栏组成与菜单顺序以用户 2026-09-19 反馈为准**：顶栏 = 品牌 · `＋新建` · `⋯更多` · 主题切换 · 登出（**去掉用户信息**，其移入 `⋯更多`）；
  `⋯更多` = 使用统计 → API 令牌 → 导入/导出 → 关于 → 已登录信息 → 登出，**删除「文件夹与标签」入口**。
- **D-24（v20）文件夹/标签管理就地化**：撤销 FR-43 的「左栏只读筛选」「增删改入口收进 `⋯更多`」——
  **文件夹**：标题行 `+` 新建顶级、行内悬浮 新建子文件夹 / 重命名 / 删除（二次确认）；
  **标签**：只做筛选（胶囊云），**不提供增删改**（标签随 prompt 编辑产生/清理）。
- **D-22（v19）分栏右栏 = 详情面，管理动作随之放宽**：分栏视图右栏**原样复用现有详情面**（用户 2026-09-19：「目前的详情页的内容就满足」），
  而该详情面**自带「删除」** ⇒ **D-21 的「管理动作只在表格视图」放宽为「表格行内 + 详情面」**。
  仍**不设独立管理页**；卡片视图**不常驻**删除按钮（分栏里的删除只出现在右栏详情面内）。
- **D-20（v13）产品定位 = 使用优先**：本服务第一身份是**日常使用工具**，「找到 → 一键复制 → 贴走」是主流程；
  默认落地页必须是**使用视图**，管理视图是次要视角（用户 2026-09-18 纠偏，见 FR-41）。
- **D-19（v12）视觉重设计方向**
  （**用户 2026-09-18 亲自选定**；B/C 不再作为整站方向）。用户同时明确：**要保留的是"排版/信息架构的重设计"**，不只是样式
  → 见 FR-40b 与 AC-31/AC-32。A 的令牌见 `docs/design/a-dark-saas/tokens.md`（三层令牌 + antd `theme.token`/`theme.components` 映射），
  登录页可参考 `docs/design/b-apple-minimal/`（只取其留白/大标题/胶囊按钮的观感）。
- **D-16（v5）客户端策略**：外部客户端一律「薄壳 + 同一个 API」；**浏览器扩展与桌面客户端不在本期**，各自单独立项
  （它们的共同前置正是本期的 Token/CORS/CLI）。
- **D-17（v6）使用记录（FR-19）为 P0**：只记"取用"（详情 / 渲染 / MCP 取用），通道分 `session`/`token`/`mcp`；
  **写 usage 不得产生版本、不得改 `updated_at`**；**不参与导入导出**。
- **D-18（v6）同时支持内网与公网两种部署形态**（用户 2026-09-18 拍板「内网、外网都会部署」）：
  服务不做 TLS；`TRUST_PROXY` **默认关闭**、`PUBLIC_ORIGIN` 控制 cookie `Secure`；交付反代样例与双形态部署文档；
  **实际暴露由 host_manger 执行**。
- **T-1（2026-09-18 已结案）外网可达性**：用户拍板 —— **内网与公网都要支持**（见 D-18 与 AC-28）。
  ⚠️ 仍不变的红线：**反代/证书/防火墙/DNS 的实际改动只由 host_manger 做**，dsh 只交付配置与文档。
- **T-2（新，待用户拍板）两实例的数据关系**：若内网与公网各跑一个实例，两份 SQLite 是**各自独立**的。
  **默认约定（本期）**：**不做自动同步**，两实例独立；需要迁移时用 `GET /api/export` + `POST /api/import`（或 CLI）人工搬运；
  外网实例建议偏向**只读用途**（供外网客户端取用），写入仍以内网主库为准。若将来要双向同步，**单独立项**（不在本期）。

- **D-29（v30）文件夹筛选 = 含子目录；inclusive 视图下拖拽限同目录内**
  `GET /api/prompts?folder_id=X` **含 X 的全部后代**（任意深度），与侧栏计数徽标**同口径** —— 依据用户 2026-09-20：「父子目录是包含关系…跟卡片计数要保持一致」。
  连带约束：`prompts.sort_order` 是**全局**列（D-28），若在 inclusive 视图允许跨目录拖拽，会把子目录条目一并重编号 ⇒
  规定 **inclusive 视图下拖拽仅在同 `folder_id` 内生效，跨目录放置忽略（不报错、不发请求）**。
- **D-30（v31）拖拽排序模型修正：取消「仅同目录内生效」，改「槽位保持」—— D-29 的连带约束作废**
  ① `folder_id` 筛选仍**含全部后代**（D-29 的前半条**继续有效**，用户 2026-09-20 明确要求「跟卡片计数保持一致」）；
  ② **拖拽不再限制同目录**：inclusive 视图下拖拽**总是生效**（原限制在默认「全部」视图下等于全面禁用，
     用户实测「根本不生效」，且被忽略时无任何反馈）；
  ③ 后端 `PATCH /api/prompts/order` 改为**槽位保持**：只在这些 id **已占据的 `sort_order` 槽位**之间重排，
     **不把它们挪到全局最前、不产生重复 `sort_order`**（修掉「目录内拖拽顶到最前」的既有缺陷）。
- **D-31（v32）品牌显示名：顶栏用简称 `PromptM`，其余位置保留 `PromptManager`** —— 用户 2026-09-20 拍板（原话「我们只改这个logo的地方，其他地方都不要改」）。
  ① 顶栏（唯一空间受限处）显示 `PromptM`；② **登录页 / 关于页 / 浏览器 `<title>` / 导出错误文案一律保持 `PromptManager`**；
  ③ **导出文件契约值 `app='promptmanager'` 永久不变**（**显示名 ≠ 契约值**，任何人不得以"统一品牌"为由改动它，否则旧导出 JSON 全部 400）。



- **D-32（v43）Docker Hub 镜像坐标与 tag 方案** —— 由 host_manger 定（用户只要求"构建并推送"，未指定细节）：
  ① **命名空间走 secret**（`${{ secrets.DOCKERHUB_USERNAME }}/promptmanager`），仓库里不出现任何用户名；
  ② 仓库名 = **`promptmanager`**；③ 平台 **仅 `linux/amd64`**；④ tag 方案 = **`<semver>` + `<major.minor>` + `latest`**；
  ⑤ **Docker Hub 仓库可见性（public/private）由用户在 Docker Hub 侧自行决定**，workflow 两种都兼容
  （注：**public** 时任何人可拉取并可读到镜像内**已编译**的 `dist/**` 与 `node_modules`，**不含**源码 `.ts`、文档、凭据与数据）。

- **D-33（v44）文档归属：README=用户 / docs/development.md=开发者 / docs/api.md=接口 / AGENTS.md=AI（英文）** —— 由 host_manger 定（用户要求"README 给用户看、开发者信息移走"）：
  ① **`README.md`（中文）** = 用户文档：是什么 / 能做什么 / **两种部署方式（Docker、源码）** / 怎么用 / 备份升级 / FAQ / 已知限制 / 文档索引；
  ② **`docs/development.md`（中文，新建）** = 开发者文档：代码质量检查、验证脚本、构建测试命令、项目结构、验收体系、CI；
  ③ **`docs/api.md`（中文，新建）** = 接口 / CLI / MCP 参考（原 README 那一大段 curl 示例）；
  ④ **`AGENTS.md`（英文，保持）** = AI 代理操作指南，**不承载人类开发者文档**（`STANDARDS.md §5.1` 硬性要求全英文），只加一行指引；
  ⑤ 用户原话提到"可以移到 AGENT.md"——**按上述拆分执行**，理由与政策依据写进 README/AGENTS 的交叉引用里（**若用户坚持要合进 AGENTS.md，再改**）。

- **D-34（v45）移动端档位：顺序倒序 + 默认卡片；桌面不动** —— 由 host_manger 定（用户原话只说"顺序变成卡片、表格、分栏"）：
  ① 移动端档位顺序 = `卡片 / 表格 / 分栏`；② **移动端默认档位 = `卡片`** —— 用户给的理由是"移动端卡片效果更好"，
  若只改顺序而不改默认，移动端首次进入**仍会落在分栏**，与理由矛盾；③ 桌面顺序与默认**一字不变**；
  ④ **不覆盖用户已有偏好**；⑤ 旧值 `list` 仍回退 `split`（保持 AC-45 ⑤ 不变）。
  ⚠️ **② 是 host_manger 的推断（用户原话只说"顺序"）—— 若用户只要改顺序，删 ② 即可（`readPref` 的默认值一处）。**

## 10. 边界与停止条件

- 遇到本文件未覆盖、且会影响交付的决策 → 写入 `QUESTIONS.md` 并**停手**，不要猜。
- 单条验收标准连续 3 次修不过 → 停手写 `QUESTIONS.md` 说明，不要反复试。
- 端口 8767 被占用且 8765–8770 全被占 → 写 `QUESTIONS.md` 停手。
- 需要 GPL/AGPL 依赖、或需要外部服务（DB/Redis/对象存储）→ 写 `QUESTIONS.md` 停手（本项目**不该**需要外部服务）。
- 禁止：修改本文件、删除或跳过测试、改动 `projects/` 之外的文件、对外发布、部署到系统、`git add -A`。

## 11. 流程要求

1. 开工前：把第 8 节验收标准翻译成你要执行的检查命令，追加到 `PROGRESS.md`。
2. 每完成一个可验收单元就提交一次，message 格式：`<type>(<scope>): <描述>`（type ∈ feat/fix/refactor/test/docs/chore）。
3. 每个阶段收尾：更新 `PROGRESS.md`（完成项 / 未完成项 / 实测输出 / 下一步）。
4. **阶段划分（每阶段 30–60 分钟量级，各自可验收；本次只做阶段 1）**：

| 阶段 | 内容 | 对应 AC |
| --- | --- | --- |
| **阶段 1（本次）** | S0 技术验证（中文检索方案实测报告 `docs/search-zh.md`）+ 项目骨架（TS/框架/SQLite 接通、`/healthz`、`npm run build`/`npm start` 可跑）+ **前端脚手架与组件库固定（Vite + React + **antd 6.6.4**；用组件库渲染一个最小页面并构建通过）** + `migrations/001_init.sql` 数据模型草案 + `docs/dependencies.md` | AC-1、AC-2、AC-19、**AC-20** |
| 阶段 2 | 认证（登录/会话/限流）+ CLI `user set-password` + `migrate` 幂等 | AC-3、AC-4、AC-15 |
| 阶段 3 | prompt/folder/tag CRUD + 检索（含 FTS 同步与兜底） | AC-5、AC-6、AC-7、AC-14 |
| 阶段 4 | 版本历史（自动留档/diff/回滚）+ 变量提取渲染 + Markdown 渲染与净化 | AC-8、AC-9、AC-12 |
| 阶段 5 | 导入导出（两种模式 + 校验 + 原子性） | AC-10、AC-11 |
| 阶段 6 | **对外可用面**（v6 范围，可拆成 2–3 个提交单元）：API Token（创建/列出/撤销 + Bearer 认证）+ CORS 白名单 + 使用侧 CLI（`pm get/render/token`，走 HTTP API）+ **使用记录 FR-19** + **内外网部署形态**（`TRUST_PROXY`/`PUBLIC_ORIGIN` + 反代样例 + 双形态部署文档） | AC-22、AC-23、AC-24、**AC-27**、**AC-28** |
| 阶段 7 | **MCP server（stdio）**：`prompt_search`/`prompt_get`/`prompt_render`，经 API + token，用**真实对端**端到端验证 | AC-25、AC-26 |
| 阶段 8 | 前端 P0（**全部用 antd 组件实现**：登录/列表/编辑器/版本面板/变量面板/Markdown 预览/响应式/亮暗）+ **净化硬化（v4 §5）** + 界面自证 | AC-13、AC-20、AC-21 |
| 阶段 9 | 部署文件 + `README.md` 四要素 + 全量测试收尾 | AC-16、AC-17、AC-18 |
| **阶段 10A** | **视觉重设计·设计打样（用户追加）**：用 `frontend-design` 定美学方向 → 用 `design-system` 落令牌 →
  用 `dsh-web-artifact-designer` 出**自包含 HTML 设计稿**，**做 2–3 个方向 × （列表页 + 编辑器）** 的亮/暗与移动版截图 →
  写 PROGRESS 一节（设计意图/色彩/字体/间距/圆角/层级决策 + 与现状对比）+ 写 `QUESTIONS.md` 请用户在 A/B/C 中挑一个 → **停手** | 无（打样阶段，产出是设计稿与决策请求） |
| **阶段 13** | **取消管理页（管理并入表格视图）+ 主题图标切换 + 修复「新建未保存即入库」**（用户三条反馈） | **AC-41、AC-42、AC-43** + 不得回归（AC-33/33b/35/36/37/39/40） |
| **阶段 14** | **分栏视图（PromptHub 式三栏）+ 删除列表视图**（用户反馈：默认落分栏、右栏复用现有详情页、档位顺序 分栏/表格/卡片） | **AC-44、AC-45、AC-46** + 不得回归（AC-33/33b/35/36/37/40/41/42/43） |
| **阶段 15** | **顶栏重排（新建·更多·主题·登出，去用户信息）+ ⋯更多 项序与删减 + 文件夹区新建入口与层级优化 + 标签胶囊云**（用户四条反馈） | **AC-47、AC-48、AC-49、AC-50** + 不得回归（AC-33/33b/35/36/37/40/41/42/43/44/45/46） |
| **阶段 16** | **Logo 更名「PromptManager」+点击回主页 · 关于页重构 · 删「拿来就用」装饰块 · FIX 非空文件夹删除静默失败 · 编辑页返回详情 · 右栏改「预览→变量→版本」· 收藏入口显性化（含表格）· 主题跟随系统图标**（用户八条反馈） | **AC-51、AC-52、AC-53、AC-54、AC-55、AC-56、AC-57、AC-58** + 不得回归（AC-33/33b/35/36/37/40~50） |
| **阶段 17** | **品牌图形全站统一：装 favicon 家族 + 顶栏 mark + 三处装饰位（同一枚图标·多尺寸）** | **AC-59** + 不得回归（AC-33/33b/35/36/37/40~58） |
| **阶段 18** | **收尾清理（注释/文案漂移）+ 构建体积瘦身（主 chunk 1,265KB → ≤500KB）+ 编辑器全屏** | **AC-60、AC-61、AC-62** + 不得回归（AC-33/33b/35/36/37/40~59） |
| **阶段 19** | **编辑器全屏形态纠正（只藏内部列表栏 + 1:1 平分 + 按钮对齐详情页）+ FIX 版本 diff 尾换行假变更 + FIX 复制兜底（内网 HTTP）** | **AC-63、AC-64、AC-65** + 不得回归（AC-33/33b/35/36/37/40~62） |
| **阶段 20** | **详情页去冗余「当前字段+预览」头部（R-1）+ `⋯更多` 删「已登录」新增「修改密码」弹窗（含 `POST /api/password`、会话语义、限流、不回显）** | **AC-66、AC-67** + 不得回归（AC-33/33b/35/36/37/40~65） |
| **阶段 21** | **备注改为纯文本（去预览与 Markdown 处理）+ 详情页标题下展示备注（样式参考 PromptHub）** | **AC-68、AC-69** + 不得回归（AC-33/33b/35/36/37/40~67） |
| **阶段 22** | **拖拽排序（卡片条目 + 文件夹树，含 `sort_order` 迁移与「自定义」排序档）+ 分栏中栏条目精简为「标题+备注固定两行」且宽度 -8%** | **AC-70、AC-71** + 不得回归（AC-33/33b/35/36/37/40~69） |
| **阶段 23** | **文件夹筛选含子目录（与侧栏计数一致）+ 卡片底部行贴底 FIX + 表格视图拖拽（含拖拽后刷新感尽力优化）** | **AC-72、AC-73、AC-74** + 不得回归（AC-14/33/33b/35/36/37/40~71） |
| **阶段 24** | **FIX 卡片拖拽在「全部 / 父目录」视图下失效（取消 D-29 跨目录限制 + 后端 order 改「槽位保持」+ 卡片本体可拖评估）** | **AC-75** + 不得回归（AC-70/71/72/73/74 与 AC-33/33b/35/36/37/40~69） |
| **阶段 25** | **顶栏品牌文字缩短为「PromptM」（仅顶栏一处；其余四处保持全名；导出契约值不动）** | **AC-76** + 不得回归（AC-47/AC-51 与 AC-33/33b/35/36/37/40~75） |
| **阶段 26** | **上线准备：文档整理（README 重写为当前状态 + 过程文档归档 + 依赖/部署文档核对）+ 临时产物清理 + GitHub Action 代码质量检查（本地可跑）+ 版本号 `1.0.0` 与版本管理规划** | **AC-77** + 不得回归（AC-1…AC-76） |
| **阶段 27** | **表格模式批量操作（复选框+全选+批量收藏/移动/删除+二次确认）· 分栏详情页显示并可改「文件夹+标签」（排版参考 PromptHub）· 分栏详情页去掉「备注」页签 · 分栏详情页去掉「变量填值」区块 · 加大「变量填值对话框」宽高** | **AC-78、AC-79、AC-80、AC-81、AC-82** + 不得回归（AC-1…AC-77） |
| **阶段 28** | **创建项目 `AGENTS.md`（AI 代理操作指南：常用命令 / 结构地图 / 约定 / 红线 / 项目特有的坑 / 文档地图 / 协作约定；由实现方据实撰写并逐条验证）** | **AC-83** + 不得回归（AC-1…AC-82） |
| **阶段 29** | **阶段 27 的 5 条视觉细化：表格批量 UI（表头 indeterminate + 复选框尺寸一致 + 工具条间距）+ 详情页元信息行（层级间距 + 长内容换行保护 + chip 样式与左栏统一）** | **AC-84、AC-85** + 不得回归（AC-1…AC-83） |
| **阶段 30**（**非阶段** —— 用户 2026-09-21 **直接交办 dsh**，不走常规派活流程） | **`docs/` 只放最终状态：`docs/shots/` 收敛为关键展示图一套（6–10 张）+ 各阶段截图移 `tmp/shots-archive/` + 改 `ui-shots.sh` 默认输出约定 + 把规范写进 `AGENTS.md`** | **AC-86**（供 dsh 自查；host_manger 验收时按它核） |
| **阶段 31** | **表格「标签」列加标签间距 + **版本保留策略（每个 prompt 最多保留最近 10 个版本，数据层裁剪 + 版本面板与 README 文案说明）** | **AC-87、AC-88** + 不得回归（AC-1…AC-86） |
| **阶段 32** | **FIX CI 步骤顺序（`typecheck:tests` 必须在构建之后，干净环境必失败）+ 登录页简化（去掉默认预填 `admin`〔安全〕+ 删除四条噪音信息，只留登录）** | **AC-89、AC-90** + 不得回归（AC-1…AC-88） |
| **阶段 33** | **GitHub Actions：构建容器镜像并推送到 Docker Hub**（`docker.yml`：tag `v*` 构建并推送 `1.0.x`/`1.0`/`latest`；`main` 只构建不推送；凭据只走 GitHub Secrets；平台 `linux/amd64`） | **AC-91**（① 静态断言 ② 与 106 一致性 ③ 等价构建 ④ **实跑待用户配 secrets 后由 host_manger 触发**）+ 不得回归（AC-1…AC-90） |
| **阶段 34** | **分栏视图手机端中栏撑满（FR-90，一处响应式宽度）+ README 重写为"用户文档"并给 Docker/源码两种部署方式（FR-91：开发者内容迁到 `docs/development.md`、接口迁到 `docs/api.md`，顺带消除 README 重复两节 = backlog R-5）+ **FR-92 移动端档位顺序改为「卡片 / 表格 / 分栏」且默认落在卡片**（桌面顺序与默认一字不变、不覆盖已有偏好）** | **AC-92、AC-93、AC-94** + 不得回归（AC-1…AC-91） |
| **阶段 12** | **导航归位 + 信息克制（用户反馈）**：使用视图只留"用"（顶栏去管理项、左栏只作筛选）、解释性文案下线并收进「设置/关于」、卡片去内部 id、状态条移出使用视图 | **AC-37、AC-38、AC-39、AC-40** + 不得回归（AC-33/33b/34/35/36） |
| **阶段 11** | **使用优先改造（用户纠偏）**：一键复制（列表/卡片/编辑器/变量面板）× 使用·管理分离（默认使用视图、模式记忆）
  × 快捷（`/`、`Ctrl+K`、`Esc`、双击、键盘选择）× 移动端大按钮 + 复制计入使用记录 | **AC-33、AC-34、AC-35、AC-36** + 不得回归（AC-13/20/21/29/31） |
| **阶段 10B** | **视觉重设计·全站应用（方向 A + 登录页 B）**：用 `antd` token/组件级定制 + 少量 CSS 落地到**全部界面**，
  **并且必须实现 FR-40b 的结构/排版改动**（编辑器三栏常驻、列表页 KPI 行/侧栏/底栏、顶栏导航、移动端形态、固定 `data-testid`）；
  重跑全部回归（组件库约束、净化硬化、测试全绿、响应式、亮暗）+ 界面自证 + **与设计稿并排对照** | AC-13、AC-20、AC-21、AC-29、AC-30、**AC-31**、**AC-32** |

5. **阶段边界**：阶段划分是**验收边界**——你可以顺手把后续阶段需要的能力做出来，但要守两条：
   ① **只宣称本阶段该交付的东西**，不要据此宣称后续阶段完成、也不要自行进入下一阶段；
   ② 顺手做的跨阶段实现写进 `PROGRESS.md` 的「本阶段自行判断的实现细节」备查——**验收只按本阶段 AC 判定**。

## 12. 变更记录

> **完整变更历史（v1–v32）已外移到 [`docs/brief-changelog.md`](docs/brief-changelog.md)** ——
> 目的：BRIEF 从 204KB 瘦身，让实现方每个阶段通读规格时不必翻 32 个版本的变更史。
> **本节只记当前版本，以及"外移"这件事本身。**

- **v45 2026-09-21（用户新增：移动端档位顺序）**：新增 **FR-92**（移动端 <768px 档位开关顺序改为 `卡片 / 表格 / 分栏`、**首次进入默认落 `卡片`**；桌面顺序与默认一字不变；**不覆盖已有本地偏好**；旧值 `list` 仍回退 `split`） + **AC-94**（按断点分别断言顺序与默认档位 + 三档可切 + 回归）+ **D-34**（② 默认档位是 host_manger 的推断，可否决） + **AC-45 ① ② 按断点修正**（原断言写死桌面顺序）；**并入阶段 34**（尚未派发）。
- **v44 2026-09-21（用户两条新需求：分栏手机端全屏 + README 用户化）**：新增 **FR-90**（分栏中栏在 <768px 必须撑满可用宽度 358；实测根因 = `SplitView.tsx` 中栏 `flex: 0 0 clamp(276px, 31.3%, 350px)` 与移动端无关 ⇒ 390 宽时被 clamp 下限抬到 276、右侧空 82；同宽度下卡片/表格视图都是 358；桌面三档 clamp 与 FR-71 比值不得回归）+ **FR-91**（README 改为用户文档：是什么/能做什么/两种部署方式〔Docker 与源码，都要能照着跑通〕/怎么用/备份升级/FAQ/已知限制/文档索引；开发者信息迁 `docs/development.md`、接口迁 `docs/api.md`；README 不得出现 AC-/FR-/阶段号等内部术语；顺带消除重复两节 = backlog R-5）+ **AC-92 / AC-93** + **D-33**（文档归属）+ **阶段 34**。
- **v43 2026-09-21（用户新需求：GitHub Actions 构建镜像推送到 Docker Hub）**：新增 **FR-89**（新增 `.github/workflows/docker.yml`：tag `v*` 构建并推送 `<semver>`/`<major.minor>`/`latest`、`main` 只构建不推送、`workflow_dispatch` 手动补跑；凭据**只**从 GitHub Secrets 取、任何 step 不得回显 secret、不硬编码命名空间；平台 `linux/amd64`；文档补「从镜像运行」与「镜像发布」） + **AC-91**（① 静态断言 ② 与 106 一致性 ③ 等价构建 ④ 端到端实跑〔**待用户配 secrets**〕）+ **D-32**（镜像坐标与 tag 方案）+ **阶段 33**。
- **v42 2026-09-21（用户两条：CI 报错 + 登录页简化）**：新增 **FR-87**（**FIX `tools/ci-check.sh` 步骤顺序** —— `typecheck:tests` 跑在 `npm run build` 之前，
  而测试 import 的是 `../dist/**` ⇒ 干净环境（CI）38 个 `TS2307`；本地因已有 `dist/` 而看不出来。判据=**先删 `dist/` 再跑 ci-check 全绿** + GitHub Actions 实跑通过）
  + **FR-88**（**登录页简化**：① **P0 去掉 `initialValues={{ username: 'admin' }}` 预填与 `placeholder="admin"`**〔不得暴露账号名〕；
  ② 删除四条噪音〔`SELF-HOSTED · 单进程单端口`、副标题、`口令由本机 CLI 设置…`、`除 /healthz…401`〕，只留 品牌图 + `PromptManager` + 表单）
  + **AC-89 / AC-90** + 阶段 32。
- **v41 2026-09-21（用户两条新需求）**：新增 **FR-85**（表格「标签」列的多个标签**加间距**——现状 `tags.map(<Tag>)` 无 gap 导致"拼在一起"；
  要求相邻间隙 ≥4px 且与卡片视图一致）+ **FR-86**（**版本保留策略（数据层）**：每个 prompt 在 `prompt_versions` 里**最多保留最近 10 个版本**，
  超出**真删**；触发时机覆盖**新建 / 更新 / 回滚 / 导入**四处（建议抽公共 `pruneVersions`）；**当前版本必须在保留集内**、**不重编号**；
  **导入同样裁剪**（并在"已知限制"写明与"导出→导入→再导出一致"的张力）；**存量不做一次性迁移**（下次产生新版本时自然裁剪）；
  **版本面板 + README 必须有"最多保留最近 10 个版本"的文案**）+ **AC-87 / AC-88** + 阶段 31。
- **v40 2026-09-21（口径澄清）**：**阶段 30 标注为「非阶段」** —— 用户明确「**不用派活，我只是一个说法，让他操作，你更新测试就行**」：
  `docs/` 收敛这件事由**用户直接交办 dsh**（不走常规派活流程）；**FR-84 / AC-86 保留**（作为 dsh 自查与 host_manger 验收的判据），
  但**不再作为"待派发的阶段"**。另：host_manger 已补做**阶段 27/28/29 的测试环境同步**（此前漏项，见 `VERIFY.md`）。
- **v39 2026-09-21（用户：`docs/` 只放最终状态）**：新增 **FR-84**（`docs/shots/` 收敛为**关键页面展示图一套（6–10 张）**、其余与**各阶段截图**整体移 `tmp/shots-archive/`；
  **改 `tools/ui-shots.sh` 约定**：默认输出 `tmp/`、只有发版/交付前才产一套到 `docs/shots/`；
  **把规范写进 `AGENTS.md`（英文）**；`docs/dev-history/` 的**文档类保留**（用户选 A）、只移截图）+ **AC-86** + **阶段 30**。
  依据：`/root/greenhouse/STANDARDS.md` **§5.2**（新增）。现状：`docs/` 下 png **172 张 / 18M**、`git ls-files docs` 里 png **94** 个。
- **v38 2026-09-21（用户：处理 5 条视觉改进建议）**：新增 **FR-82**（表格批量 UI 细化：① 表头复选框**补 indeterminate 半选态**且与行内**尺寸一致**；
  ② 批量工具条与表头行间距 **≥6px**）+ **FR-83**（详情页元信息行细化：③ 与备注行间距 **≥12px** 且不大于与页签行间距；④ **长内容换行保护**（长文件夹名 + ≥5 标签不溢出、「+ 添加标签」不出界）；
  ⑤ 标签 chip 样式与**左栏统一**）+ **AC-84 / AC-85** + 阶段 29。
  来源：这 5 条是 **host_manger 在验收阶段 27 时逐张看图提出**的视觉/健壮性问题（**不影响功能**，故单列一批）。

- **v37 2026-09-21（修正 AC-83 ⑧ 的判据命令 —— 实现方实测发现）**：**去掉判据里的 `LC_ALL=C`**。
  背景：实现方在阶段 28 按 AC-83 ⑧ 原样执行判据时拿到 `rc=2`（`grep: character value in \x{} or \o{} is too large`），
  遂**做了对照实验**并报告：`LC_ALL=C` 会把 PCRE2 压进**字节模式**（`\x{}` 上限 0xFF）⇒ 原命令**永远跑不通**；
  而"顺手修"成 `LC_ALL=C grep -cP '\p{Han}'` 会**假绿**（对纯中文文件也返回 0）。
  我在验收时**独立复现确认**：原判据 rc=2；去掉 `LC_ALL=C` 的同区间判据对英文版 = 0、对中文版 = 192；`\p{Han}`（不带 `LC_ALL`）对中文版 = 188。
  ⇒ **判据改为**：`grep -cP '[\x{4e00}-\x{9fff}\x{3000}-\x{303f}\x{ff00}-\x{ffef}]' AGENTS.md` → **0**（含中文标点，**不加 `LC_ALL=C`**）。
  **这是"实现方发现规格缺陷 → 改规格而不是迎合"的正面案例**（与 v5/v6 的 FR-16 同类）。
- **v36 2026-09-21（用户定「文档语言规范」）**：**`AGENTS.md` 必须全英文** —— 用户明确「**给 AI 看的文件内容都使用英文，方便 AI 识别和更新**」。
  依据 **`/root/greenhouse/STANDARDS.md` §5.1（新增）**：**AI 代理指令文件（`AGENTS.md` / `CLAUDE.md` / `.cursorrules` 等）一律英文**；
  其余文档（`BRIEF` / `STANDARDS` / `PROGRESS` / `VERIFY` / `README` / `deploy/**` / `docs/**`）**保持中文**（人机共读）。
  连带：**§7 的 `AGENTS.md` 条目**补注"必须全英文"；**AC-83 新增第 ⑧ 条**（不得含任何 CJK 字符，判据 = `grep -cP` 命中 0）。
- **v35 2026-09-21（用户：为项目创建 AGENTS.md）**：新增 **§7 交付物 `AGENTS.md`** + **AC-83**（受众是"新会话/新代理"而非产品介绍：
  常用命令必须**逐条真跑**、结构路径必须**逐个存在**、不得含过期表述、**含 ≥5 条本项目特有的坑**（指向历史证据）、
  与既有文档**指向而非复制**、并以"无上下文的新会话能否 5 分钟上手"做自检）+ **阶段 28**。
  **纪律**：内容由**实现方根据自己对项目的真实了解**撰写（**不许套模板/凭空写**），用户明确要求"必须由开发者自己写"。

- **v34 2026-09-21（用户 5 条新需求）**：新增 **FR-77**（表格模式**批量操作**：首列复选框 + 表头全选 + 工具条「已选择 N 项」+ **批量收藏 / 批量移动 / 批量删除**（二次确认）+ 取消；落库建议新增批量接口，判据"一次操作 1 个请求"）
  + **FR-78**（分栏详情页**新增「文件夹 + 标签」元信息行**并可**内联修改**，排版参考 PromptHub：标题 → 备注 → 元信息 → 页签 → 正文）
  + **FR-79**（分栏详情页**去掉「备注」字段页签**，只留 用户提示词 / 系统提示词）
  + **FR-80**（分栏详情页**去掉「变量填值」区块**；但编辑器变量面板与复制时的填值弹窗**保留**）
  + **FR-81**（**加大「变量填值对话框」宽高**各 ≥15%；先确认该弹窗 = `VarsDialog`）
  + **AC-78 / AC-79 / AC-80 / AC-81 / AC-82** + 阶段 27。

- **v33 2026-09-20（上线准备）**：新增**阶段 26「上线准备」** + **AC-77**（① 文档整理：README 重写为当前状态、过程文档归档、依赖/部署文档核对；
  ② 临时产物清理：`tmp/` / `var/cache/` / 过程截图；③ 质量与版本：GitHub Action 代码质量检查（本地可跑）、版本号 `1.0.0` 与版本管理规划；
  ④ **零代码改动证明**）；**§12 变更记录（v1–v32）外移到 `docs/brief-changelog.md`**；**§7 交付物清单补全**
  （补 `tools/ac-stage*.sh`、`docs/dev-history/**`、`docs/brief-changelog.md`、`CHANGELOG.md`、`.github/workflows/**`、容器化文件）；
  **容器化部署文件（Dockerfile/compose）与 git 历史重新初始化由 host_manger 执行**（不在 dsh 的实现范围）。
