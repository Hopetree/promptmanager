# promptmanager · BRIEF 变更历史（v1–v32）

> 本文件由 `BRIEF.md` §12 **外移**而来（2026-09-20，BRIEF v33）——目的是控制 BRIEF 体积、便于实现方通读规格。
> **`BRIEF.md` 正文仍是唯一需求来源**；本文件只用于追溯"某个需求/决策是何时、因何加入的"。
> 格式：**倒序**（最新在上）。

- **v32 2026-09-20（用户：顶栏 logo 名称太长 → 缩短）**：新增 **FR-76**（顶栏品牌文字 `PromptManager` → **`PromptM`**，
  **只改 `AppHeader.tsx` 这一处**；用户明确「其他地方都不要改」⇒ `<title>` / 登录页 / 关于页 / `pure.ts` 错误文案**四处保持全名**，
  导出契约值 `app='promptmanager'` **不动**）+ **AC-76**（含"反例断言：其余四处仍是全名"，防顺手全局替换）+ **D-31**（显示名 ≠ 契约值）。
  背景：候选名查证 —— `Promptly` / `Prompta` **均已撞名**（10k+ 用户扩展、同名 AI 公司），`PromptHub` 撞本机既有服务；
  `PromptM` 无撞名，且比 `Prompt` 多保留"管理器"语义。

- **v31 2026-09-20（用户报 bug：卡片拖拽在全部/父目录视图下不生效）**：新增 **FR-75**（取消 D-29 的「仅同目录内生效」——
  该限制在默认「全部」视图下等于**全面禁用**（测试环境实测 7 张卡片只有 1 对可拖）且**静默无反馈**；
  **后端 `PATCH /api/prompts/order` 改为「槽位保持」**：只在这些 id 已占据的 `sort_order` 槽位间重排，
  修掉「目录内拖拽把条目顶到全局最前 + 产生重复 sort_order」的既有缺陷；**卡片本体也可拖**（成本低则做，不作为阻塞项））
  + **AC-75** + **D-30**（**D-29 的连带约束作废**，其「筛选含全部后代」前半条继续有效）。
  ⚠️ **规格作者自省**：本 bug 由**我的规格设计失误**引入（D-29 过严），且我的验收**漏检**——
  AC-74 ⑥ 的夹具恰好是「4 条同目录」，落在「能生效」的组合上，掩盖了真实数据分布下的失效；冒烟也没验拖拽。

- **v30 2026-09-20（用户三条：目录包含关系 + 卡片底部行 + 表格拖拽）**：新增 **FR-72**（`folder_id` 筛选改为**含全部后代**、与侧栏计数同口径；
  inclusive 视图下拖拽**仅同目录内生效**；连带修订 AC-14 与 3 个测试文件的旧断言）+ **FR-73**（卡片视图 FIX：**无标签卡片的「元信息+复制」行不贴底** ⇒
  末行恒定贴卡片底边 14px±1、同一网格行卡片底边对齐、与有无标签无关）+ **FR-74**（**表格视图支持拖拽排序**，复用 `@dnd-kit` 与 `PATCH /api/prompts/order`、不新增依赖；
  ② **拖拽后「刷新感」优化为尽力项** —— 乐观更新消除 loading 闪烁，**成本高可跳过并在 PROGRESS 说明**）+ **AC-72 / AC-73 / AC-74** + **D-29**（筛选口径与拖拽边界）。
- **v29 2026-09-20（用户两条：拖拽排序 + 分栏中栏精简）**：新增 **FR-70**（卡片条目与同层级文件夹可**拖拽排序**；`prompts` 新增 `sort_order` 迁移 + 新增排序档「**自定义**」+ 新增 order 接口（幂等、非法 id 400）；
  拖拽库用 `@dnd-kit`（MIT，新依赖须登记+CVE 查询）；**排版舒适度硬要求**：手柄不增高行高、拖动不跳动（≤150ms 过渡）、拖后未动条目像素差 ≤1px、无横向溢出）
  + **FR-71**（分栏中栏条目**只留 标题 + 备注（固定两行）**、去除全部元信息与正文摘要；**中栏宽度 -8%**（真实像素比值 0.90–0.94）；卡片/表格视图不受影响）
  + **AC-70 / AC-71** + **D-28**（排序模型：自定义档 + 仅同层级重排）。
- **v28 2026-09-20（用户两条：备注纯文本 + 标题下展示备注）**：新增 **FR-68**（备注=纯文本：编辑器 Markdown 预览去掉「备注」字段；
  详情页「备注」页签按原样文本显示、不解析 Markdown、不请求 `/api/render/markdown`）+ **FR-69**（详情面标题行之下、字段页签行之上新增 `pm-detail-notes`：
  纯文本、13px、次级灰、行高 1.6、与标题间距 ≤10px、最多 2 行省略 + `title` 全文、空备注不占位；样式依据 PromptHub 实测：比标题小 4–6px、浅灰、纯文本、紧凑）
  + **AC-68 / AC-69**。
- **v27 2026-09-20（用户两条：待办池 R-1 + 新增修改密码）**：新增 **FR-66**（详情页去掉冗余的「当前字段 + 预览」头部：`MarkdownPreview` 在 `fields.length === 1` 时隐藏头部；
  编辑器页 3 字段保持现状）+ **FR-67**（`⋯更多` 删除「已登录：<用户名>」只读项、**原位置**改为「**修改密码**」弹窗；新增 `POST /api/password` 契约；
  **当前密码错误返回 400（明确不用 401，避免前端误判会话失效）**；新密码 ≥8 字符且不得与当前相同；错误计数复用登录限流（5 次/60s → 429）；
  **改密码成功后保留当前会话、吊销其它会话**；CLI `user set-password` 行为不变）+ **AC-66 / AC-67**；并**修订 FR-48 的菜单项顺序**（「已登录信息」由「修改密码」取代）。
- **v26 2026-09-19（用户定义环境划分）**：新增 **D-27 三层环境（开发 / 测试 / 生产）** —— 开发环境 = 实现方临时实例（数据可随意重建，dsh 管）；
  测试环境 = 228 systemd 8767（**数据由 host_manger 管理、可随意改动，用户亦会改动**）；生产环境 = 未来公网（数据主要由用户操作）。
  连带口径更正：此前写作「生产/生产库/上线」而实指 8767 的表述，一律按**测试环境**理解；本项目现阶段的"上线"= 同步到测试环境。
- **v25 2026-09-19（用户三条反馈）**：**FR-63** 撤销 FR-62 的「浏览器全屏 + 隐藏外壳」形态，改为**应用内全屏**（只隐藏编辑器内部 `editor-list`、
  编辑区与右栏 **1:1** 平分、按钮与详情页 `pm-detail-fullscreen` 同款、`Esc` 只退全屏）+ **AC-63**；
  **FR-64** FIX 版本 diff 的**尾部换行假变更**（`snapshotText` 不以换行结尾 + 字段尾部空白未归一化 → `[notes]` 必然中招；附复现原文）+ **AC-64**；
  **FR-65** FIX **复制在内网 HTTP 下失效**（评估结论：机制限制已有兜底但 `VariablePanel` 绕过 → 判为 bug；统一到 `use-copy` 的 `writeClipboard`）+ **AC-65**（**必须用内网 IP 验收**）。
- **v24 2026-09-19（用户追加需求）**：新增 **FR-62**（**编辑器全屏**：Fullscreen API 优先、失败自动退回应用内全屏；隐藏顶栏/侧栏/列表；
  编辑区高度 ≥ 视口 70%、编辑区与右栏独立滚动、工具栏常驻；退出三路径 = 按钮 / `Esc` / 浏览器退出并同步 UI；与详情面「全屏展开」互不顶替）
  + **AC-62**（真鼠标 + 真实尺寸测量 + 大文本 5000 字自测 + Esc 不丢内容），并入**阶段 18** 同批交付。
- **v23 2026-09-19（收尾清理，用户要求"一起处理"遗留项）**：新增 **FR-60**（注释与用户可见文案的漂移清理：`Workspace.tsx:73` 过期注释、
  `app.css` 旧术语「使用视图/管理视图」、`pure.ts` 小写品牌名文案；**只改表述不改行为**，并给出**不许改的白名单**：历史文档/`app` 契约值/文件名/历史测试断言）
  与 **FR-61**（构建体积：主 chunk 1,265KB → ≤500KB，允许懒加载与 manualChunks，**禁止 CDN**）+ **AC-60 / AC-61**。
- **v22 2026-09-19（用户第三次定调品牌图形）**：新增 **FR-59**（全站共用一枚提示符 `>_` 图标，只换尺寸：favicon 家族 / 顶栏 mark 26 /
  登录页 96 / 空态 72 / 关于页 48）、**AC-59**、**D-26**；**作废**插画方案（用户提供的第三方图与 host_manger 自绘版均不用）；
  附：AC-59 明确要求「装饰位真鼠标点击无动作」「空态有数据不出现」「引用单张 ≤250KB」。
- **v21 2026-09-19（用户八条反馈，阶段 16）**：新增 **FR-51**（Logo 改 `PromptManager` 且可点回主页，复位视图/搜索/筛选/选中）、
  **FR-52**（关于页重构：一行状态条 + 服务/使用/维护三分区，删重复与内部引用）、**FR-53**（删「拿来就用」装饰标题，实测省 ~37px）、
  **FR-54**（**P0 FIX**：非空文件夹删除**静默失败** —— 我方真鼠标复现，服务端 409 被前端吞掉，要求可见反馈/前置禁用 + 浮层真可点自证）、
  **FR-55**（编辑页按来源返回详情）、**FR-56**（编辑页右栏顺序 = 预览 → 变量 → 版本）、**FR-57**（收藏显性化：四屏常驻可点，**表格行补上**）、
  **FR-58**（跟随系统图标改 太阳+月亮 组合，P2）；新增 **AC-51~AC-58** 与 **D-25**。
  附：AC-54/AC-57 明确要求**用真鼠标事件**验收（JS `.click()` 测不出"是否真的点得动"——这是阶段 15 验收的方法漏洞）。
- **v20 2026-09-19（用户四条反馈）**：新增 **FR-47**（顶栏只留 品牌·`＋新建`·`⋯更多`·主题切换·登出 并固定顺序；**删掉登录用户信息**）、
  **FR-48**（`⋯更多` 子项顺序 使用统计→API 令牌→导入/导出→**关于**→已登录信息→登出；**删除「文件夹与标签」入口**；「设置」改名「关于」但 testid 不变）、
  **FR-49**（文件夹区标题行常驻 `+` 新建；行内悬浮 新建子文件夹/重命名/删除；层级显示优化：展开三角、按级缩进、文件夹图标两态、右侧条目数、选中态）、
  **FR-50**（标签区改 **胶囊云**：`#` 前缀、圆角胶囊、流式换行、**去计数徽标**）；新增 **AC-47/48/49/50** 与 **D-23/D-24**；
  并**修订** FR-43 中「顶栏含用户/登出、`⋯更多` 内放文件夹与标签、左栏只读」三处表述（划删并指向新条款）。
- **v19 2026-09-19（用户反馈：分栏视图 + 删除列表视图）**：新增 **FR-46**（三栏分栏视图 = 现有左筛选栏 + 中列表 + 右详情面；
  **默认视图改为分栏**；视图开关按「分栏 / 表格 / 卡片」顺序；**删除「列表」档**；移动端单栏降级）+ **D-22**（分栏右栏复用详情面 ⇒ D-21 放宽）
  + **AC-44 / AC-45 / AC-46**；并**清理两处过期内容**：FR-41b 视图档位里的「列表」、固定 testid 清单里已随 FR-43 移除的 `pm-mode-use` / `pm-mode-manage`；
  同时修订 AC-41 ⑤ 的措辞（「删除只在表格里」→「表格行内 + 详情面」）。
- **v18 2026-09-19（阶段 13 验收后的规格对齐，仅文档）**：① **AC-34 显式标注作废**（管理页已取消）；
  ② **AC-31 显式标注部分作废**（`pm-kpi-row`/`pm-filter-row`/`pm-statusbar` 随 FR-43 移除），保留 `pm-topnav`/`pm-sidebar`/编辑器三栏；
  ③ **testid 漂移对齐**：卡片网格容器 `pm-use-grid` → `pm-use-viewmode` + `pm-view-card`（行为等价，实现侧命名演进，经我实测确认）。
  目的：避免后续按已失效的旧 AC 判定（这三条是"规格随产品演进"的正常作废，不是实现缺陷）。
- **v17 2026-09-19（用户三条反馈）**：①「**实际上我们并不需要一个管理页面**…完全可以放表格视图里面」→ **FR-43**（取消双模式与模式记忆；
  编辑/删除并入表格视图行内；顶栏只留 品牌·主题图标·＋新建·⋯更多·用户；使用统计/API令牌/导入导出/设置 收进 ⋯更多；左栏仍只读筛选）+ **D-21**；
  ②「**我们还是需要明暗主题切换按钮，跟随主题是默认行为…只需要一个图标**」→ **FR-44**（`pm-theme-toggle` 图标按钮，点击循环 亮→暗→跟随系统，默认跟随系统）；
  ③「**有 bug，新建 prompt 没有保存就自动保存了**」→ **FR-45**（附 host_manger 复现：点新建即 +1「未命名 prompt」、Esc 后仍留库；
  要求未点保存不得入库，草稿只能在前端 localStorage）。
  新增 **AC-41 / AC-42 / AC-43**；**AC-34 自本版作废**、**AC-38 由 AC-41 修订取代**（避免按旧标准判定）。
- **v16 2026-09-19（用户反馈两条设计问题）**：用户原话「①**导航太不合理，很多属于管理的东西应该放到管理页面，类似于设置**
  ②**很多页面和卡片都有一些提示信息，比如数据库信息，监听信息，操作提示的过量意思等**」→ 新增 **FR-42a（使用视图只留"用"：顶栏去 环境标识/使用统计/API 令牌/导入导出/新建；左栏仅筛选、不要管理按钮；管理视图 + 设置入口）**、
  **FR-42b（信息克制：`DATA_DIR`/接口路径/SQLite/监听/查询耗时/筛选命中/渲染不写库/version_no/操作说明 等解释性文案从可见 UI 下线，
  统一收进「设置/关于」或 tooltip；保留操作反馈与危险确认；卡片删内部 id；状态条移出使用视图）**、testid `pm-settings`/`pm-about` 与 **AC-37~AC-40**、**阶段 12**。
  取证依据：host_manger 在生产 UI 上 dump 的可见文本清单（顶栏含 `使用统计/API 令牌/导入 / 导出/新建` 与 `127.0.0.1:8767`；
  左栏含「点文件夹 = 只看该文件夹的 prompt；再点一次取消筛选。」；卡片含 `#1`；状态条含 `数据在本机 SQLite 单文件 · DATA_DIR/pm.db 备份 = 拷文件，或 GET /api/export … 监听 127.0.0.1:8767 · 本次查询 27 ms`）。
- **v15 2026-09-19（用户对未填变量行为的决定）**：用户原话「**没填写的变量按照原样输出，让 dsh 在新会话做**」→
  ① §4 FR-41e 第 2 条补明：未填变量在**预览与复制结果里原样保留 `{{name}}`**，前端**不得传空串**（服务端 §6.5 早已如此，属前端修）；
  ② §8 AC-33b 的期望随之更新（填一个、留一个 → 预览/剪贴板里未填的仍是 `{{name}}`）。
- **v14 2026-09-18（PromptHub 实页考察后）**：用户授权后 host_manger **登录 PromptHub（203:3871）逐屏查看**（用其口令；为自动化浏览，
  经用户同意**临时关闭其登录验证码** `AUTH_CAPTCHA_ENABLED=false`，改动记在部署侧，可一行回滚）。
  据实页新增 **FR-41e（吸收：详情底部固定操作条 / 复制时的填变量对话框+实时预览+自动记忆 / 纯文本·编辑预览·全屏 / 视图切换与密度与列设置 /
  版本历史三视图与变更备注）**、**FR-41f（摒弃：Skills·Rules 模块 / AI Test·AI 改写·图片反推·AI Response 列 / Image 类型与媒体 /
  关系图谱 / Relations·Custom Output Format / 云同步多端 / i18n）**、4 个新的固定 testid，以及 **AC-33b / AC-33c**。
- **v13 2026-09-18（用户纠偏：使用优先）**：用户指出"**服务是给管理员日常使用的，不是只用来管理的**"，具体为
  ①**连最基本的复制都没有** ②**界面排版没有考虑使用与管理分离** ③**大部分场景是"使用"而不是"管理"** ④建议参考 PromptHub 页面。
  据此新增 **FR-41（含 FR-41a 一键复制 / FR-41b 使用·管理分离 / FR-41c 快捷 / FR-41d 移动优先）**、
  **AC-33~AC-36**、**D-20（产品定位）** 与 **阶段 11**；并把 host_manger 实测的 **PromptHub 使用侧文案频次与视图模块清单**写进 §4 作为参考依据。
  ⚠️ 本版**不改**接口契约/数据模型（复制＝取用复用 FR-19 的 usage；不新增接口）。
- **v12 2026-09-18（用户选定方向 A）**：§4 新增 **FR-40b「结构/排版重设计必须落地」**（含 5 条结构改动清单 + 9 个固定 `data-testid` +
  「设计稿即契约、降级必须列清单」）；§8 新增 **AC-31**（结构落地的机械判据：源码 testid + 运行时三栏同时可见 + 详情面 `.ant-tabs-tab`=0 + KPI 数字 ≥4）、
  **AC-32**（≥4 组"设计稿 vs 实现"并排对照 + 降级清单）；§9 新增 **D-19**（方向 = A 为主 + 登录页按 B 极简，用户选定）；
  §11 更新阶段 10B 范围与 AC。用户原话：「**我选择 A**，我发现他不仅仅好了样式还对排版进行了重新设计，**这才是我需要的设计，让他保持这种设计感**」。
- **v11 2026-09-18（用户追加 UI 需求）**：用户反馈"**目前的 UI 效果不太行、太单调，需要设计感和美感**"→
  §4 新增 **FR-40 视觉重设计**（明确"先定方向→打样→用户挑选→再铺开"的流程、允许手段、五条禁止项、四条不得回归项、美学要求）；
  §5 补实现边界；§8 新增 **AC-29**（token 真被定制 / 无 CDN / 组件库约束不回归）、**AC-30**（全站覆盖 + 逐张识图 + 修前修后成对）；
  §11 阶段表新增 **阶段 10A（打样）/ 10B（全站应用）**。
  ⚠️ 本版**不改**任何接口契约/数据模型/既有功能行为——**纯表现层**变更；重设计上线由 host_manger 在阶段门后同步 `/opt` 并重启复验。
- **v10 2026-09-18（首次真实部署后新增 = FIX-1）**：§5 增「**unit 必须允许 `AF_NETLINK`**」硬约束（+ `deploy/README.md` 必须有排查条目）；
  §8 的 AC-18 增补第 ⑤ 条静态断言，并**明确运行时不兼容只能靠"真实 unit 真起一次"发现**（由 host_manger 在部署时执行）。
  来源：`VERIFY.md` 部署记录 —— 首次部署时服务在 systemd 下启动即崩（`errno 97 EAFNOSUPPORT`，`os.networkInterfaces()` 需要 netlink），
  已由 host_manger 用 drop-in 先行修补，本版把修复与断言回填到交付物。
- **v9 2026-09-18（阶段 8 验收后新增）**：§4 新增 **FR-10b 导入校验收紧**（拒绝缺 `title`/`user_prompt` 的条目）。
  来源是阶段 5 验收时记录、阶段 8 前端完成后重新评估的加固项：既然导入是"外部内容进库"的唯一入口，校验就该在后端做死。
- **v8 2026-09-18（阶段 7 验收后新增）**：① §4 新增 **FR-11b**（导入界面二次确认，`replace` 模式必须显式提示"将清空现有数据"）；
  来源是阶段 5 验收发现"合法空文件 + replace = 清空整库"，判定为**后端契约正确、UI 需加护栏**。
  ② 同轮验收记录一条**遗留小项**：`bin/pm-mcp.mjs` 的 git mode 是 `100644`、工作区权限 600，与项目自身约定
  （`2c82c9f`「CLI/脚本置 755」）及 `bin/pm.mjs`(755) 不一致 → 列入阶段 8 顺手修正，不影响本轮判定。
- **v7 2026-09-18（阶段 6 验收中确认的规格修正）**：**Token 子命令的引导路径**。
  背景：v5/v6 的 FR-16 写「`pm token *` 一律走 HTTP API」，与 AC-22 ① 的夹具（只设 `DATA_DIR`/`PORT`、没有任何 HTTP 凭据）**自相矛盾** ——
  严格执行会陷入"没有 token 就无法创建第一个 token"。dsh 在阶段 6 开工前**主动把这个张力写成决策请我复核**，并实现为：
  `pm get`/`pm render` 纯 HTTP；`pm token *` 双通道且**把所用通道打印到 stderr**（不静默）。
  **判定：采纳其设计，并据此修正规格**（§4 FR-16、§5 CLI 段、AC-22 ① 措辞）。这是"实现方发现规格缺陷 → 改规格而不是改实现"的正面案例。
  另：同阶段实测确认 `PUBLIC_ORIGIN`/`TRUST_PROXY`/`CORS_ORIGINS` 三开关行为与 D-18 一致，无需改。
- **v6 2026-09-18（用户三条决定）**：① **T-1 结案：内网与公网都要支持** → §3/§5 增"部署形态就绪"
  （服务不做 TLS；`TRUST_PROXY` **默认关闭**、`PUBLIC_ORIGIN` 控制 cookie `Secure`；交付 `deploy/reverse-proxy.example.conf`
  与"内网直连 / 公网反代"双形态部署文档；**实际暴露仍只由 host_manger 执行**），新增 **AC-28**；
  ② **FR-25 使用记录提升为 P0**（重编为 **FR-19**，见 §5 契约与 **AC-27**：只记取用、通道分 session/token/mcp、
  **不得产生版本/不得改 updated_at**、**不参与导出**，Prompt 对象新增 `use_count`/`last_used_at`）；
  ③ **客户端明确不做**：§3 改写为"本项目只交付**服务端 + 可对接面**，且**不得为任何特定客户端做假设或特化**"；
  ④ §9 增 **D-17/D-18**、T-1 标记**已结案**、新增 **T-2（两实例数据关系：默认不做同步）**；
  ⑤ §11 **阶段 6 扩为"对外可用面"**（Token/CORS/CLI/usage/部署形态），阶段 7/8/9（MCP/前端/收尾）不变。
- **v5 2026-09-18（用户拍板「统一做 token/CORS 与 MCP」）**：把「**使用侧**」正式纳入范围 ——
  ① §1 增「成功标准 = 被用出去」；② §3 明确**浏览器扩展 / 桌面客户端不在本期**（各自单独立项），并写明本期不做公网暴露；
  ③ §4 新增 **FR-15~FR-18**（Token / 使用侧 CLI / CORS / MCP），P1 新增 **FR-25 使用记录**；
  ④ §5 新增「**外部客户端就绪**」硬约束（token 只存 sha256、CORS 禁 `*`、CLI 与 MCP 一律经 API、MCP stdio 不占端口、**先对齐真实对端协议版本**）；
  ⑤ §7 新增 `bin/pm-mcp.mjs`、`src/mcp/**`、`deploy/mcp-register.example.json`；⑥ §8 新增 **AC-22~AC-26**；
  ⑦ §9 新增 **D-13~D-16** 与**待定项 T-1（外网可达性）**；⑧ §11 阶段表重排：**6 = 客户端就绪、7 = MCP、8 = 前端 P0（原阶段 6）、9 = 收尾（原阶段 7）**。
  ⚠️ **阶段号有位移**：dsh 关于「阶段 7 = 收尾」的旧笔记以本表为准；**阶段 5 的 AC（AC-10/11）不受影响**。
- **v4 2026-09-18（阶段 4 验收发现）**：新增「Markdown 净化硬化」条款（§5）。
  背景：阶段 4 的 AC-12（不含 `<script`、不含 `javascript:`、含 `pre/code` + 高亮）**已通过**，
  但我用 12 类 payload 复测时发现**非脚本类**内容会存活（`style` 属性、`<style>/<form>/<input>/<math>/<mtext>`）——
  属**低危但真实**的 UI 伪装面，且阶段 5 引入导入后风险具体化。**作为硬化要求写进规格，不判阶段 4 失败。**


- **v3 2026-09-18（阶段 1 施工中，仅改 BRIEF，未动代码）**：据 dsh 的 `docs/search-zh.md` 复核结果更正 §6.8 的
  `unicode61` 数字（原写"全 0 命中"，实际**随语料变**：`会话交接` 在其语料下 1 命中，成因是全角冒号造成分词边界）——
  **结论与 D-3 不变**（必须 trigram + <3 字符 LIKE 兜底）；同时把权威记录指向 §2。
  起因：dsh 主动、如实报告了与我方数字的差异（诚信信号）；我据此修正规格而不是让它去迎合错误数字。
- **v2 2026-09-18（用户追加技术栈要求）**：「**前端页面必须使用框架的组件库，不要自己造轮子**」→
  ① §5 前端节改写：UI 一律用 **Ant Design（antd 6.6.4 + @ant-design/icons 6.3.4，均 MIT，React 19 原生支持）**，
  禁止自建/拷源码改，禁止引第二套样式体系（默认不引 Tailwind），禁止 CDN；
  ② 新增 **D-11 / D-12**（组件库选型 + 阶段 1 就落地）；③ 新增 **AC-20（组件库落地机械证据）/ AC-21（组件库真的在渲染）**；
  ④ 阶段 1 与阶段 6 的范围相应更新；⑤ 同规则已写入合集 `STANDARDS.md` §4.1/§4.2（对所有项目生效）。
  ⚠️ **未走 dsh 交接前的口径**：`antd` 版本在阶段 1 安装前须用 `npm view antd version` 复核（本次实测 6.6.4 @ 2026-09-14）。
- v1 2026-09-18 初版（host_manger）：基于用户 09-18 决策（clean-room 重做轻量 PromptManager）+ 上游体量实测
  + 228 真机 POC（中文 FTS5 方案 / trigram 长度下限 / 截图能力 / 端口）。
